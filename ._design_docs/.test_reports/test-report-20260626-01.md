# Stage 26 test execution (Metrics Alignment + Stage 24/25 Carry-Over Resolution)

RunId: `stage26-rerun-20260626-01`
RunRoot: `D:\source\llama.cpp-jet\._test_output\stage26-rerun-20260626-01`
Route: `/v1/chat/completions`
Date: 2026-06-26
Status: PARTIAL

Implementation: D26-IMPL-PASS (12 steps delivered by Developer, build clean,
137/137 unit tests pass).

## Per-row verdict table

| Row ID | Verdict | Failure classification | Evidence |
| --- | --- | --- | --- |
| TP-26-UT-01 cold_metric_tracks_per_id_bytes | PASS | none | `test-cache-controller: Stage 26 cold metric tracks per-id bytes... PASSED` |
| TP-26-UT-02 cold_metric_decrements_on_evict | PASS | none | `test-cache-controller: Stage 26 cold metric decrements on evict... PASSED` |
| TP-26-UT-03 cold_metric_decrements_on_cleanup | PASS | none | `test-cache-controller: Stage 26 cold metric decrements on cleanup... PASSED` |
| TP-26-UT-04 cold_metric_no_double_count_on_redemote | PASS | none | `test-cache-controller: Stage 26 cold metric no double-count on redemote... PASSED` |
| TP-26-UT-05 cold_payload_files_count_matches_disk | PASS | none | `test-cache-controller: Stage 26 cold payload files count matches disk... PASSED` |
| TA-26-FA-01 SEH activation | PASS | none | `D:\tmp\test-crash-dump\llama-server-19360-20260626-003941.dmp` (223276 bytes); dbghelp linked; `--crash-dump-dir` accepted by CLI parser |
| TA-26-FA-02 label uniqueness | PASS | none | 0 duplicate `,mode=".*",mode="` and 0 duplicate `,scope=".*",scope="` patterns across 1213 metric lines |
| TA-26-FA-03 metrics format compliance | PASS | none | 0 old-format `^llamacpp_` (underscore) lines; 404 new-format `^llamacpp:` (colon) lines |
| TA-26-FA-04 runner MetricNames scrape | PASS | none | all 10 MetricNames entries found 3+ times across 4 legs in `metrics-after.txt` |
| TP-26-IT-01 Stage 24 rerun (S02 + S03) | PARTIAL | S02 PASS, S03 FAIL | `S02-chat/comparison.json` (PASS), `S03-chat/comparison.json` (FAIL-http-request) |
| TP-26-IT-02 D-EXEC-24-03 reproduction | REPRODUCED | server died mid-leg at request 258 | `S03-chat/hybrid-stage24/server.err.log` (no FATAL/OOM/SEGV/exception; last OK `s03-exact-0-0` cache_n=15, first error `s03-exact-0-1`); no SEH dump captured (runner does not pass `--crash-dump-dir`) |
| TP-26-PF-01 cross-stage latency (PF-03) | DELTA-RECORDED | S02 hybrid +454.81 ms median vs native; S03 hybrid +76.16 ms median vs native | `S02-chat/comparison.json` + `S03-chat/comparison.json` `timing_delta` |

## Build evidence

- `build-cuda/bin/Release/test-cache-controller.exe` mtime `2026-06-26 00:27:15`,
  size 147.95 MB
- `build-cuda/bin/Release/llama-server.exe` mtime `2026-06-26 00:30:09`,
  size 160.88 MB
- `build-cuda/CMakeCache.txt` `GGML_CUDA:BOOL=ON` (verified)
- Stage 26 implementation includes new `tools/server/server-crash-handler.{h,cpp}`
  mtime `2026-06-26 00:29:57` (per Developer implementation log)

## Test execution summary

- Focused test binary: `build-cuda/bin/Release/test-cache-controller.exe`
- Output: `._test_output/stage26-atomic-20260626-01/test-output.txt`
- Result: **137 / 137 PASS**, exit 0
- Test buckets per test summary:
  `31 original, 5 Part 14, 4 Stage 4, 4 Stage 5, 5 Stage 6, 4 Stage 7,
   7 Stage 9, 9 Stage 10, 3 Stage 10 2026-06-04 T114, 15 Stage 17,
   2 Stage 18, 6 Stage 21, 9 Stage 23, 15 Stage 22, 2 Stage 24,
   10 Stage 25, 5 Stage 26 cold-store accounting (total 137)`
- Stage 26 unit tests (TP-26-UT-01..05) all PASS

## Fixture assertion results

| ID | Check | Result | Evidence |
| --- | --- | --- | --- |
| TA-26-FA-01 | SEH activation | PASS | `dumpbin /dependents` lists `dbghelp.dll`; `--crash-dump-dir` accepted by `common_params_parse` (pre-scan splices out before parse, exit code 0 for `--help`); smoke trigger with `--crash-dump-dir D:\tmp\test-crash-dump --model nonexistent.gguf` caused STATUS_ACCESS_VIOLATION (exit -1073741819) and SEH filter wrote `llama-server-19360-20260626-003941.dmp` (223276 bytes) |
| TA-26-FA-02 | Label uniqueness | PASS | `Select-String ',mode=".*",mode="'`: 0 matches; `Select-String ',scope=".*",scope="'`: 0 matches across 1213 metric lines from all 4 legs' `metrics-after.txt` |
| TA-26-FA-03 | Metric format compliance | PASS | `Select-String '^llamacpp_'`: 0 matches (old underscore format absent); `Select-String '^llamacpp:'`: 404 matches (new colon format) |
| TA-26-FA-04 | Runner MetricNames scrape | PASS | All 10 entries in `$MetricNames` array in `stage24-chat-s02-s03-comparison.ps1` (lines 26-36) are present in emitted `/metrics` endpoint output (3 matches each per leg across 4 legs; `cache_checkpoint_admissions_by_shape_total` has 5 matches per leg because multiple shape label values) |

## Integration evidence (TP-26-IT-01)

### S02-chat native-legacy

- planned: 16, observed: 2516, status_counts: `{"200":2516}`
- cache_n nonzero_rate: 0.9984 (2512 / 2516)
- CUDA runtime proof: PASS (RTX 5060 Ti x 2)
- cleanup: PASS (PID 4068 stopped, port 8900 released)

### S02-chat hybrid-stage24

- planned: 16, observed: 1420, status_counts: `{"200":1420}`
- cache_n nonzero_rate: 0.0225 (32 / 1420)
- cold_budget.metric_bytes_after: 0 (gauge reports 0 demoted bytes)
- cold_budget.filesystem_bytes_after: 0 (cold path disk usage 0)
- cold_budget.state: PASS-filesystem-fallback
- prompt_evidence.records: 1420, profiles: `{"checkpoint_dependent":1420}`,
  lookup_outcomes: `{"exact_entry_absent":1388,"hit":32}`
- CUDA runtime proof: PASS
- cleanup: PASS (PID 35604 stopped, port 8900 released)

### S03-chat native-legacy

- planned: 256, observed: 1523, status_counts: `{"200":1523}`
- cache_n nonzero_rate: 0.9980 (1520 / 1523)
- CUDA runtime proof: PASS
- cleanup: PASS (PID 27484 stopped, port 8910 released)

### S03-chat hybrid-stage24 (FAIL)

- planned: 256, observed: 258 (status_counts: `{"200":257,"request-error":1}`),
  state: `aborted-server-unreachable-after-health`
- last OK: `s03-exact-0-0` (request 257), cache_n=15
- first failed: `s03-exact-0-1` (request 258), error
  `An error occurred while sending the request.`
- last server.err.log line: `2.29.644.865 I srv params_from_: Chat format: peg-native`
- last cache state: 10 entries, 502.506 MiB payload, 637 tokens
  (under 512 MiB budget and 4096 token limit)
- cold_budget.state: PASS-filesystem-fallback
- prompt_evidence.records: 257, profiles: `{"checkpoint_dependent":257}`,
  lookup_outcomes: `{"exact_entry_absent":252,"hit":5}`
- CUDA runtime proof: PASS
- cleanup: PASS (PID 33356 stopped, port 8910 released)

## Cold-store drift ratio (TA-26-FA-04b)

- S02 hybrid: metric_bytes_after = 0, filesystem_bytes_after = 0
- S03 hybrid: metric_bytes_after = 0, filesystem_bytes_after = 0
- ratio: undefined (0 / 0); both budgets PASS via filesystem fallback
- Note: hybrid log shows `cold store I/O worker: demotion queue full (32/32)`
  warnings in both hybrid legs; cold demotions did not complete. Cold-store
  metric stays at 0 because the per-id accounting fix correctly credits zero
  bytes when no demotion succeeds. This is a separate observation from the
  Stage 25 atomic-transactional demote-throughput behavior, not a Stage 26
  cold-store accounting regression.

## Metrics format check (TA-26-FA-03)

- `^llamacpp_` (old underscore format): 0 matches across 1213 metric lines
- `^llamacpp:` (new colon format): 404 matches
- Sample first 10 colon-form names: `llamacpp:cache_branch_lookup_hits_total`,
  `llamacpp:cache_branch_lookups_total`,
  `llamacpp:cache_branch_metadata_admission_rejections_total`,
  `llamacpp:cache_branch_nodes_created_total`,
  `llamacpp:cache_branch_pruned_metadata_bytes_total`,
  `llamacpp:cache_branch_pruning_total`,
  `llamacpp:cache_branch_traversals_total`,
  `llamacpp:cache_budget_branch_metadata_bytes`,
  `llamacpp:cache_budget_branch_metadata_over_limit`,
  `llamacpp:cache_budget_branch_metadata_ratio`

## Label uniqueness check (TA-26-FA-02)

- 0 duplicate `,mode=".*",mode="` patterns across 1213 metric lines
- 0 duplicate `,scope=".*",scope="` patterns across 1213 metric lines
- The `mode="hybrid"` label appears as a single label per metric series
  (e.g. `llamacpp:cache_cold_payload_bytes{mode="hybrid"} 0`), no duplicate
  `mode` label collisions.

## D-EXEC-24-03 status (TP-26-IT-02)

- REPRODUCED in Stage 26 rerun (S03 hybrid)
- Crash signature:
  - last OK request: `s03-exact-0-0` (request 257 of 256 planned + extend),
    cache_n=15, total_ms=267.842
  - first failed request: `s03-exact-0-1` (request 258), status=request-error
  - server.err.log ends with `2.29.644.865 I srv params_from_: Chat format: peg-native`
    (chat-format normalization, no FATAL/OOM/SEGV/exception line)
  - last cache state line: `10 entries, 502.506 MiB payload, 502.508 MiB total, 637 tokens`
    (within 512 MiB budget and 4096 token limit)
- Stage 24 -06 vs Stage 26 -01 S03 hybrid crash:

| Run | Last OK request | First failed | Cache state at death | Demote queue |
| --- | --- | --- | --- | --- |
| -06 | s03-new-5 (req 280) | s03-exact-6-0 (req 281) | 4073 tokens / 505 MiB | 0 demote warnings |
| -01 | s03-exact-0-0 (req 257) | s03-exact-0-1 (req 258) | 637 tokens / 502 MiB | `demotion queue full (32/32)` warnings |

- SEH dump path: **none captured** during this run
  - Reason: `stage24-chat-s02-s03-comparison.ps1` does NOT pass
    `--crash-dump-dir` to `llama-server.exe` (lines 933-934:
    `$args = $LegPlan.server_flags + @('--model', ...)`; no
    `--crash-dump-dir` flag present).
  - The SEH infrastructure is verified separately in TA-26-FA-01 (smoke
    trigger captured `D:\tmp\test-crash-dump\llama-server-19360-20260626-003941.dmp`).
  - Per test plan part-07 row `TP-26-IT-02`: hybrid leg "PASS or
    crash-with-dump" - this rerun is `crash-without-dump` because the
    runner script does not enable the SEH filter. The crash itself
    reproduces as expected (FAIL classification unchanged from -06).

## PF-03 perf comparison (TP-26-PF-01)

| Leg | Native median (ms) | Hybrid median (ms) | Delta (ms) | Hybrid p95 (ms) |
| --- | ---: | ---: | ---: | ---: |
| S02-chat | 402.37 | 857.18 | +454.81 | 993.76 |
| S03-chat | 280.28 | 356.44 | +76.16 | 421.80 |

- Stage 24 -06 baseline: S02 hybrid and S03 hybrid had no per-request
  median timing reported (only wall-clock + per-leg totals). Stage 25 -01
  S03 hybrid crashed at request 281 with no completed hybrid median.
- Stage 26 -01 hybrid medians: S02 hybrid 857 ms; S03 hybrid 356 ms
  (computed over 1420 / 258 OK responses respectively).
- Classification: both legs `slower` (hybrid median > native median).
  Within-25% threshold cannot be evaluated against a numeric Stage 24
  baseline because that baseline was never recorded as a hybrid median;
  treat PF-03 as `delta-recorded` rather than `closed`.

## CUDA proof

- `build-cuda/CMakeCache.txt` `GGML_CUDA:BOOL=ON` verified.
- All 4 legs reported `cuda_runtime_proof.state = PASS` with
  `NVIDIA GeForce RTX 5060 Ti (16310 MiB, 15172 MiB free)` matches in
  server.err.log lines 3-6 of each leg.

## Leak scan

- Per-leg leak scan: PASS, hit_count: 0
- `final-leak-scan.json`: PASS, hit_count: 0

## Cleanup proof

- All 4 legs report `cleanup.state = PASS`. Runner-owned PIDs (4068, 35604,
  27484, 33356) stopped; ports 8900 and 8910 released between legs.
- S03 hybrid server process exit detected and cleaned up by runner.

## Product bugs (handed off)

- **D-EXEC-24-03 (carry-over)**: S03 hybrid server dies silently mid-leg
  at request 258 (vs request 281 in -06). Different request index,
  smaller cache state at death (637 tokens vs 4073). Same Qwen3.5-4B MTP
  fixture, same hybrid flags, same workload, same chat route. The
  Stage 26 SEH infrastructure did not capture a dump because the runner
  script does not pass `--crash-dump-dir`. New observation: S02 hybrid
  log shows `demotion queue full (32/32)` warnings; cold demotions are
  not completing in either hybrid leg under the post-fix binary.
- **R26-OBS-01 (new)**: cold demotion queue saturation. The Stage 25
  atomic-transactional demote behavior under high concurrent hybrid
  workload may have reduced demote throughput below the cache eviction
  rate, leaving the cold-store accounting metric at 0. Not a Stage 26
  regression (cold-store accounting correctly reports 0 when no
  demotion succeeds) but a downstream observation about hybrid cache
  cold-write throughput.

## Runner blockers

- 0 runner blockers. The runner completed all 4 legs without exception
  and wrote the durable report at the whitelisted path.
- Live log: `D:\source\llama.cpp-jet\._test_output\stage26-rerun-20260626-01-live.log`
  final line: `Stage 24 runner complete: report=...test-report-20260626-01.md
  runRoot=...stage26-rerun-20260626-01`

## Hard-constraints check

- Production code unmodified (no edits to `tools/server/server-*.{cpp,h}`,
  `tests/test-cache-controller.cpp`, `tools/server/CMakeLists.txt`,
  `stage24-chat-s02-s03-comparison.ps1`).
- ASCII only, LF line endings (no CR bytes), no BOM.
- No git commit or push performed.
- Tracker and document-index untouched (Manager owns).
- ASCII bytes only verified via `[System.IO.File]::ReadAllBytes` on
  this report (no high-bit bytes detected).

## Handoff

- Status: PARTIAL
- 137 / 137 unit tests PASS; Stage 26 cold-store accounting unit tests all
  PASS.
- 4 / 4 fixture assertions PASS (SEH infrastructure verified separately
  via smoke trigger).
- Stage 24 rerun: S02 PASS, S03 FAIL (D-EXEC-24-03 reproduces; crash
  signature and request index differ from -06 but root cause still
  undiagnosed without SEH dump).
- D-EXEC-24-03 carry-over product bug remains OPEN. R26-OBS-01
  (demote queue saturation) recorded as new observation. PF-03
  delta-recorded, not closed.
- Ready for test-results review: yes
- Next handoff: Manager + Developer (D-EXEC-24-03 root-cause
  investigation, optional `--crash-dump-dir` flag in runner for
  next rerun, demote-throughput investigation).
