# Stage 39 full QA execution report

Date: 2026-07-17
Gate: D39-QA-01
Verdict: BLOCKED
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260717-01/`

## Scope

Manager Part 139 authorized this fresh clean-build run. Product, fixture, and
contract changes were excluded. The run stopped costly coverage after
TP-39-03 fixed the closure verdict, as Part 139 requires.

## Environment and build

- HEAD: `2c501427cdc318b50d50fcfe329e71f90a9a1e89`; dirty Stage 39 tree recorded
  before report creation in `git-status-before.txt` and `git-diff-stat-before.txt`.
- Host: Windows x64, MSVC Visual Studio 18 2026, Release, CPU backend,
  `GGML_CUDA=OFF`, `LLAMA_STAGE39_LIVE_TEST_SEAM=ON`.
- Fixtures: Qwen3-0.6B Q8_0 for standard, TP-39-02, TP-39-04, negatives,
  and legacy; Qwen3.5-4B MTP Q4_K_M plus its checked-in template for TP-39-03.
- Clean build: removed `build` and `build-stage39-seam-on`, configured a new
  seam-ON Release tree, then built `llama-server` and all 11 focused targets
  named by `run_coverage.ps1`. Configure and build exited 0. Every required
  executable was younger than ten minutes at the focused-test start.
- Commands, versions, binary timestamps, fixture inventory, GPU inventory,
  memory, server arguments, and exclusions are saved under the evidence root.

All 11 focused executables passed. `ctest -C Release -R cache -V` passed 1/1.
The fresh guarded route run passed 148 tests with two reviewed terminal nodes
deselected. Those two nodes reuse Part 137's `2 PASS / 0 FAIL / 0 BLOCKED`
artifacts at `._test_output/stage39-route-fixture/exec35-midpoint` and
`exec35-step2`. Current `server-cache-hybrid.cpp/.h` and helper hashes are
exactly the Part 124/138 values, so the reused evidence is source-current.

## Results

| ID | Verdict | Evidence |
| --- | --- | --- |
| TP-39-01 | PASS | Fresh six-request standard run restored 71 tokens, emitted `retained_cold/cold_room` and `commit/none`, and kept eviction and pruning deltas zero. |
| TP-39-02 | PASS | Focused multi-victim fault matrix plus canonical live `Assert-Tp3902`; live summary records two decision series, one transaction series, and two cold files. |
| TP-39-03 | BLOCKED | Canonical MTP startup and exact 3631/3632 token preflight passed, but discovery returned two hot exact candidates and no checkpoint candidate in either cold set. Driver stopped at `SKIP-preflight-compatible-checkpoint-set`. Owner: Developer test-results review. |
| TP-39-04 | PASS | Canonical measured 8,259,716-byte resident and 8,373,860-byte serialized pair exceeded both 7 MiB lowered budgets; `Assert-Tp3904` passed. |
| TP-39-05 | PASS | Fresh cold-disabled and hot-zero summaries passed; hot-zero emitted no Stage 39 family. |
| TP-39-06 | PASS | Focused transaction fault boundaries passed with rollback/error reasons and retained hot state. |
| TP-39-07 | PASS | Focused target/draft full lifecycle passed. |
| TP-39-08 | PASS | Focused same-entry independent descriptor pressure passed. |
| TP-39-09 | PASS | Focused protected-root descendant pressure passed. |
| TP-39-10 | PASS | Focused concurrent production hot-pressure updates passed. |
| TP-39-11 | PASS | Fresh legacy run passed with both Stage 39 families absent. |
| TP-39-12 | PASS | TP-39-01 drove real `tx_save`, cold demotion, transaction commit, and restore. |
| TP-39-13 | PASS | Focused exact-fit, over, overhead, and checked-add overflow boundaries passed. |
| TP-39-14 | PASS | Focused transaction fault-position matrix and repeated restart-idempotence checks passed. |
| TP-39-15 | PASS | Focused typed cardinality passed; fresh live samples used fixed labels and bounded families. |

TP counts: PASS 14, FAIL 0, SKIP 0, BLOCKED 1.

The first TP-39-01 invocation used the stale 24-request README/default shape
and correctly hit the binding six-chat cap. QA changed no file and reran with
`-Requests 6`; that fresh run passed. Both attempts remain preserved.

## Coverage

Coverage verdict: BLOCKED. The four required PowerShell 7/5 success and forced
merge-failure blocks were not started after TP-39-03 fixed the stage verdict.
No merged artifact or real percentage exists, so the 80 percent gate is not
met. `coverage-stop.txt` records all four block classifications and the
Part 139 fail-fast reason.

## Handoff

Stage 39 does not close. Developer owns TP-39-03 result review: explain why
checkpoint payload bytes contribute to hot totals while guarded discovery
returns only exact candidates and empty cold sets for both owners. After a
reviewed correction and fresh Manager gate, QA reruns TP-39-03 and all four
coverage blocks. `artifact-verification.csv` hashes the evidence files, and no
`llama-server` process remained at handoff.
