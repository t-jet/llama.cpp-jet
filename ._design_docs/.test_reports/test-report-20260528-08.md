# Cache handling QA report: Qwen3.5 public MTP cache rerun

Test run ID: `test-report-20260528-08`
Date: 2026-05-28
Tester: QA agent
Plan: [cache-handling-test-plan.md](../cache-handling-test-plan.md)
Scope: focused public H53/H54 MTP cache coverage with the Qwen3.5 MTP fixture.

## Inputs reviewed

- Test plan H53/H54 rows in [Part 3](../cache-handling-test-plan/part-03-integration-test-matrix.md)
- Architecture Part 6: [Stage 5 draft context modes and pairing](../cache-handling-architecture/part-06-stage-5-draft-context-modes-and-pairing.md)
- Implementation Parts 12-14: draft-context compatibility review, BUG-001 startup fix review, and final documentation reconciliation
- Prior reports: [test-report-20260528-06.md](test-report-20260528-06.md) and [test-report-20260528-07.md](test-report-20260528-07.md)

## Environment

- OS and shell: Windows, PowerShell
- Git commit: `f8c08f42570d9569d43f58404c0b3409b7e2d773`, working tree dirty before this QA pass
- Build directory: `D:\source\llama.cpp-jet\build`
- Server binary: `D:\source\llama.cpp-jet\build\bin\Release\llama-server.exe`
- Artifact directory: [test-report-20260528-08-artifacts/](test-report-20260528-08-artifacts/)

## Model inventory

MTP fixture:

```text
D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf  2834975040 bytes  2026-05-24 13:07:39
D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\mmproj-F32.gguf        1334074944 bytes  2026-05-24 13:07:15
```

Paired non-MTP fixture:

```text
D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-GGUF\Qwen3.5-4B-Q4_1.gguf  2784416928 bytes  2026-05-24 13:05:37
D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-GGUF\mmproj-F32.gguf      1334075072 bytes  2026-05-24 13:05:08
```

The `mmproj-F32.gguf` files were inventoried but not used. H53/H54 are text completion cache rows.

## Clean build evidence

Commands:

```powershell
if (Test-Path build) { Remove-Item -Recurse -Force build }
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release --target llama-server -j 4
```

Result: PASS.

Binary evidence after build:

```text
llama-server.exe          2026-05-28 18:17:29  7661568 bytes
server-context.cpp        2026-05-28 16:45:47   239525 bytes
server-cache-hybrid.cpp   2026-05-28 14:01:35    48021 bytes
server-cache-hybrid.h     2026-05-28 14:03:26    16878 bytes
```

The source scan found the BUG-001 null-check path in `server-context.cpp` and the Stage 5 draft mode labels `mtp-target-model` and `mtp-separate-model` in `server-cache-hybrid.cpp`.

Build notes:

- CMake configured with Visual Studio 18 2026 and MSVC 19.51.36243.0.
- OpenSSL was not found, so HTTPS support was disabled. This does not affect local HTTP cache probes.
- The UI build fell back to provisioned Hugging Face UI assets and checksum verification passed.

## Harness note

The first H53 attempt included the older plan default `--server-slots`. This binary rejected it with `error: invalid argument: --server-slots`, so that attempt is discarded as harness error. The accepted H53/H54 probes below omit that flag.

## H53 target-derived MTP

Command:

```powershell
.\build\bin\Release\llama-server.exe `
  --model "D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf" `
  --host 127.0.0.1 `
  --port 18153 `
  --ctx-size 512 `
  --parallel 1 `
  --cont-batching `
  --kv-unified `
  --cache-ram 100 `
  --temp 0 `
  --seed 42 `
  --metrics `
  --cache-mode hybrid `
  --spec-type draft-mtp `
  --log-verbosity 5
```

Completion request, sent twice:

```json
{"prompt":"QA cache MTP probe. Answer with one word: ready","n_predict":1,"temperature":0}
```

Evidence:

- Startup reached `/health`: `{"status":"ok"}`
- Startup log includes MTP setup evidence: `getting device memory data for MTP context overhead` and `MTP context host overhead = 498 MiB`
- Response 1: HTTP 200, `tokens_evaluated=12`, `timings.cache_n=0`
- Response 2: HTTP 200, `tokens_evaluated=12`, `timings.cache_n=0`
- Metrics after request 2: `llamacpp_cache_hits_total{mode="hybrid"} 1`, `llamacpp_cache_misses_total{mode="hybrid"} 1`, `llamacpp_cache_hot_payload_descriptors{mode="hybrid"} 1`
- Failure counters stayed zero: restore failures, descriptor validation failures, pairing violations, and fallback restores

Artifacts:

- [h53-target-derived-mtp-rerun.summary.txt](test-report-20260528-08-artifacts/h53-target-derived-mtp-rerun.summary.txt)
- [h53-target-derived-mtp-rerun.result.json](test-report-20260528-08-artifacts/h53-target-derived-mtp-rerun.result.json)
- [h53-target-derived-mtp-rerun.err.log](test-report-20260528-08-artifacts/h53-target-derived-mtp-rerun.err.log)

Outcome: FAIL.

Reason: startup is no longer blocked and metrics report one successful hybrid cache restore, but the public acceptance row requires the repeated request to show `timings.cache_n > 0`. Both responses report `timings.cache_n=0`.

## H54 separate-model MTP

Combination selected:

- Target: `D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-GGUF\Qwen3.5-4B-Q4_1.gguf`
- Draft: `D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf`

Reasoning: Architecture Part 6 defines separate-model MTP as `--spec-type draft-mtp` with a separate draft model loaded as an MTP context. The normal Qwen3.5 model is the safer target because it keeps target and draft roles distinct and uses the MTP-capable file only for the draft side. Using the MTP file as both target and draft would test a less representative same-file pair.

Command:

```powershell
.\build\bin\Release\llama-server.exe `
  --model "D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-GGUF\Qwen3.5-4B-Q4_1.gguf" `
  --model-draft "D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf" `
  --host 127.0.0.1 `
  --port 18154 `
  --ctx-size 512 `
  --parallel 1 `
  --cont-batching `
  --kv-unified `
  --cache-ram 100 `
  --temp 0 `
  --seed 42 `
  --metrics `
  --cache-mode hybrid `
  --spec-type draft-mtp `
  --log-verbosity 5
```

Completion request, sent twice:

```json
{"prompt":"QA cache separate MTP probe. Answer with one word: ready","n_predict":1,"temperature":0}
```

Evidence:

- Startup reached `/health`: `{"status":"ok"}`
- Response settings report `speculative.types: "none,draft-mtp"`
- Response 1: HTTP 200, `tokens_evaluated=13`, `timings.cache_n=0`
- Response 2: HTTP 200, `tokens_evaluated=13`, `timings.cache_n=0`
- Metrics after request 2: `llamacpp_cache_hits_total{mode="hybrid"} 1`, `llamacpp_cache_misses_total{mode="hybrid"} 1`, `llamacpp_cache_hot_payload_descriptors{mode="hybrid"} 1`
- Failure counters stayed zero: restore failures, descriptor validation failures, pairing violations, and fallback restores
- A lower-verbosity startup-only rerun also reached `/health`

Artifacts:

- [h54-separate-model-mtp-nonmtp-target-mtp-draft.summary.txt](test-report-20260528-08-artifacts/h54-separate-model-mtp-nonmtp-target-mtp-draft.summary.txt)
- [h54-separate-model-mtp-nonmtp-target-mtp-draft.result.json](test-report-20260528-08-artifacts/h54-separate-model-mtp-nonmtp-target-mtp-draft.result.json)
- [h54-separate-model-mtp-nonmtp-target-mtp-draft.err.log](test-report-20260528-08-artifacts/h54-separate-model-mtp-nonmtp-target-mtp-draft.err.log)
- [h54-separate-model-mtp-startup-lowlog.summary.txt](test-report-20260528-08-artifacts/h54-separate-model-mtp-startup-lowlog.summary.txt)

Outcome: FAIL.

Reason: startup is no longer blocked and metrics report one successful hybrid cache restore, but the public acceptance row requires `timings.cache_n > 0` on the repeated request. Both responses report `timings.cache_n=0`.

## Bug handoff

BUG-002: MTP hybrid restore metrics increment while public response timing reports no cached tokens.

Affected rows: H53 and H54.

Repro:

1. Use either accepted command above.
2. Wait for `/health` to return `{"status":"ok"}`.
3. Send the same `/completion` JSON body twice.
4. Read the second response and `/metrics`.

Observed:

- `/metrics` reports `llamacpp_cache_hits_total{mode="hybrid"} 1`.
- `/metrics` reports zero restore, descriptor validation, pairing, and fallback failures.
- The second HTTP response still reports `timings.cache_n=0`.

Expected:

- The repeated public request should report `timings.cache_n > 0`, or the test plan needs a deliberate acceptance change that treats hybrid hit metrics as the public restore signal for MTP modes.

## Results

| Category | PASS | FAIL | SKIP | BLOCKED |
| --- | ---: | ---: | ---: | ---: |
| Clean build | 1 | 0 | 0 | 0 |
| Public H53/H54 probes | 0 | 2 | 0 | 0 |
| Total | 1 | 2 | 0 | 0 |

Overall status: BUG-HANDOFF.

H53/H54 startup status: unblocked with the Qwen3.5 MTP fixture.

H53/H54 cache acceptance status: not passed. Both rows fail the current public `timings.cache_n > 0` criterion.

Next gate recommendation: send BUG-002 to Developer. Do not reopen BUG-001; the Qwen3.5 MTP fixture starts successfully for both target-derived and separate-model MTP probes.
