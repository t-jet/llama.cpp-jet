# Stage 39 QA post-fix execution report

Date: 2026-07-12
Verdict: FAIL
Plan: `cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260712-03/`

## Session

QA captured the dirty tree and commit before execution. Existing Stage 39 and
Grafana changes were preserved. QA changed no product, test, runner, or dashboard
file.

QA removed `build`, configured Release, and built `llama-server` plus all 11
focused targets required by `run_coverage.ps1`. The first build command timed out
after 124 seconds; the resumed command completed successfully from the same fresh
configure. `configure.log`, `build.log`, and `build-resume.log` preserve both
commands.

## Results

| ID | Verdict | Evidence and reason |
| --- | --- | --- |
| TP-39-01 | PASS | `live-standard/state.json` records cold retention, 71 restored tokens, one promotion, zero payload eviction, and zero pruning. |
| TP-39-02 | BLOCKED | Focused multi-victim evidence passed, but no live equal-rank payload-ID tie-break tuple and victim inventory was produced. |
| TP-39-03 | BLOCKED | Calibrated 10/11 MiB run emitted 17 `retained_cold/cold_room_made` decisions, not `evicted/both_filled`; it did not establish no eligible victim. |
| TP-39-04 | BLOCKED | Calibrated 10/10 MiB run also emitted `retained_cold/cold_room_made`, not `evicted/oversized_both`; selected measured object did not become the pressured candidate. |
| TP-39-05 | PASS | Calibrated cold-disabled rerun passed. Hot-zero emitted neither Stage 39 family. |
| TP-39-06 | PASS | Controller rollback and transaction-fault coverage passed. |
| TP-39-07 | PASS | Named target/draft lifecycle assertions passed in `test-cache-controller`. |
| TP-39-08 | PASS | Same-entry independent descriptor-pressure assertions passed. |
| TP-39-09 | PASS | Protected-root/live-descendant pressure assertions passed. |
| TP-39-10 | PASS | Concurrent production `tx_update` pressure assertions passed. |
| TP-39-11 | PASS | Canonical legacy run passed with both Stage 39 families absent. |
| TP-39-12 | PASS | Standard live run used production save pressure and committed cold data before restore. |
| TP-39-13 | PASS | Focused exact-fit, one-byte-over, overhead, and overflow assertions passed. |
| TP-39-14 | PASS | Focused mutation-position and restart-idempotence matrix passed. |
| TP-39-15 | FAIL | Live metrics contain duplicate `mode="hybrid"` labels in every Stage 39 decision series. This is not a valid fixed-label tuple. |

Counts: PASS 11, FAIL 1, BLOCKED 3, SKIP 0.

## Additional failure and coverage

The required-target build passed, but direct execution of
`test-step10-metrics.exe` failed at `test_cold_payload_bytes_gauge`,
`tests/test-step10-metrics.cpp:176`. All ten other focused executables passed.
Scoped `ctest -C Release -R cache` passed 1/1 because it includes only
`test-cache-controller`.

Fail-closed coverage stopped on its first smoke target. OpenCppCoverage returned
exit 0 but produced no `.cov` for `test-cache-controller`; therefore no changed-
line percentage exists and the required 80% gate is BLOCKED. See
`coverage-console.log` and `coverage/01-test-cache-controller.log`.

## Handoff

Developer results review owns three items: reproduce and classify the Stage 10
cold-byte gauge failure; remove the duplicate Stage 39 `mode` label at its source
and add a live exporter regression; determine whether TP-39-03/04 need runner
workload control or expose a product reason-selection defect. Coverage tooling
must produce a smoke `.cov` before another full run. TP-39-02 still needs its live
equal-rank half.

Every path cited by this report was checked with `Test-Path`; results are in
`artifact-verification.txt`.
