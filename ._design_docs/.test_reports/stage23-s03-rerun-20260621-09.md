# Stage 23 S03 focused rerun 20260621-09

Status: FAIL-product-crash
Date: 2026-06-21
Owner: QA
Scope: focused S03 rerun only after Architect startup-crash fix review PASS.

## Gate

S04..S08 and L01..L03 were not run. The durable report suffix is `09`
because non-durable roots `04` through `08` already existed before this
session and could not be used as fresh run roots.

## Preflight

Run root: `._test_output/stage23-s03-rerun-20260621-09`

Cold root: `D:\tmp\cache-cold-stage23-s03-rerun-20260621-09`

Model: `D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf`

Evidence:

- Branch and dirty state: `preflight/01-branch.txt`,
  `preflight/02-git-status.txt`; branch was `work-branch`.
- CUDA build flag: `preflight/03-cmake-cuda.txt`, `GGML_CUDA:BOOL=ON`.
- CUDA device state: `preflight/04-nvidia-smi-before.txt`; two NVIDIA
  GeForce RTX 5060 Ti GPUs, no running GPU processes before the row.
- Fixture exists: `preflight/05-fixture.txt`; size 2,834,975,040 bytes.
- Ports: `preflight/06-port-listeners-8900-8921.txt` was empty. BasePort 8900
  selected.
- Cold path: `preflight/07-cold-path-before.txt` was empty.
- Disk: `preflight/08-disk.txt`; D: free space above Stage 23 thresholds.

## Clean build

Commands:

```text
cmake --build build-cov --config Release --target clean
cmake --build build-cov --config Release --target test-cache-controller -j 4
cmake --build build-cov --config Release --target llama-server -j 4
.\build-cov\bin\Release\test-cache-controller.exe
```

Evidence:

- `preflight/09-clean.log`
- `preflight/10-build-test-cache-controller.log`
- `preflight/11-build-llama-server.log`
- `preflight/12-test-cache-controller.log`
- `preflight/13-binary-freshness.txt`

Result: PASS. `test-cache-controller` passed 119 tests, including 7 Stage 23
focused tests. Binary freshness records `llama-server.exe`,
`llama-server-impl.dll`, and `test-cache-controller.exe`.

## Dry run

Command shape:

```text
kickoff-stage20-stress-longrun.ps1 -RowsToRun S03 -RunRoot ._test_output\stage23-s03-rerun-20260621-09 -ModelPath <Qwen3.5 MTP gguf> -CacheColdPath D:\tmp\cache-cold-stage23-s03-rerun-20260621-09 -CachePromptEvidenceDir ._test_output\stage23-s03-rerun-20260621-09\prompt-evidence -CacheColdMaxMib 512 -CacheRamMib 512 -CachePromptEvidence redacted -JinjaVariant new -BasePort 8900 -BatchSize 1 -DryRun
```

Evidence:

- `preflight/14-wrapper-dry-run.log`
- `preflight/14-wrapper-dry-run-exit.txt`
- `preflight/15-dry-run-side-log-checks.txt`
- `batch-summary.log.side`

Result: PASS. Side log shows `DryRun OK S03`, fresh run root under
`._test_output`, `--cache-mode hybrid`, `--cache-cold-max-mib 512`,
`--cache-ram 512`, `--n-gpu-layers all`, `--fit off`, redacted prompt evidence
directory, model path, and created cold/evidence directories.

## Live S03

Command shape:

```text
kickoff-stage20-stress-longrun.ps1 -RowsToRun S03 -RunRoot ._test_output\stage23-s03-rerun-20260621-09 -ModelPath <Qwen3.5 MTP gguf> -CacheColdPath D:\tmp\cache-cold-stage23-s03-rerun-20260621-09 -CachePromptEvidenceDir ._test_output\stage23-s03-rerun-20260621-09\prompt-evidence -CacheColdMaxMib 512 -CacheRamMib 512 -CachePromptEvidence redacted -JinjaVariant new -BasePort 8900 -BatchSize 1
```

Evidence:

- Wrapper command and exit: `preflight/17-wrapper-live-command.txt`,
  `preflight/17-wrapper-live-exit.txt`; wrapper exit 1.
- GPU sampling during row: `preflight/18-nvidia-smi-during-live.txt`.
- Row output: `S03-Jnew/`.
- Server log: `S03-Jnew/server.err.log`.
- Metrics before: `S03-Jnew/metrics-before.txt`.
- Prompt evidence: `prompt-evidence/cache-prompt-evidence.jsonl`.
- Resource samples: `S03-Jnew/resource-samples.csv`.

CUDA evidence: PASS. Server log lists CUDA0 and CUDA1 RTX 5060 Ti and listens
on `http://127.0.0.1:8900`. Live `nvidia-smi` samples show
`llama-server.exe` on both GPUs with about 1407 MiB on GPU0 and 1849 MiB on
GPU1.

## Row verdict

| Row | Verdict | Evidence | Notes |
| --- | --- | --- | --- |
| S03 | FAIL-product-crash | `S03-Jnew/launch.log`, `launch.err`, `server.err.log`, `batch-summary.log.side` | Server reached request phase, wrote `metrics-before.txt`, then exited early with code `-1073741819`; `metrics-after.txt` and `cap-exit.json` are missing. |

## Checks

- Prompt evidence mode: PASS. JSONL has 1645 records with redacted fields only:
  `preparation_id`, `namespace_hash`, `profile`, `pair_state`, `token_count`,
  `boundary_count`, `first_user_boundary`, `token_span_checksum`,
  `lookup_outcome`, and `prefix_candidate`.
- Cold budget: FAIL evidence item. Cold files total 107,055,836 bytes, under
  512 MiB, but server log records cache payload above 512 MiB before crash:
  561.766 MiB total, then repeated budget warnings.
- Metrics: FAIL evidence item. `metrics-before.txt` exists; `metrics-after.txt`
  is missing because server exited early.
- S03 behavior: INCOMPLETE. Log has 507 `try_restore - found match` lines, 0
  `unsafe_prefix_rejected` lines, and 1645 `exact_entry_absent` evidence
  records before crash. The row cannot pass because it did not complete.

## Failure evidence

`S03-Jnew/launch.log`:

```text
S12-S03 large branch forests; stub=False
Server exited early with code -1073741819
```

`batch-summary.log.side`:

```text
row_gate S03 exitCode=0 ok=False evidenceFiles=6 present=server.out.log,server.err.log,metrics-before.txt missing=metrics-after.txt
```

Server tail ends after demotion pressure, not clean shutdown:

```text
demote_payload: outstanding demotions exceed payload budget for payload_id 3289
demotion failed for payload_id 3289, falling back to immediate eviction
```

## Classification

Product bug. Setup gates passed, CUDA was active, the fixture existed, port 8900
was free, the cold path existed, dry-run flags were correct, and the server
reached request traffic. The process then crashed before final metrics.

Next owner: Developer. Investigate the S03 large-branch-forest crash under CUDA
with 512 MiB RAM/cold budget, focusing on the demotion pressure path and the
payload-budget overshoot warnings before the access violation.

## Handoff

Stage 23 remains stopped at S03. Manager may not resume S04..S08 or L01..L03
until Developer fixes or reclassifies this S03 product crash and Architect
reviews the fix or reclassification.

Repository note: `git check-ignore -v` reports
`._design_docs/.test_reports/.gitignore:4:*` for this new report. The file is
present on disk, but normal `git status` hides it until the pre-existing ignore
ordering is fixed or the file is explicitly added by a human-approved command.
