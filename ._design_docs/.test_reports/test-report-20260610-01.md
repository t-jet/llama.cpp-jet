# Test report 20260610-01: Stage 13 blocked-row endpoint rerun

- Date: 2026-06-10 local
- Owner: QA execution
- Scope: Stage 13 blocked rows only: E13-01c, E13-07, E13-08, E13-10, E13-13, E13-14, E13-16
- Verdict: BLOCKED
- Evidence root: `._design_docs/.test_reports/test-report-20260610-01-artifacts/`
- CUDA-only rule: user clarified all testing must use CUDA. Earlier `build\` CPU artifacts in this artifact root are discarded setup evidence and are not used for row verdicts.

## Build and environment

CUDA environment evidence:

- `cuda-environment.txt`: `cuda_path_prefix=D:\app\cuda_13_2\bin\x64`
- `cuda-environment.txt`: two `NVIDIA GeForce RTX 5060 Ti` devices, each reporting `16311 MiB` total and `16050 MiB` free at capture time.
- `git-status-before-cuda-build.txt`: dirty tree captured before CUDA build. Pre-existing Stage 13 source/doc changes were left intact.

CUDA clean build command:

```text
cmake --build build-cuda --config Release --target clean
```

CUDA target build command:

```text
cmake --build build-cuda --config Release --target llama-server test-cache-controller
```

CUDA clean completed:

- `cuda-clean-release-build.log`: CMake reconfigured `build-cuda`, found CUDA toolkit, included CUDA backend, warned NCCL missing, then `clean_exit=0`.

CUDA target build did not complete:

- Initial command timed out after 1204.143 seconds while building CUDA objects.
- User interrupted the follow-up wait and then requested no more build/testing attempts.
- `cuda-build-release-targets.log` tail shows active `nvcc.exe` compile of `ggml-cuda\mmq.cu` under `build-cuda`.
- `processes-stopped-at-user-request.txt` captured active processes before stop: `cmake.exe` PID 24564, `MSBuild.exe` PID 21268, `nvcc.exe` PID 28112, plus unrelated service processes matched by broad process name search.
- Same file records stopped build processes: PID 24564 `cmake`, PID 21268 `MSBuild`, PID 28112 `nvcc`.
- `build-cuda\bin\Release\llama-server.exe` and `build-cuda\bin\Release\test-cache-controller.exe` were absent after the stopped build.

## Discarded CPU setup evidence

Before the CUDA-only clarification, QA ran a CPU clean build under `build\`, executed `test-cache-controller.exe`, and started an Omni server. These artifacts remain for traceability but are invalid for Stage 13 verdicts:

- `build-commands.txt`, `build-release-targets.log`, `build-binary-timestamps.txt`
- `E13-13-test-cache-controller.log`
- `omni-server-command.txt`, `omni-server.err.log`, `E13-01c-*`, `E13-07-*`, `E13-08-*`

Because the user clarified "Always use CUDA for testing, never CPU" and requested all tests be reviewed as CUDA-only, none of that evidence is counted.

## Row results

| Row | Verdict | CUDA evidence |
| --- | --- | --- |
| E13-01c | BLOCKED | No CUDA `llama-server.exe` was produced. Multimodal Omni endpoint probes did not run on CUDA. CPU Omni artifacts are discarded. |
| E13-07 | BLOCKED | No CUDA `llama-server.exe` was produced. `/v1/audio/transcriptions` did not run on CUDA. CPU curl artifacts are discarded. |
| E13-08 | BLOCKED | No CUDA `llama-server.exe` was produced. `/audio/transcriptions` alias did not run on CUDA. CPU curl artifacts are discarded. |
| E13-10 | BLOCKED | No CUDA `llama-server.exe` was produced. `/slots` save/restore/erase sequence did not run on CUDA. |
| E13-13 | BLOCKED | No CUDA `test-cache-controller.exe` was produced. Focused route-neutral helper did not run on CUDA. CPU helper PASS is discarded. |
| E13-14 | BLOCKED | No CUDA `llama-server.exe` was produced. Degraded fallback endpoint probe and bounded leak scan did not run on CUDA. |
| E13-16 | BLOCKED | CUDA clean completed, but CUDA target build was stopped while `nvcc` was compiling. Fresh CUDA binaries were absent, so endpoint rows are invalid. |

## Summary

- PASS: 0
- FAIL: 0
- BLOCKED: 7
- Product bugs: 0 confirmed
- Valid CUDA endpoint/helper evidence: none

Stage 13 endpoint execution remains blocked. Next owner is QA or build owner, depending on gate direction. QA needs a completed clean CUDA Release build that produces `build-cuda\bin\Release\llama-server.exe` and `build-cuda\bin\Release\test-cache-controller.exe` before rerunning the blocked rows. If the CUDA target build remains slow or hung, build owner should provide a stable CUDA build path or a prebuilt fresh CUDA artifact with timestamps before QA resumes endpoint probes.
