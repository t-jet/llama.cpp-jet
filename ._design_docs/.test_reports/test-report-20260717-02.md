# Stage 39 focused QA rerun

Date: 2026-07-17
Gate: D39-QA-02
Verdict: BLOCKED
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260717-02/`

## Scope

Manager Part 149 authorized one fresh canonical TP-39-03 node followed, only
after that node passed, by four coverage blocks. The canonical node stopped in
the guarded proof preflight. Per the fail-fast rule, no coverage block ran.
No product, driver, fixture, seam, test plan, threshold, or contract changed.

## Environment and build

- HEAD was `2c501427cdc318b50d50fcfe329e71f90a9a1e89`. The pre-run dirty tree is in
  `git-status-before.txt` and `git-diff-stat-before.txt`.
- Host was Windows x64. CMake was 4.3.2, PowerShell was 7.6.3, Windows
  PowerShell was 5.1.26100.8875, and OpenCppCoverage was the 77,824-byte
  executable at `D:/app/OpenCppCoverage/OpenCppCoverage.exe`.
- Configuration was Release, CPU (`GGML_CUDA=OFF`), with
  `LLAMA_STAGE39_LIVE_TEST_SEAM=ON` and tests enabled.
- The Qwen3.5-4B MTP model was 2,834,975,040 bytes with SHA-256
  `3874209241C9A397E2F62CD3F70F80FD2DFBF0DFCCB6838416BDB48A714E8630`.
  Its checked-in template SHA-256 was
  `47A7BDCBAE1F459E0A6F793D22E428A17F355C5A3100B77A14A601737A82F7F5`.
  `fixtures.json` records this and the coverage fixture identity.

QA removed `build` and `build-stage39-seam-on`. The first build wrapper hit a
tool timeout and left its own MSBuild tree stalled. QA recorded the exact PIDs,
stopped only those processes, removed the partial tree, and repeated configure
and build from zero. Final configure exited 0. Final build exited 0 after
231.083 seconds for `llama-server` and all 11 targets named by
`run_coverage.ps1`. At inventory capture, all required executables were 0.29 to
0.90 minutes old. The launcher and `llama-server-impl.dll` identities are saved
separately.

Evidence: `build-removal-audit.json`, `build-timeout-processes.json`,
`build-timeout-cleanup-final.json`, `configure-final-result.json`,
`configure-final.log`, `build-final-result.json`, `build-final.log`,
`binary-inventory.json`, `server-impl-inventory.json`, and
`cmake-cache-flags.txt`.

## Cheap gates

| Check | Result |
| --- | --- |
| PowerShell 7 parser API | PASS, 0 errors |
| Windows PowerShell 5 parser API | PASS, 0 errors |
| PowerShell 7 metric-validation self-test | PASS, exit 0 |
| Windows PowerShell 5 metric-validation self-test | PASS, exit 0 |

Exact commands and outputs are in `cheap-tests.json`, `parser-pwsh7.log`,
`parser-powershell5.log`, `selftest-pwsh7.log`, and
`selftest-powershell5.log`.

## TP-39-03

QA ran exactly one canonical node with the approved model, fresh
`TP-39-03/` root, port 8193, 8192-token context, two chats, and the driver's
20-minute, 16-GiB RSS, 4-GiB cold-root, and six-chat guards. The exact command
is in `tp39-03-command.txt`.

The node exited 1 after 352.723 seconds:

```text
SKIP-preflight-tp39-03-proof-component
```

The failure came from line 129 of `stage39-two-layer-pressure.ps1`, before
`Assert-Tp3903`, apply, lowered-budget calculation, or terminal proof. The
saved evidence proves:

- exact prompt counts 3631 and 3632, total 7263, context margin 929;
- one eligible hot exact candidate, empty cold set, and no cold bytes;
- discovery reports `pair_state="target_only"`;
- server logs contain six created checkpoints but both `tx_save` rows report
  `dft: 0.000`;
- peak sampled RSS was 5,254,770,688 bytes and cold-root bytes stayed zero;
- no proof summary, canonical budget, apply request, final metrics, or terminal
  state artifact exists.

The driver does not preserve the linked or explicit proof response when this
preflight throws. Therefore the artifacts identify the failed assertion group,
but not which proof-row conjunct failed. The target-only discovery and zero
draft save rows are consistent with the driver's required positive draft
component check; this is an inference, not a complete proof-row diagnosis.

| ID | Verdict | Reason |
| --- | --- | --- |
| TP-39-03 | BLOCKED | Approved live fixture did not reach the proof-component precondition; `Assert-Tp3903` never ran. No product invariant was tested or violated. |

Evidence: `tp39-03-result.json`, `tp39-03-run.log`,
`tp39-03-diagnosis.json`, `tp39-03-postfailure-artifact-presence.json`, and
the saved files under `TP-39-03/`.

## Coverage

| Block | Classification |
| --- | --- |
| PowerShell 7 success | NOT RUN, required fail-fast stop |
| PowerShell 7 forced merge failure | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 success | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 forced merge failure | NOT RUN, required fail-fast stop |

Coverage verdict: BLOCKED. No fresh coverage root, capture `.cov`, merged XML,
Markdown report, delegated exit-23 message, or percentage exists. The 80
percent gate is unmet. `coverage-stop.json` records all four absent roots.

## Cleanup and handoff

Cleanup found zero `llama-server.exe` processes and zero listeners on port
8193. `cleanup.json` records the check. Artifact hashes are in
`artifact-verification.csv`.

Final classification: TP-39-03 `BLOCKED`; coverage `BLOCKED`; session
`BLOCKED`. Developer test-results review should determine why the approved MTP
run exposes target-only/zero-draft proof inputs and should preserve raw linked
and explicit proof responses on any future preflight failure. Fresh Manager
authorization is required before another model run or any coverage block.
