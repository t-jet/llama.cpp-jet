# Test report test-report-20260527-03

Date: 2026-05-27 18:01:41  
Tester: QA agent  
Gate: Phase 4 Stage 4 focused H31-H37 evidence rerun after test-plan correction  
Plan: [cache-handling-test-plan.md](../cache-handling-test-plan.md)

## Scope

This run covers the corrected focused Stage 4 evidence path. H31 and H32 use model-backed public HTTP evidence from `run_stage4_h30_h37.ps1`. H33, H35, H36, and H37 are checked against focused C++ controller coverage where public HTTP cannot create the required preconditions.

The run started from a clean build. Product code was not modified.

## Environment

- Repository: D:\source\llama.cpp-jet
- Git commit: 3fbf8ac6dd5199ac813673a14ad478e8d517956a (working tree dirty)
- Generator: Visual Studio 18 2026
- Compiler: MSVC 19.51.36243.0
- Build type: Release
- Server binary: D:\source\llama.cpp-jet\build\bin\Release\llama-server.exe
- Server binary timestamp: 2026-05-27T18:01:24.5149803+03:00
- Server binary size: 7644160 bytes
- Binary age at focused HTTP run start: 0.137 minutes
- Controller test binary: D:\source\llama.cpp-jet\build\bin\Release\test-cache-controller.exe
- Controller test binary timestamp: 2026-05-27T18:01:27.7679718+03:00
- Model: D:\source\llama.cpp-jet\._test_models\Qwen2.5-VL-3B-Instruct-GGUF\Qwen2.5-VL-3B-Instruct-Q6_K.gguf (2538157056 bytes)
- Log directory: ._test_output\cache-handling\20260527-180132
- Clean build status: PASS

## Commands

Clean build:

```powershell
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release --target llama-server -j 4
cmake --build build --config Release --target test-cache-controller -j 4
$Binary = Get-Item build\bin\Release\llama-server.exe
$BuildAge = (Get-Date) - $Binary.LastWriteTime
if ($BuildAge.TotalMinutes -gt 10) { throw "llama-server.exe is stale. Run the clean build again." }
```

Outcome: PASS. `llama-server.exe` and `test-cache-controller.exe` were built from a clean `build` directory.

Focused HTTP rerun:

```powershell
& ._design_docs\cache-handling-test-scripts\run_stage4_h30_h37.ps1
```

Outcome: PASS=2, FAIL=0, SKIP=0, BLOCKED=6. The script exited 0 because there were no FAIL rows.

Focused C++ controller tests:

```powershell
ctest --test-dir build -C Release -R test-cache-controller --output-on-failure
build\bin\Release\test-cache-controller.exe
```

Outcome: PASS. `ctest` reported `1/1 Test #28: test-cache-controller Passed`. Direct binary output reported all 36 controller tests passed, including:

- `hybrid protected eviction paths`
- `hybrid payload budget eviction`
- `hybrid refresh enforces payload budget`
- `hybrid multiple protected evictions count decisions`
- `LRU policy planning`

Python cache-mode tests:

```powershell
$env:LLAMA_SERVER_TEST_SKIP_MODEL_PRELOAD='1'
$env:LLAMA_CACHE_TEST_MODEL=(Resolve-Path '._test_models\Qwen2.5-VL-3B-Instruct-GGUF\Qwen2.5-VL-3B-Instruct-Q6_K.gguf').Path
Remove-Item Env:\LLAMA_SERVER_BIN_PATH -ErrorAction SilentlyContinue
python -m pytest tools/server/tests/unit/test_cache_modes.py -q
```

Outcome: PASS with existing expected xfail: `2 passed, 1 xfailed in 3.48s`. Pytest emitted existing dependency and asyncio fixture-scope warnings.

## Results

| ID | Status | Result |
| --- | --- | --- |
| H30 | PASS | Public HTTP evidence from the focused script passed. Payload eviction increased and resident bytes stayed within the 1 MiB budget. |
| H31 | BLOCKED | Public HTTP showed a successful A refresh, but A did not survive the pressure step. The evidence does not isolate whether this is an eviction-order defect or an insufficient public harness signal. |
| H32 | BLOCKED | Public HTTP did not keep A and B resident long enough to prove the successful-restore precondition. |
| H33 | BLOCKED | No focused C++ controller test in this build simulates a restore failure after lookup and then proves the failed entry keeps its old eviction priority. |
| H34 | PASS | Public HTTP evidence from the focused script passed. Equivalent refresh did not duplicate the entry and later budget enforcement stayed within 1 MiB. |
| H35 | PASS | Focused C++ controller coverage proves mixed protected and unprotected eviction priority with protected decision stats. |
| H36 | PASS | Focused C++ controller coverage proves protected-only fallback eviction and protected decision accounting. |
| H37 | BLOCKED | No focused C++ controller test in this build admits an oversized trusted protected entry and proves protected admission rejection. |

## Evidence

### H30: resident payload byte budget

Status: PASS

- Source: `run_stage4_h30_h37.ps1`
- Budget bytes: 1048576
- Measured after one entry: entries=1, resident_payload_bytes=443564
- After pressure: resident_payload_bytes=664892, payload_evictions_delta=8
- Log files: ._test_output\cache-handling\20260527-180132\H30.log, ._test_output\cache-handling\20260527-180132\H30.err.log

### H31: deterministic LRU ordering

Status: BLOCKED

- Source: `run_stage4_h30_h37.ps1`
- Operation order: save A, save B, restore A, save C, verify A, probe B
- Setup entries=2, setup bytes=1034679
- A refresh cache_n=13
- A verify cache_n=0
- Hit delta=1, B probe miss delta=1, payload_evictions_delta=3
- Final entries=2, bytes=1034679

Limitation: the corrected public run proves that A was restored before pressure, but it does not prove the expected final ordering because A missed after C was added. The public evidence lacks entry-id or ordered cache-state visibility, so this remains a QA harness blocker rather than a confirmed product bug in this session.

### H32: successful restore refreshes recency

Status: BLOCKED

- Source: `run_stage4_h30_h37.ps1`
- Operation order: save A/B, successful restore A, save C, verify A, probe B
- Setup entries=1, setup bytes=628003
- Restore A cache_n=0
- Verify A cache_n=0
- B probe miss delta=1, payload_evictions_delta=5

Limitation: the setup failed before the required recency assertion. A and B were not both resident, and A did not restore successfully.

### H33: failed restore does not refresh recency

Status: BLOCKED

- Source checked: `tests/test-cache-controller.cpp`
- Available focused tests cover partial-match rejection, payload-budget eviction, refresh-budget enforcement, protected eviction paths, and lookup edge paths.
- `rg` found restore-failure counter paths in product code, but no focused controller test that corrupts or replaces an admitted serialized payload after lookup.
- Required signal remains `n_restore_failures` plus next-eviction ordering for the failed candidate.

Limitation: public HTTP cannot create this failure mode, and the available C++ controller suite does not yet provide the requested fault-injection evidence.

### H34: equivalent-entry refresh enforces budget

Status: PASS

- Source: `run_stage4_h30_h37.ps1`
- Entries after first save=1, after equivalent refresh=1
- Refresh cache_n=12
- After pressure: resident_payload_bytes=813351, payload_evictions_delta=5
- Source line check: `tests/test-cache-controller.cpp:566` also covers refresh budget enforcement through `test_hybrid_refresh_enforces_payload_budget()`.

### H35: protected-root priority

Status: PASS

- Source: `ctest --test-dir build -C Release -R test-cache-controller --output-on-failure`
- Source line check: `tests/test-cache-controller.cpp:521` covers `test_hybrid_payload_budget_eviction()`.
- The test creates a protected entry with 700 KiB payload and an unprotected entry with 500 KiB payload under a 1 MiB budget.
- Assertions prove only the protected entry remains, the unprotected entry no longer matches, `protected_payload_bytes == 700 * 1024`, `unprotected_payload_bytes == 0`, and `n_protected_root_decisions >= 1`.
- Direct controller binary output included `hybrid payload budget eviction... PASSED`.

Limitation: this is code-level controller evidence, not public HTTP evidence. That is the accepted path in the corrected test plan because public chat metadata is degraded and cannot create trusted protected entries.

### H36: protected-root fallback eviction

Status: PASS

- Source: `ctest --test-dir build -C Release -R test-cache-controller --output-on-failure`
- Source line check: `tests/test-cache-controller.cpp:589` covers `test_hybrid_multiple_protected_evictions_count_decisions()`.
- The test creates three protected entries of 900 KiB each, lowers the hot payload budget to 1 MiB, and refreshes the newest protected entry.
- Assertions prove one protected entry remains, two older protected entries no longer match, `resident_payload_bytes == 900 * 1024`, `n_protected_root_evictions == 2`, and `n_protected_root_decisions >= 3`.
- Source line check: `tests/test-cache-controller.cpp:615` covers `test_lru_policy_planning()`, including protected fallback order after unprotected candidates.
- Direct controller binary output included `hybrid multiple protected evictions count decisions... PASSED` and `LRU policy planning... PASSED`.

Limitation: this is code-level controller evidence, not public HTTP evidence. That is the accepted path in the corrected test plan.

### H37: protected admission rejection

Status: BLOCKED

- Source checked: `tests/test-cache-controller.cpp`
- Product code exposes `n_protected_root_admission_rejections`, but the focused controller suite does not contain a test that admits an oversized trusted protected payload and asserts rejection before entry creation.
- Public HTTP remains unusable for this row because parser or HTTP validation can reject before `save_slot()`, and public chat does not create trusted non-degraded protected metadata.

Limitation: no current public or focused controller run produced the required admission-path evidence.

## Counts

| Status | Count |
| --- | ---: |
| PASS | 4 |
| FAIL | 0 |
| SKIP | 0 |
| BLOCKED | 4 |

Counted rows: H30-H37 from the focused Stage 4 suite.

## Handoff

No product bug was proven by this session. H35 and H36 can move forward on focused C++ controller evidence. H31 and H32 still need a stats-capable or entry-state-aware harness to make recency ordering conclusive. H33 needs restore-failure fault injection or a focused controller test. H37 needs a focused admission-rejection test or stats-capable harness that can create an oversized trusted protected entry without HTTP/parser rejection.
