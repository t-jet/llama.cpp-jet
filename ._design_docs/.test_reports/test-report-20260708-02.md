# Stage 35 focused TP-35-COV-01 QA rerun

Test run id: `test-report-20260708-02`
Date: 2026-07-08
Tester: QA
Plan: [Part 40](../cache-handling-test-plan/part-40-stage35-upstream-merge-regression.md)
Prior report: [test-report-20260708-01.md](test-report-20260708-01.md)
Artifact root: `_test_output/stage35-upstream-merge-20260708-02/`
Build dir: `build-stage35-qa`
Verdict: FAIL

## Scope and source state

This session reran only TP-35-COV-01 after F35-QA-FIX-01 Architect fix
re-review PASS. The previous Stage 35 report remains the evidence for the
runtime, route, metrics, router, stream, and Stage 34 synthetic rows.

Source-ref proof before and after the rerun:

- `MERGE_HEAD`: `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`
- `origin/upstream_master`: `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`
- remote `refs/heads/upstream_master`: `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`
- unresolved conflict paths: none

Evidence:

- `_test_output/stage35-upstream-merge-20260708-02/source/`
- `_test_output/stage35-upstream-merge-20260708-02/source-final/`

No commit, push, PR, reviewer response, or merge abort was performed.

## Build evidence

Clean configure and focused target build were run in `build-stage35-qa`.
The first build wrapper timed out after the target logs showed outputs, so it
was not used as final build evidence. A same-target incremental build exited
0, then the build was reconfigured with the Stage 18 coverage flags so
OpenCppCoverage could use PDB line data.

Coverage build flags:

```text
CMAKE_CXX_FLAGS_RELEASE:STRING=/O2 /Ob2 /DNDEBUG /Zi
CMAKE_C_FLAGS_RELEASE:STRING=/O2 /Ob2 /DNDEBUG /Zi
CMAKE_EXE_LINKER_FLAGS_RELEASE:STRING=/INCREMENTAL:NO /debug /DEBUG:FULL
CMAKE_MODULE_LINKER_FLAGS_RELEASE:STRING=/INCREMENTAL:NO /debug /DEBUG:FULL
CMAKE_SHARED_LINKER_FLAGS_RELEASE:STRING=/INCREMENTAL:NO /debug /DEBUG:FULL
```

Final focused build command:

```powershell
cmake --build build-stage35-qa --config Release --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-step12-branch-graph test-step13-stage8 -j 8
```

Result: PASS.

Build summary:

- configure exit: 0
- coverage-flag configure exit: 0
- coverage-flag build exit: 0
- stale-binary check: PASS, stale count 0
- newest source: `tests/test-step10-metrics.cpp`
- PDBs present for the focused executables and server artifacts

Evidence:

- `_test_output/stage35-upstream-merge-20260708-02/build/cmake-configure.stdout.log`
- `_test_output/stage35-upstream-merge-20260708-02/build/cmake-build-incremental.stdout.log`
- `_test_output/stage35-upstream-merge-20260708-02/build/cmake-configure-coverage-flags-fixed.stdout.log`
- `_test_output/stage35-upstream-merge-20260708-02/build/cmake-build-coverage-flags-fixed.stdout.log`
- `_test_output/stage35-upstream-merge-20260708-02/build/build-summary.json`

## Row result

| Row | Result | Evidence |
| --- | --- | --- |
| TP-35-COV-01 | FAIL | Corrected focused targets built and ran, but coverage is below both required floors: combined 0.734 against 0.80, product-only 0.5856 against 0.70. |

Counts:

- PASS: 0
- FAIL: 1
- BLOCKED: 0
- SKIP: 0

## Coverage evidence

The plan wrapper command was attempted first:

```powershell
pwsh -NoProfile -File ._design_docs\cache-handling-test-scripts\run_coverage.ps1 `
  -BuildDir build-stage35-qa `
  -OutDir _test_output\stage35-upstream-merge-20260708-02\coverage-final `
  -Port 8153
```

Result: FAIL, exit 1. The corrected test processes exited 0, but the wrapper
emitted no `.cov` files and stopped at `No .cov files produced; cannot generate
coverage report.`

The same corrected target set was then run with direct OpenCppCoverage
invocations to separate coverage measurement from the wrapper invocation path.
That produced `.cov`, Cobertura XML, HTML, and markdown evidence.

Coverage artifacts:

- `_test_output/stage35-upstream-merge-20260708-02/coverage-manual/coverage-report.md`
- `_test_output/stage35-upstream-merge-20260708-02/coverage-manual/coverage-summary.json`
- `_test_output/stage35-upstream-merge-20260708-02/coverage-manual/coverage-merged.xml`
- `_test_output/stage35-upstream-merge-20260708-02/coverage-manual/html/index.html`
- `_test_output/stage35-upstream-merge-20260708-02/coverage-manual/cov-binary/`

Coverage summary:

```text
Combined line rate: 0.734
Combined covered: 5879 / 8010
80% threshold: FAIL

Product-only line rate: 0.5856
Product-only covered: 2350 / 4013
70% threshold: FAIL
```

Per-file rows are in
`_test_output/stage35-upstream-merge-20260708-02/coverage-manual/coverage-report.md`.

Cov files produced:

| File | Size |
| --- | --- |
| `01-test-cache-controller.cov` | 47620 |
| `02-test-step10-metrics.cov` | 30017 |
| `03-test-stage10-cold-store-hardening.cov` | 31197 |
| `04-test-step12-branch-graph.cov` | 5165 |
| `05-test-step13-stage8.cov` | 31933 |
| `06-server-http-probe.cov` | 109 |

The server probe reached `/health`, completed the cache prompt traffic, and
produced a 109-byte `.cov`. It did not change the measured denominator rows.

## Failure

### F35-QA-02: corrected coverage target set is measurable but below threshold

Required row: TP-35-COV-01.

The original compile drift is fixed. `test-step10-metrics`,
`test-stage10-cold-store-hardening`, `test-cache-controller`,
`test-step12-branch-graph`, and `test-step13-stage8` all ran under
OpenCppCoverage and exited 0.

The row still fails because the markdown coverage report is below the required
T114/T114a floors:

- combined: 0.734, below 0.80
- product-only: 0.5856, below 0.70

This is not a missing coverage-tool blocker. OpenCppCoverage exists and direct
invocation produced usable `.cov`, Cobertura, HTML, and markdown artifacts.

Secondary harness observation: `run_coverage.ps1` produced no `.cov` files
through its `Start-Process -ArgumentList` path on this host, even after PDBs
were present. Direct OpenCppCoverage invocation worked. That wrapper behavior
should be handled separately, but it does not hide the coverage-threshold
failure.

## Closure state

The focused rerun plus prior report does not close Stage 35 QA execution.
Prior report `test-report-20260708-01.md` keeps the already passed runtime,
route, metrics, router, stream, and Stage 34 synthetic evidence. This rerun
keeps TP-35-COV-01 at FAIL.

Next owner: Developer or Manager.

Next gate: Developer test-results review for F35-QA-02, or Manager decision to
change the coverage contract before another QA rerun.
