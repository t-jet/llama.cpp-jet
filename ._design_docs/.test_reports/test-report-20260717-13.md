# Stage 39 D39-QA-13 execution report

Date: 2026-07-17
Verdict: FAIL
Authority: `cache-handling-phase39-implementation/part-199-manager-d39-qa13-rerun-gate-20260717.md`
Prior order baseline: `cache-handling-phase39-implementation/part-195-manager-d39-qa12-rerun-gate-20260717.md`
Test run ID: `test-report-20260717-13`
Evidence root: `._test_output/test-report-20260717-13/`

## Scope and execution status

Authorized order:

1. Fresh clean Release seam-ON full target build.
2. PowerShell 7 parser and `-MetricValidationSelfTest`.
3. Windows PowerShell 5 parser and `-MetricValidationSelfTest`.
4. One canonical TP-39-03 model node.
5. Coverage only if canonical TP-39-03 reaches full `Assert-Tp3903` PASS.

QA reached coverage because canonical TP-39-03 passed. The first required
coverage success block failed to produce a valid coverage denominator, so QA
stopped there. The forced-failure and Windows PowerShell 5 coverage blocks were
not run.

No product, fixture, workload, budget, threshold, seam behavior, coverage
policy, commit, push, PR, or reviewer-response change was made. QA added this
report and `._test_output/test-report-20260717-13/` evidence only.

## Plan or automation changes made

None. `document-index.md` was not changed because this was execution-only and
no durable plan, script, or README contract changed.

## Commands and results

| Step | Command | Result | Evidence |
| --- | --- | --- | --- |
| Setup | `git rev-parse HEAD`; `git status --short`; tool versions; model hash | PASS | `setup/` |
| Build root precheck | `Test-Path build-stage39-seam-on-qa13` | PASS, absent | `setup/build-root-precheck.txt` |
| Configure | `cmake -S . -B build-stage39-seam-on-qa13 -DLLAMA_STAGE39_LIVE_TEST_SEAM=ON -DLLAMA_BUILD_TESTS=ON -DCMAKE_BUILD_TYPE=Release` | PASS | `setup/cmake-configure.log`, `setup/cmake-configure.exit` |
| Full target build | `cmake --build build-stage39-seam-on-qa13 --config Release --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-step6-demotion-protocol test-step7-promotion-protocol test-step11-test-hooks-fault-injection test-step12-branch-graph test-step13-stage8 -j 4` | PASS | `setup/cmake-build-full-target-set.log`, `setup/cmake-build-full-target-set.exit` |
| Auxiliary target build | `cmake --build build-stage39-seam-on-qa13 --config Release --target test-step1-state-machine test-step2-cold-store test-step3-4-cold-store-write-read test-step9-startup-validation test-stage10-policy-lru -j 4` | PASS | `setup/cmake-build-auxiliary-targets.log`, `setup/full-target-binaries.json` |
| PowerShell 7 parser | `pwsh.exe -NoProfile -Command <literal parserCommand>` | PASS | `parser-pure/pwsh7-parser-rerun.log` |
| PowerShell 7 pure | `pwsh.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -MetricValidationSelfTest` | PASS | `parser-pure/pwsh7-metric-selftest.log` |
| Windows PowerShell 5 parser | `powershell.exe -NoProfile -Command <literal parserCommand>` | PASS | `parser-pure/powershell5-parser.log` |
| Windows PowerShell 5 pure | `powershell.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -MetricValidationSelfTest` | PASS | `parser-pure/powershell5-metric-selftest.log` |
| Canonical TP-39-03 | `pwsh.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -LlamaServerPath build-stage39-seam-on-qa13/bin/Release/llama-server.exe -Scenario both-filled -Requests 2 -ContextSize 8192 -HotBudgetMiB 2048 -ColdBudgetMiB 2048 -Port 8296 -RunRoot ._test_output/test-report-20260717-13/TP-39-03-node` | PASS | `TP-39-03/TP-39-03-node.exit.json`, `TP-39-03-node/summary.json` |
| Coverage block 1, PowerShell 7 success | `pwsh.exe -NoProfile -File .../run_coverage.ps1 -BuildDir D:\source\llama.cpp-jet\build-stage39-seam-on-qa13 -SourceRoot D:\source\llama.cpp-jet -OutDir D:\source\llama.cpp-jet\._test_output\test-report-20260717-13\coverage\pwsh7-success-rerun -ModelPath D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf -Port 8297` | FAIL | `coverage/pwsh7-success-rerun/`, `coverage/pwsh7-success-rerun.log` |
| Coverage block 2, PowerShell 7 forced merge failure | Part 149 forced block | BLOCKED | `coverage/coverage-stop.json` |
| Coverage block 3, Windows PowerShell 5 success | Part 149 success block | BLOCKED | `coverage/coverage-stop.json` |
| Coverage block 4, Windows PowerShell 5 forced merge failure | Part 149 forced block | BLOCKED | `coverage/coverage-stop.json` |

## Evidence and failures

PowerShell 7 and Windows PowerShell 5 self-tests both reported:

```text
Outcome               : PASS
MalformedRowsRejected : True
Tp3903Roles           : {source, incoming}
Tp3903Lengths         : {721, 723}
```

Canonical TP-39-03 exited `0` and wrote:

```json
{"Outcome":"PASS","Scenario":"both-filled","Requests":2,"DecisionSeries":2,"TransactionSeries":1,"ColdFiles":2}
```

TP-39-03 state evidence includes exact same-owner pressure with:

```text
HotBudgetBytes=187834252
ColdBudgetBytes=187834316
ColdDescriptorBytes=187834316
ColdPayloadBytes=187834316
PayloadEvictions delta=1
Pruning delta=0
```

Coverage block 1 first had a QA invocation defect: `-SourceRoot .` generated
relative `.\tools\...` source paths, and OpenCppCoverage rejected them before
target execution. QA preserved that under `coverage/pwsh7-success/` and reran
the same block in a fresh root with absolute paths.

The absolute-path coverage rerun executed all 11 focused tests and the server
probe under OpenCppCoverage, producing 12 `.cov` files. Merge exited `0`, but
the merged Cobertura XML is empty:

```xml
<coverage line-rate="1" branch-rate="0" ... lines-covered="0" lines-valid="0">
  <sources/>
  <packages/>
</coverage>
```

The wrapper correctly rejected the result as below threshold:

```text
Combined line rate: 0  (0 / 0 lines)
80% threshold: FAIL
```

Per the QA memory rule for OpenCppCoverage empty denominators, the root issue is
not an accepted `line-rate="1"` pass. It is a required coverage block failure:
real `.cov` files and merged XML exist, but `lines-valid=0`, so no valid 80%
coverage evidence exists.

Cleanup passed. `cleanup.json` records no `llama-server.exe` process and ports
`8296` and `8297` closed.

## Final outcome

- Setup and clean seam-ON Release full target build: PASS.
- PowerShell 7 parser and pure tests: PASS.
- Windows PowerShell 5 parser and pure tests: PASS.
- Canonical TP-39-03: PASS.
- Coverage block 1: FAIL, empty merged denominator (`lines-valid=0`).
- Coverage blocks 2 through 4: BLOCKED by required stop after block 1 failure.

Final counts: PASS 9, FAIL 1, BLOCKED 3, SKIP 0.

Status: bug-handoff.

Next owner: Developer for D39-QA-13 results review. Coverage remains unresolved
until the success block produces non-empty merged coverage and reaches the 80%
threshold, then the forced-failure blocks can run.
