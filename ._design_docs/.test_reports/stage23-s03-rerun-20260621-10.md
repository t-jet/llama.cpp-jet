# Stage 23 S03 focused rerun 20260621-10

Status: PASS
Date: 2026-06-21
Owner: QA
Scope: focused CUDA S03 rerun only after Architect PASS for
F-23-S03-RERUN09-01. S04..S08 and L01..L03 were not run.

## Gate

Run id: `stage23-s03-rerun-20260621-10`

Suffix `10` was selected because report/output/cold roots for `09` already
existed and `10` was unused across durable report, `._test_output`, and
`D:\tmp`.

Run root: `._test_output/stage23-s03-rerun-20260621-10`

Cold root: `D:\tmp\cache-cold-stage23-s03-rerun-20260621-10`

Model:
`D:\source\llama.cpp-jet\._test_models\Qwen3.5-4B-MTP-GGUF\Qwen3.5-4B-Q4_K_M.gguf`

## Preflight

Evidence root: `._test_output/stage23-s03-rerun-20260621-10/preflight`

Checks:

- Branch: `01-branch.txt`; branch was `work-branch`.
- Dirty state: `02-git-status.txt`; pre-existing Stage 21/22/23 changes were
  present and were not reverted.
- CUDA build flag: `03-cmake-cuda.txt`; `GGML_CUDA:BOOL=ON`.
- CUDA device state: `04-nvidia-smi-before.txt`; two NVIDIA GeForce RTX 5060 Ti
  devices, no active GPU process at preflight.
- Fixture: `05-fixture.txt`; model size 2,834,975,040 bytes.
- Ports: `06-port-listeners-8900-8921.txt`; empty, so BasePort 8900 was used.
- Cold path: `07-cold-path-before.txt`; empty.
- Disk: `08-disk.txt`; D: free space above 30 GiB output and 10 GiB cold
  thresholds.

## Clean build

Commands:

```text
cmake --build build-cov --config Release --target clean
cmake --build build-cov --config Release --target test-cache-controller -j 4
cmake --build build-cov --config Release --target llama-server -j 4
.\build-cov\bin\Release\test-cache-controller.exe
```

Evidence:

- `preflight/09-clean.log`, `09-clean-exit.txt`
- `preflight/10-build-test-cache-controller.log`,
  `10-build-test-cache-controller-exit.txt`
- `preflight/11-build-llama-server.log`, `11-build-llama-server-exit.txt`
- `preflight/12-test-cache-controller.log`,
  `12-test-cache-controller-exit.txt`
- `preflight/13-binary-freshness.txt`

Result: PASS. All exit files record `exit=0`. Binary freshness records
`llama-server.exe`, `llama-server-impl.dll`, and
`test-cache-controller.exe` after the clean build.

## Dry run

Command summary:

```text
kickoff-stage20-stress-longrun.ps1 -RowsToRun S03 -RunRoot ._test_output\stage23-s03-rerun-20260621-10 -ModelPath <Qwen3.5 MTP gguf> -CacheColdPath D:\tmp\cache-cold-stage23-s03-rerun-20260621-10 -CachePromptEvidenceDir ._test_output\stage23-s03-rerun-20260621-10\prompt-evidence -CacheColdMaxMib 512 -CacheRamMib 512 -CachePromptEvidence redacted -JinjaVariant new -BasePort 8900 -BatchSize 1 -DryRun
```

Evidence:

- `preflight/14-wrapper-dry-run-command.txt`
- `preflight/14-wrapper-dry-run.log`
- `preflight/14-wrapper-dry-run-exit.txt`
- `preflight/15-dry-run-side-log-checks.txt`
- `batch-summary.log.side`

Result: PASS. Dry-run exit was 0. Side log and check file show S03 only, run
root under `._test_output`, model path, `--cache-mode hybrid`,
`--cache-cold-max-mib 512`, `--cache-ram 512`, `--n-gpu-layers all`,
`--fit off`, redacted prompt evidence directory, and created cold/evidence
directories.

## Live S03

Command summary:

```text
kickoff-stage20-stress-longrun.ps1 -RowsToRun S03 -RunRoot ._test_output\stage23-s03-rerun-20260621-10 -ModelPath <Qwen3.5 MTP gguf> -CacheColdPath D:\tmp\cache-cold-stage23-s03-rerun-20260621-10 -CachePromptEvidenceDir ._test_output\stage23-s03-rerun-20260621-10\prompt-evidence -CacheColdMaxMib 512 -CacheRamMib 512 -CachePromptEvidence redacted -JinjaVariant new -BasePort 8900 -BatchSize 1
```

Evidence:

- Wrapper: `preflight/17-wrapper-live-command.txt`,
  `preflight/17-wrapper-live.log`, `preflight/17-wrapper-live.err`,
  `preflight/17-wrapper-live-exit.txt`
- GPU sampling: `preflight/18-nvidia-smi-during-live.txt`
- Row output: `S03-Jnew/`
- Row files: `launch.log`, `launch.err`, `server.out.log`, `server.err.log`,
  `metrics-before.txt`, `metrics-after.txt`, `resource-samples.csv`,
  `evidence-summary.md`
- QA extracts: `S03-Jnew/qa-summary-counts.txt`,
  `S03-Jnew/qa-metrics-selected.txt`, prompt evidence head/tail/raw scan files
- Prompt evidence: `prompt-evidence/cache-prompt-evidence.jsonl`

CUDA evidence: PASS. Server log lists CUDA0 and CUDA1 RTX 5060 Ti and listens
on `http://127.0.0.1:8900`. Live `nvidia-smi` samples show
`llama-server.exe` PID 14768 on both GPUs, about 1407 MiB on GPU0 and 1849 MiB
on GPU1.

## Row verdict

| Row | Verdict | Evidence | Notes |
| --- | --- | --- | --- |
| S03 | PASS | `S03-Jnew/evidence-summary.md`, `metrics-after.txt`, `server.err.log`, `batch-summary.log.side`, prompt JSONL | 30 minute run completed, wrapper exit 0, row gate OK, 5056 requests, after metrics present, no crash, no restore failure, no corrupt or unsafe restore. |

## Metrics and checks

- Wrapper: PASS. `17-wrapper-live-exit.txt` records `exit=0`; side log records
  `row_gate S03 exitCode=0 ok=True`.
- Workload: PASS. Evidence summary records 5056 requests and 1800 seconds.
- Cache outcomes: PASS. `llamacpp_cache_hits_total{mode="hybrid"} 0` and
  `llamacpp_cache_misses_total{mode="hybrid"} 5056`.
- Restore safety: PASS. `llamacpp_cache_restore_failures_total 0`,
  `cache_restore_misses_total{reason="exact_entry_absent"...} 5056`,
  1729 `try_restore - found match` log lines, 5056 `no exact match` lines, and
  no crash/access-violation marker.
- S03 unsafe-prefix behavior: PASS. No unsafe prefix restore occurred.
  `cache_prefix_candidates_total{result="none",reason="none"} 0` and log
  `unsafe_prefix_rejected` count was 0, so the row produced no unsafe prefix
  candidate to reject explicitly.
- Redacted evidence: PASS. JSONL has 5056 records. Head/tail records contain
  only redacted fields such as `namespace_hash`, token counts, checksum,
  `lookup_outcome`, and `prefix_candidate`. Raw-key regex scan count was 0 for
  prompt/message/content/role/user/assistant/system keys.
- Cold budget: PASS. Metrics show `cache_cold_bytes 53577036` and
  `cache_cold_budget_bytes 536870912`. Cold directory held 535,508,696 bytes,
  still under the 512 MiB budget. Cold pressure was bounded by 1743
  `cache_cold_evictions_total{reason="cold_budget_pressure"}`.
- Bounded pressure diagnostics: PASS. 3302 demotion-budget-pressure failures
  were recorded as structured diagnostics and hot evictions; no filesystem
  write failure or host allocation failure was observed.
- Checkpoint admissions: PASS for S03 scope. Metrics show 5056 checkpoint
  admission failures by descriptor shape; these were bounded misses and did not
  produce a restore failure or crash.
- `cap-exit.json`: not produced by the S03 script. This is a harness evidence
  quirk, not a row failure here, because the script completed normally, wrote
  after metrics, and the wrapper row gate passed.

## Handoff

Focused CUDA S03 rerun PASS. The F-23-S03-RERUN09-01 crash did not reproduce
after the reviewed fix.

Manager may decide whether to resume the stopped Stage 23 matrix at
S04..S08 and L01..L03.

Repository note: `git check-ignore -v` reports
`._design_docs/.test_reports/.gitignore:4:*` for this new report. The file is
present on disk, but normal `git status` hides it until the pre-existing ignore
ordering is fixed or the file is explicitly added by a human-approved command.
