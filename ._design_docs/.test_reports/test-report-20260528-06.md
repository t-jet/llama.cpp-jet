# Cache handling QA report: post-closure compatibility and Qwen3 model-suite pass

Test run ID: `test-report-20260528-06`
Date: 2026-05-28
Tester: QA agent
Plan: [cache-handling-test-plan.md](../cache-handling-test-plan.md)
Scope: Stage 5 post-closure draft-context compatibility and Qwen3 public mode smoke coverage.

## Environment

- OS and shell: Windows 11, PowerShell
- Git commit: `f8c08f42570d9569d43f58404c0b3409b7e2d773`, working tree dirty before this QA pass
- Build directory: `D:\source\llama.cpp-jet\build`
- Server binary: `D:\source\llama.cpp-jet\build\bin\Release\llama-server.exe`
- Focused test binary: `D:\source\llama.cpp-jet\build\bin\Release\test-cache-controller.exe`
- Target model: `D:\source\llama.cpp-jet\._test_models\Qwen3-8B-GGUF\Qwen3-8B-Q6_K.gguf`, 6,725,899,040 bytes
- Draft model: `D:\source\llama.cpp-jet\._test_models\Qwen3-0.6B-GGUF\Qwen3-0.6B-Q8_0.gguf`, 639,446,688 bytes
- Artifact directory: [test-report-20260528-06-artifacts/](test-report-20260528-06-artifacts/)

## Clean build evidence

Commands:

```powershell
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release --target llama-server -j 4
cmake --build build --config Release --target test-cache-controller -j 4
```

Result: PASS.

Binary timestamps after build:

```text
llama-server.exe          2026-05-28 16:37:35, 7,662,080 bytes
test-cache-controller.exe 2026-05-28 16:37:39,   579,584 bytes
freshness check time      2026-05-28T16:40:31+03:00
```

Notes:

- CMake configured with Visual Studio 18 2026 and MSVC 19.51.36243.0.
- OpenSSL was not found, so HTTPS support was disabled. This did not affect local HTTP cache tests.
- UI source build fell back to the provisioned Hugging Face UI bucket and checksum verification passed.

## Focused controller evidence

Commands:

```powershell
ctest --test-dir build -C Release -R test-cache-controller --output-on-failure
build\bin\Release\test-cache-controller.exe
```

Result: PASS.

Evidence:

```text
1/1 Test #28: test-cache-controller ............ Passed 0.03 sec
100% tests passed, 0 tests failed out of 1
test-cache-controller: namespace isolation - draft context modes...
  PASSED
All tests passed successfully!
Total: 44 tests (31 original + 5 Part 14 comprehensive + 4 Stage 4 focused + 4 Stage 5 focused)
```

Mapping:

- H57 PASS: `test_namespace_isolation_draft_context_modes()` asserts distinct namespaces for no draft, normal separate draft, target-derived MTP, and separate-model MTP.
- N25 PASS: the same focused test asserts normal separate draft mode differs from both MTP modes.
- N26 PASS: the same focused test asserts both MTP modes differ from normal separate draft mode.

This is focused compatibility-key evidence. It is not public GGUF-backed MTP save/restore evidence.

## Public server mode probes

All public probes used:

```powershell
--model <Qwen3-8B target>
--host 127.0.0.1
--ctx-size 512
--parallel 1
--cache-ram 100
--temp 0
--seed 42
--metrics
--log-verbosity 5
```

The completion request was:

```json
{"prompt":"QA cache probe. Answer with one word: ready","n_predict":1,"temperature":0}
```

| Row | Mode | Extra flags | Outcome | Evidence |
| --- | --- | --- | --- | --- |
| H40 | No draft, hybrid | `--cache-mode hybrid` | PASS | Server reached `/health`; request 1 `cache_n=0`; request 2 `cache_n=9`; metrics after: `hits_total=1`, `misses_total=1`, `hot_payload_descriptors=1`, `bytes=1475627`. |
| H43 | Normal separate draft, alias 1 | `--cache-mode hybrid --model-draft <Qwen3-0.6B>` | PASS | Server reached `/health`; request 1 `cache_n=0`; request 2 `cache_n=9`; metrics after: `hits_total=1`, `misses_total=1`, `hot_payload_descriptors=1`, `bytes=2623323`. |
| H43/H56 | Normal separate draft, alias 2 | `--cache-mode hybrid --spec-draft-model <Qwen3-0.6B>` | PASS | Same public behavior as `--model-draft`: request 2 `cache_n=9`, `hits_total=1`, `bytes=2623323`. Alias spelling did not change the observed public restore shape. |
| H53 | Target-derived MTP | `--cache-mode hybrid --spec-type draft-mtp` | BLOCKED | Server never reached `/health`. Exit code `1`. Logs end during model tensor loading after `getting device memory data for MTP context overhead`. Retry with `--fit off` also exited `1` before readiness. |
| H54 | Separate-model MTP | `--cache-mode hybrid --spec-type draft-mtp --model-draft <Qwen3-0.6B>` | BLOCKED | Server never reached `/health`. Exit code `-1073741819` on both default fit and `--fit off`, indicating an access violation during startup. |
| H50/H58 | Legacy smoke | `--cache-mode legacy` | PASS | Server reached `/health` with `{"status":"ok"}`; one completion returned HTTP 200; metrics stayed `mode="legacy"` and descriptor counters stayed zero. |

Artifact summaries:

- [qwen3-no-draft-hybrid.summary.txt](test-report-20260528-06-artifacts/qwen3-no-draft-hybrid.summary.txt)
- [qwen3-normal-draft-model-draft.summary.txt](test-report-20260528-06-artifacts/qwen3-normal-draft-model-draft.summary.txt)
- [qwen3-normal-draft-spec-draft-model.summary.txt](test-report-20260528-06-artifacts/qwen3-normal-draft-spec-draft-model.summary.txt)
- [qwen3-draft-mtp-target.summary.txt](test-report-20260528-06-artifacts/qwen3-draft-mtp-target.summary.txt)
- [qwen3-draft-mtp-target-fit-off.summary.txt](test-report-20260528-06-artifacts/qwen3-draft-mtp-target-fit-off.summary.txt)
- [qwen3-draft-mtp-separate.summary.txt](test-report-20260528-06-artifacts/qwen3-draft-mtp-separate.summary.txt)
- [qwen3-draft-mtp-separate-fit-off.summary.txt](test-report-20260528-06-artifacts/qwen3-draft-mtp-separate-fit-off.summary.txt)
- [qwen3-legacy-smoke.summary.txt](test-report-20260528-06-artifacts/qwen3-legacy-smoke.summary.txt)

## Bug and blocker notes

BUG-001: separate-model MTP startup crashes before readiness.

- Severity: HIGH for H54 public coverage; not evidence of a hybrid cache restore bug because the server does not reach cache setup or `/health`.
- Affected row: H54.
- Repro command:

```powershell
& ".\build\bin\Release\llama-server.exe" `
  --model "D:\source\llama.cpp-jet\._test_models\Qwen3-8B-GGUF\Qwen3-8B-Q6_K.gguf" `
  --model-draft "D:\source\llama.cpp-jet\._test_models\Qwen3-0.6B-GGUF\Qwen3-0.6B-Q8_0.gguf" `
  --cache-mode hybrid `
  --spec-type draft-mtp `
  --ctx-size 512 `
  --parallel 1 `
  --cache-ram 100 `
  --temp 0 `
  --seed 42 `
  --metrics `
  --log-verbosity 5
```

- Observed: exits before `/health` with `-1073741819`; `--fit off` gives the same exit code.
- Evidence: `qwen3-draft-mtp-separate.summary.txt`, `qwen3-draft-mtp-separate.err.log`, `qwen3-draft-mtp-separate-fit-off.summary.txt`.

BLOCKER-001: target-derived MTP public coverage is unavailable with this local target.

- Affected row: H53.
- Observed: exits before `/health` with code `1`; `--fit off` also exits `1`.
- Evidence: `qwen3-draft-mtp-target.summary.txt`, `qwen3-draft-mtp-target.err.log`, `qwen3-draft-mtp-target-fit-off.summary.txt`.
- Gate treatment: blocked by startup support, not by cache compatibility-key evidence.

## Results

| Category | PASS | FAIL | SKIP | BLOCKED |
| --- | ---: | ---: | ---: | ---: |
| Clean build | 1 | 0 | 0 | 0 |
| Focused controller | 1 | 0 | 0 | 0 |
| Public server probes | 4 | 0 | 0 | 2 |
| Total | 6 | 0 | 0 | 2 |

Overall status: BUG-HANDOFF.

Next gate recommendation:

- Accept the post-closure compatibility-key update for focused H57/N25/N26 evidence and for public Qwen3 target-only plus normal separate draft coverage.
- Keep public MTP save/restore coverage blocked until startup supports the selected model or model pair.
- Send BUG-001 to Developer or the speculative-startup owner. The crash blocks H54 public model-suite coverage but does not reopen the Stage 5 compatibility-key closure by itself.
