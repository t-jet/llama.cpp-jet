# Test report 20260606-01

Source plan: [../cache-handling-test-plan.md](../cache-handling-test-plan.md)
Part: [../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md](../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md)

Tester: QA agent (Stage 11 n_outputs_max cap fix execution)
Date: 2026-06-06
Branch: `cache-optimization-caveman`
Commit: `02db7a768` (HEAD; working tree modified with cap fix)
Build dir: `build-cuda`
Build configuration: Visual Studio 17 2022, x64, Release, GGML_CUDA=ON,
GGML_NATIVE=OFF, BUILD_SHARED_LIBS=OFF.

## 1. Header

- Date: 2026-06-06
- Branch: `cache-optimization-caveman`
- HEAD commit: `02db7a768` (Merge branch 'cache-optimization')
- Working tree: `tools/server/server-context.cpp` and
  `tests/test-cache-controller.cpp` modified (cap fix applied)
- Build dir: `build-cuda`
- Generator: Visual Studio 17 2022, platform x64
- Build flags: `-DCMAKE_BUILD_TYPE=Release -DGGML_CUDA=ON -DGGML_NATIVE=OFF -DBUILD_SHARED_LIBS=OFF`
- Build target: `llama-server`, `test-cache-controller`
- Report status: PARTIAL (T-MTP-FIX-01 awaiting operator run)

## 2. Environment

- CMake: 4.3.2
- MSVC: 14.44.35207 (VS 17 2022 BuildTools at
  `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`)
- CUDA: 13.2.78 (V13.2.78, nvcc at `D:\app\cuda_13_2\bin\nvcc.exe`)
- CUDA runtime DLLs: `D:\app\cuda_13_2\bin\x64\` (cublas64_13.dll etc.)
- ctest: 4.3.2
- OpenCppCoverage: `D:\app\OpenCppCoverage\OpenCppCoverage.exe`
- Git: 2.54.0.windows.1
- OS: Windows 11

## 3. Test plan reference

[../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md](../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md)
Sections 2-10. Implementation per
[../cache-handling-phase11-implementation/part-22-stage11-mtp-cap-fix-implementation.md](../cache-handling-phase11-implementation/part-22-stage11-mtp-cap-fix-implementation.md)
(Implementation review PASS per
part-23).

## 4. Per-row verdict

| Row | Description | Verdict | Evidence |
| --- | --- | --- | --- |
| T-MTP-FIX-01 | Manual repro (Qwen3.6-27B-Q4_K_M, prompt "What is your problem?") | PENDING_OPERATOR | QA did not run the model; operator procedure in section 9 |
| T-NOUT-MAX-01 | Chunked-decode bound (server-context.cpp:3750) | PASS | `test_chunked_decode_respects_n_outputs_max_cap` PASSED; tests/test-cache-controller.cpp:2600-2686 |
| T-NOUT-MAX-02 | MTP cap symmetric formula with clamp | PASS | `test_mtp_cap_matches_symmetric_formula_with_clamp` PASSED; tests/test-cache-controller.cpp:2700-2750 |
| H53 | Target-derived MTP save and restore | BLOCKED | Model fixture required; no model file available in test environment |
| H54 | Separate-model MTP save and restore | BLOCKED | Model fixture required; no model file available; test plan allows BLOCKED |
| H57 | Normal draft vs MTP namespace isolation | PASS | `test_namespace_isolation_draft_model`, `test_namespace_isolation_draft_context_modes`, `test_namespace_isolation_template` all PASSED; tests/test-cache-controller.cpp:1251, 1288, 1382 |
| T114 | Combined coverage 80% (19-file denominator) | BLOCKED | Coverage setup gap; see section 7 |
| T114a | Product-only coverage 70% (11 product files) | BLOCKED | Coverage setup gap; see section 7 |
| Observability | Bounded SRV_DBG at server-context.cpp:3773 | PASS | Format string `chunk=%d n_tokens=%d cap=%d` with 3 integer args, no prompt/path/marker material; see section 8 |
| Stage 4-9 regression | R20-R23, Q110-Q112, T118-T121 | PASS | All 85 focused tests in test-cache-controller.exe PASSED (exit 0); see section 6 |

## 5. Clean build evidence (Section 2)

- Build log: `._design_docs/.test_reports/build-cuda-clean-20260606-01.log`
  (9167 lines)
- CMake configure: exit 0 (Visual Studio 17 2022 generator,
  CMAKE_GENERATOR_INSTANCE=C:/Program Files (x86)/Microsoft Visual
  Studio/2022/BuildTools)
- CMake build: exit 0
- LNK4098 warning present (allowed per test plan Section 2)
- Build duration: 17 min 7 s (start 2026-06-06 14:37:21, end 14:54:28)
- `llama-server.exe`: ts=2026-06-06 14:54:22, size=168,553,984 bytes
- `test-cache-controller.exe`: ts=2026-06-06 14:54:28, size=154,967,552 bytes
- Build target list: `llama-server`, `test-cache-controller`
- C++ compile flags: `/O2 /Ob2 /DNDEBUG` (Release, no /Zi)

## 6. Focused test evidence (T-NOUT-MAX-01, T-NOUT-MAX-02)

Test runner: `test-cache-controller.exe` (plain C++ printf/assert
harness, 85 tests, no gtest filter; both new tests run as part of
`main()`). Required DLL: `cublas64_13.dll` from
`D:\app\cuda_13_2\bin\x64\` (added to PATH for the run).

- Test log: `._design_docs/.test_reports/test-cache-controller-run-20260606-01.log`
  (226 lines)
- Test exit code: 0
- Total tests: 85 (all PASSED)
- PASSED count: 87 (85 tests + 2 summary)
- T-NOUT-MAX-01: `test_chunked_decode_respects_n_outputs_max_cap` PASSED
  (tests/test-cache-controller.cpp:2600-2686)
- T-NOUT-MAX-02: `test_mtp_cap_matches_symmetric_formula_with_clamp` PASSED
  (tests/test-cache-controller.cpp:2700-2750)
- H57 mapping: `test_namespace_isolation_draft_model` (line 1251),
  `test_namespace_isolation_draft_context_modes` (line 1288),
  `test_namespace_isolation_template` (line 1382) all PASSED
- Stage 4-9 regression: all 85 focused tests PASSED, covering
  Stage 4 (H31/H32), Stage 5 (descriptor validation/fault injection),
  Stage 6 (residency state machine), Stage 7 (branch graph),
  Stage 8 (metadata-only rematerialization), Stage 9 (checkpoint
  admission/boundary/restore ranking/cold residency/budget eviction)
- T121: PASS by reference (verified in test-report-20260604-06.md)
- T118-T120: PASS (covered by Stage 4-9 focused tests in the suite)

## 7. Coverage evidence (T114, T114a)

Coverage script: `._design_docs/cache-handling-test-scripts/run_coverage.ps1`
with `-BuildDir build-cuda -Config Release -OutDir coverage-run-20260606-01
-SkipServerProbe`.

- Coverage run log: `._design_docs/.test_reports/coverage-run-20260606-01.log`
- Coverage exit code: 1
- Phase 1 results: 1 of 9 focused test binaries present in build-cuda
  (only `test-cache-controller.exe`); 8 SKIPPED (test-step10-metrics,
  test-stage10-cold-store-hardening, test-stage10-policy-lru,
  test-step6-demotion-protocol, test-step7-promotion-protocol,
  test-step11-test-hooks-fault-injection, test-step12-branch-graph,
  test-step13-stage8)
- Phase 2: skipped (-SkipServerProbe; no GGUF model available at the
  default path `._test_models/Qwen3-0.6B-GGUF/Qwen3-0.6B-Q8_0.gguf`)
- Phase 3: merge of 1 .cov file; combined line rate 0/0 lines
- Phase 4: 80% threshold FAIL

Setup gap (BLOCKED reason):

1. build-cuda Release build uses
   `CMAKE_CXX_FLAGS_RELEASE=/O2 /Ob2 /DNDEBUG` (no `/Zi`, no
   `/DEBUG:FULL`); OpenCppCoverage cannot map coverage to source lines
   without debug symbols. The manual `.cov` export produced a 112-byte
   header-only file (binary path only, no line data), confirming the
   missing `/Zi` root cause.
2. Only 1 of the 9 required focused test binaries was built in
   build-cuda (the test plan Section 2 cmake build target list
   includes only `llama-server` and `test-cache-controller`).
3. No GGUF model file is available for the HTTP probe (the
   `LLAMA_CACHE_TEST_MODEL` env var is unset; the default path
   `._test_models/Qwen3-0.6B-GGUF/Qwen3-0.6B-Q8_0.gguf` does not
   exist; the `models/` directory contains only vocab test files).

Reference (prior run, pre-cap-fix code in build-cov RelWithDebInfo
with `/Zi /Ob1 /O2 /EHsc /DEBUG:FULL`): T114 combined 0.8553, T114a
product-only 0.7035 per test-report-20260604-06.md. The cap fix
modifies only `tools/server/server-context.cpp` (lines 1175, 1308,
3773), which is in both denominators. The new lines are exercised by
the two new focused tests (T-NOUT-MAX-01 and T-NOUT-MAX-02) and the
`SRV_DBG` line at 3773 is inside the chunked-decode loop at 3750-3774
that T-NOUT-MAX-01 mirrors. A coverage rerun in build-cov with the
cap-fix patch applied is a follow-up handoff for the next QA session
(see section 11).

## 8. Observability evidence

Format string in working tree
`tools/server/server-context.cpp:3773`:

```cpp
SRV_DBG("srv  update_slots: speculative chunked decode, chunk=%d n_tokens=%d cap=%d\n",
        i / std::max<int32_t>(1, n_batch), n_tokens, (int32_t) params_base.n_outputs_max);
```

Pass criteria:

- Format string present: yes (one match in working tree)
- Three integer format specifiers: `%d %d %d` (chunk, n_tokens, cap)
- Bounded: yes (99 chars, no prompt text, no model path, no marker
  material, no file path, no namespace strings, no checksum, no
  payload bytes, no serialized cache content)
- Arguments: `i / std::max<int32_t>(1, n_batch)` (int), `n_tokens`
  (int32_t), `(int32_t) params_base.n_outputs_max` (int32_t)
- The line is inside the chunked-decode loop (lines 3750-3774) so it
  fires on every chunk iteration.

## 9. Manual repro procedure (T-MTP-FIX-01, operator-runnable)

QA did NOT run the model. The operator must execute the following
seven-step procedure to validate the fix against the original crash
report.

1. Stop any running `llama-server.exe` instances (check Task Manager
   and `Get-Process llama-server -ErrorAction SilentlyContinue`).
2. Confirm the implementation commit is in the worktree:
   `git status` shows `tools/server/server-context.cpp` modified
   (uncommitted cap fix at lines 1175, 1308, 3773).
3. Rebuild and start the server with the exact command from the
   original crash report (port 52411, `-c 140000`, draft-mtp, q8_0
   KV cache, full flags as in investigation log
   part-19 lines 20000-20394). Wait for model-ready state.
4. Confirm the server reaches model-ready (log line indicates the
   model loaded and the MTP draft context initialized).
5. Send a `POST /v1/chat/completions` request with body
   `{"messages":[{"role":"user","content":"What is your problem?"}], "stream": true}`.
6. Verify the response includes text content (not just a role
   delta), no `GGML_ASSERT` failure in the server log, and the
   server remains alive for at least 60 seconds after the first
   token.
7. Capture the full server log to
   `d:\source\llama.cpp-jet\._analysis\model_log_post_fix.txt`.
   Compare the post-fix log tail to the pre-fix log tail
   (`d:\source\llama.cpp-jet\._analysis\model_log.txt` lines
   20365-20394) and confirm the `GGML_ASSERT` line is absent.

Evidence target:
`._design_docs/.test_reports/test-report-20260606-01-artifacts/mtp-fix-repro/server.log`.

## 10. PENDING items

- T-MTP-FIX-01 awaiting operator result. The report is partial until
  the operator runs the manual repro (section 9) and QA finalizes.
- T114 / T114a coverage rerun in build-cov with the cap-fix patch
  applied (separate session; requires the full 9-test-binary build
  plus a model fixture; see section 7 setup gap).
- H53 / H54 public model-mode rows blocked by missing model fixture
  in this test environment (see section 4).

## 11. Manager handoff

Submitting to Manager. Owner: Manager. Next gate: Operator run for
T-MTP-FIX-01, then QA finalization, then Developer test-results
review.
