# Stage 39 focused QA rerun

Date: 2026-07-17
Gate: D39-QA-03
Verdict: BLOCKED
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260717-03/`

## Scope

Manager Part 155 authorized one fresh canonical TP-39-03 node, followed only
after a pass by four coverage blocks. The canonical node stopped at its guarded
startup-log proof. The fail-fast rule therefore kept every coverage block
unopened. QA changed no product, driver, fixture, seam, plan, or threshold.

## Environment and build

- HEAD was `2c501427cdc318b50d50fcfe329e71f90a9a1e89`; pre-run dirty state is in
  `git-status-before.txt` and `git-diff-stat-before.txt`.
- Configuration was Release, CPU (`GGML_CUDA=OFF`), with tests and
  `LLAMA_STAGE39_LIVE_TEST_SEAM=ON`.
- The Qwen3.5-4B MTP fixture was 2,834,975,040 bytes with SHA-256
  `3874209241C9A397E2F62CD3F70F80FD2DFBF0DFCCB6838416BDB48A714E8630`.
  Its 10,313-byte template SHA-256 was
  `47A7BDCBAE1F459E0A6F793D22E428A17F355C5A3100B77A14A601737A82F7F5`.

QA safely removed the resolved workspace `build` and
`build-stage39-seam-on` trees. Fresh configure exited 0 in 10.567 seconds.
Fresh build exited 0 in 249.141 seconds for `llama-server` and all 11 coverage
targets. `binary-inventory.json` and `server-impl-inventory.json` preserve
fresh timestamps, sizes, and hashes. Tool and fixture evidence is also under
the report artifact root.

## Cheap gates

| Check | Result |
| --- | --- |
| PowerShell 7 parser API | PASS, exit 0, zero errors |
| Windows PowerShell 5 parser API | PASS, exit 0, zero errors |
| PowerShell 7 pure self-test | PASS, exit 0 |
| Windows PowerShell 5 pure self-test | PASS, exit 0 |

Evidence: `cheap-tests.json` and the four parser/self-test logs.

## TP-39-03

QA ran exactly one canonical node with the approved fixture, port 8194,
8192-token context, two chats, and the binding 20-minute, 16-GiB RSS,
4-GiB cold-root, and six-chat caps. It exited 1 after 168.5 seconds:

```text
SKIP-preflight-checkpoint-startup-proof
```

The failure was at driver line 1161, before discovery, apply, or
`Assert-Tp3903`. Prompt counts were 3,631 and 3,632 (7,263 total, 929-token
margin). Peak sampled RSS was 5,524,627,456 bytes; cold-root bytes stayed zero.

The live server did initialize speculative decoding, enable checkpoints, create
checkpoints, and save target-plus-draft state twice. The guard nevertheless
requires the literal marker `speculative decoding will use checkpoints`. The
fresh server emitted `speculative decoding context initialized` instead. The
other three required markers matched. This is a driver startup-log contract
blocker; no Stage 39 product invariant was reached or violated.

| ID | Verdict | Reason |
| --- | --- | --- |
| TP-39-03 | BLOCKED | Startup proof rejected the current equivalent speculative-decoding marker before `Assert-Tp3903`. |

Evidence: `tp39-03-command.txt`, `tp39-03-result.json`,
`tp39-03-diagnosis.json`, `tp39-03-run.log`, and `TP-39-03/`.

## Coverage

| Block | Classification |
| --- | --- |
| PowerShell 7 success | NOT RUN, required fail-fast stop |
| PowerShell 7 forced merge failure | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 success | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 forced merge failure | NOT RUN, required fail-fast stop |

Coverage remains BLOCKED: no fresh capture, merged XML, Markdown percentage,
or forced exit-23 evidence exists. `coverage-stop.json` records all four absent
roots.

## Cleanup and handoff

Cleanup found zero `llama-server.exe` processes and zero port 8194 listeners.
Artifact hashes are in `artifact-verification.csv`.

Final classification: TP-39-03 `BLOCKED`; coverage `BLOCKED`; session
`BLOCKED`. Developer review should reconcile the strict startup marker with the
fresh server's current marker without weakening the required MTP/checkpoint
proof. Fresh Manager authorization is required before another model run.
