# Stage 39 full QA execution report

Date: 2026-07-13
Verdict: BLOCKED
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260713-01/`

## Scope and authority

This is the fresh post-F39-QA4-01 execution gate. Authority was Part 43, QA
report 20260712-04 and its Developer review, fixes, implementation Part 36, and
Architect Part 37 PASS. QA changed no product, test, runner, or plan file.

## Environment and clean build

- Commit and dirty state: `git-head.txt`, `git-status-before.txt`.
- Clean gate: the old `build` directory was removed, then CMake configured a
  Visual Studio 2022 x64 tree. Configure and required-target build exited 0.
- Build: Release, `GGML_CUDA:BOOL=OFF`. Host memory was 66,190,503,936 bytes,
  with 40,606,511,104 bytes free at fixture inventory time.
- Required targets: `llama-server` plus all 11 executables named by the
  canonical coverage script. Logs: `configure.log`, `build.log`.

## Result matrix

| ID | Verdict | Fresh evidence |
| --- | --- | --- |
| TP-39-01 | PASS | `live-standard/summary.json` passed. State records a 71-token restore, promotion delta 1, payload-eviction delta 0, pruning delta 0, 8,373,860 cold descriptor bytes, and 16,633,369 cold-file bytes. |
| TP-39-02 | BLOCKED | Focused multi-victim matrix passed, but no live equal-rank payload-ID tie-break exists. Qwen3.5-4B at 64/128 MiB emitted `cold_room_made`, ended with two roughly 55.8 MB cold objects, then failed the required standard restore. No canonical control makes all required victims equal-rank. |
| TP-39-03 | BLOCKED | Qwen3-0.6B 7/8 MiB and Qwen3.5-4B 48/64 MiB attempts emitted no Stage 39 row. The 4B log shows six payloads of 55,052,492 through 55,216,432 bytes rejected against the 50,331,648-byte hot budget, before cold eligibility. No `evicted/both_filled` tuple exists. |
| TP-39-04 | BLOCKED | Qwen3-0.6B 7/7 MiB and Qwen3.5-4B 48/48 MiB attempts emitted no Stage 39 row. The 4B attempt has the same upstream 55,052,492 through 55,216,432 versus 50,331,648-byte hot rejection. No `evicted/oversized_both` tuple or partial pair exists. |
| TP-39-05 | PASS | Canonical `cold-disabled` summary passed. Fixed `hot-zero` completed and wrote a PASS summary with both Stage 39 families absent. |
| TP-39-06 | PASS | Focused rollback and transaction-fault assertions passed. |
| TP-39-07 | PASS | Focused target/draft lifecycle passed. |
| TP-39-08 | PASS | Focused same-entry descriptor pressure passed. |
| TP-39-09 | PASS | Focused protected-root/live-descendant pressure passed. |
| TP-39-10 | PASS | Focused concurrent production pressure passed. |
| TP-39-11 | PASS | Fixed canonical `legacy` completed and wrote a PASS summary with both Stage 39 families absent. |
| TP-39-12 | PASS | Standard live run drove production cold transactions and restored 71 tokens. |
| TP-39-13 | PASS | Focused size-boundary and overflow cases passed. |
| TP-39-14 | PASS | Focused mutation-position and restart matrix passed. |
| TP-39-15 | PASS | Focused exporter checks passed. Live samples retain one `mode` label and bounded families. |

TP counts: PASS 12, FAIL 0, BLOCKED 3, SKIP 0. Coverage is a separate gate and
is not included in TP counts.

## Focused and canonical live execution

All 11 focused executables exited 0. Their per-binary logs and `summary.json`
are under `focused/`. `ctest -C Release -R cache -V` passed 1/1.

The canonical live driver was used without modification for `standard`,
`both-filled`, `oversized-both`, `cold-disabled`, `hot-zero`, and `legacy`.
Standard, cold-disabled, hot-zero, and legacy wrote PASS summaries. The two
calibration scenarios wrote state and request evidence, then failed their exact
tuple guards because no Stage 39 decision series was emitted.

F39-QA4-01 is closed by execution: `live-hot-zero/summary.json` and
`live-legacy/summary.json` both exist, report `Outcome=PASS`, and record zero
decision and transaction series.

## Fixture inventory and calibration

`gguf-inventory.csv` records 21 on-disk GGUF files. It distinguishes main model
files from `mmproj` auxiliaries. Beyond Qwen3-0.6B, QA ran both viable 4B main
fixtures:

- Qwen3.5-4B-Q4_1, 2,784,416,928 bytes;
- Qwen3.5-4B-Q4_K_M, 2,834,975,040 bytes.

Both loaded and completed 24 requests at 64/128 MiB. Both failed the required
repeat restore, so neither produced a standard PASS summary. The regular 4B
calibration rerun at 48 MiB hot produced exact upstream rejection evidence
before two-layer selection. Larger main fixtures and multimodal-only pairings
remain enumerated in the CSV; running them would not remove the demonstrated
hot-admission precondition, and the clean build is CPU-only.

TP-39-02 still lacks an equal-rank live workload control. TP-39-03 and TP-39-04
remain unreachable because lowering the positive hot budget below the measured
pair causes `tx_save` to reject the candidate before Stage 39 policy selection.

## Canonical coverage

`run_coverage.ps1` ran fail-closed with no bypass switch. Phase 1 produced 11
focused `.cov` files. Mandatory Phase 2 issued the `llama-server` HTTP probe and
produced `server-http-probe.cov` (98 bytes). Phase 3 attempted to merge all 12
captures.

The merge exited 1. `coverage/merge.stderr` says `"exit" is not recognized as
an internal or external command`. The generated Cobertura file has
`lines-covered="0"` and `lines-valid="0"`; the report therefore records 0/0,
not a percentage. Coverage verdict: BLOCKED-canonical-merge. The binding 80%
gate is not met. No custom runner, server-probe skip, or incomplete-coverage
allowance was used.

## Gate and handoff

No fresh product FAIL was observed. Stage 39 cannot close because TP-39-02,
TP-39-03, TP-39-04, and measurable changed-line coverage remain blocked.

Next owner: Manager. Choose a reviewed diagnostic/calibration seam or revise the
three live row contracts. Developer owns any authorized seam and the canonical
coverage merge invocation defect. Architect reviews those changes before QA
reruns the blocked scope.

Every cited path was checked. `artifact-verification.txt` contains the final
existence and size audit.
