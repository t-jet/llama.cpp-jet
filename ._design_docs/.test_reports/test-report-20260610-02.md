# Test report 20260610-02: Stage 13 CUDA blocked-row endpoint rerun

- Date: 2026-06-10 local
- Owner: QA execution
- Scope: Stage 13 blocked rows only: E13-01c, E13-07, E13-08, E13-10, E13-13, E13-14, E13-16
- Verdict: FAIL
- Evidence root: `._design_docs/.test_reports/test-report-20260610-02-artifacts/`
- Build directory: `build-cuda-stage13`
- CUDA-only rule: all verdict evidence in this report uses `build-cuda-stage13` CUDA binaries.

## Build and environment

CUDA settings were captured in `cmake-cache-cuda-settings.txt`:

- `GGML_CUDA:BOOL=ON`
- `GGML_CUDA_NCCL:BOOL=OFF`
- `CMAKE_CUDA_ARCHITECTURES:STRING=native`

Fresh clean build commands:

```text
cmake --build build-cuda-stage13 --config Release --target clean
cmake --build build-cuda-stage13 --config Release --target llama-server test-cache-controller --parallel 4
```

Build evidence:

- `cuda-clean-release-build.log`: clean target exited 0.
- `cuda-build-release-targets.log`: target build exited 0 after native CUDA compile.
- `cuda-binary-timestamps.txt`: `llama-server.exe` timestamp `2026-06-10 10:54:04`, `test-cache-controller.exe` timestamp `2026-06-10 10:54:09`, `ggml-cuda.dll` timestamp `2026-06-10 10:53:19`.
- `git-status-before-build.txt` and `git-status-after-run.txt`: dirty tree captured. Pre-existing Stage 13 source/doc changes were not modified by QA.

## Row results

| Row | Verdict | Evidence |
| --- | --- | --- |
| E13-01c | FAIL | CUDA multimodal servers reached readiness with Qwen2.5 Omni and Qwen2.5 VL plus mmproj. Documented native `/completion` multimodal object prompt with generated JPEG and `<__media__>` marker returned 400 `Failed to tokenize prompt` in `E13-01c-image-marker-*`. OAI `image_url` data/video attempts also failed, and one invalid multimodal attempt aborted server at `tools/server/server-common.cpp:398: GGML_ASSERT(!has_mtmd) failed` in `omni-rerun-server.err.log`. No miss/hit or restore evidence was possible. |
| E13-07 | FAIL | `/v1/audio/transcriptions` on Omni CUDA server accepted multipart `file` and converted request to chat, then connection reset. `audio-only-server.err.log` shows audio tokens added and abort at `tools/server/server-common.cpp:398: GGML_ASSERT(!has_mtmd) failed`. |
| E13-08 | FAIL | `/audio/transcriptions` alias was run first in a fresh Omni CUDA process. `E13-08-audio-alias-first-status.txt` shows curl exit 56, and `audio-alias-first-server.err.log` shows the same `GGML_ASSERT(!has_mtmd)` after audio token processing. |
| E13-10 | PASS | `/slots` rerun used model server, not router mode, with `--slot-save-path`. Save, restore, and erase returned stable public schemas: `E13-10-rerun-slots-save-response.json`, `E13-10-rerun-slots-restore-response.json`, `E13-10-rerun-slots-erase-response.json`. Earlier 501 without `--slot-save-path` is preserved as setup evidence only. |
| E13-13 | PASS | `build-cuda-stage13\bin\Release\test-cache-controller.exe` ran with CUDA DLL path and exited 0. `E13-13-test-cache-controller.log` includes `Stage 13 route-neutral preparation identity... PASSED` and final `All tests passed successfully! Total: 87 tests`. |
| E13-14 | FAIL | Native and chat degraded probes returned responses and leakage scans found no marker, tool arg, checksum, raw media, request, or response echo in matched log lines. However both `E13-14-diagnostic-lines.txt` and `E13-14-chat-diagnostic-lines.txt` record `no cache metadata diagnostic line emitted`, so the bounded degraded diagnostic contract was not met. |
| E13-16 | PASS | Fresh report and clean CUDA Release build gate satisfied before endpoint/helper verdicts. Build logs, commands, git status, and binary timestamps are captured under this report's artifact root. |

## Historical context

Rows that passed in `test-report-20260609-04.md` remain historical context only and are not recounted here: E13-01a, E13-01b, E13-01d, E13-02, E13-03, E13-04, E13-05, E13-06, E13-09, E13-11, E13-12, and E13-15.

## Summary

- PASS: 3
- FAIL: 4
- BLOCKED: 0
- Product bugs: yes

Product bug evidence:

- Multimodal/audio MTMD paths can abort `llama-server` through `GGML_ASSERT(!has_mtmd)` in `server-common.cpp:398` after media/audio prompt handling.
- Native multimodal `/completion` documented prompt shape with Qwen2.5 VL plus mmproj returned 400 `Failed to tokenize prompt`.
- Degraded fallback probes did not emit the bounded `cache metadata:` diagnostic line required by Stage 13.

Next owner: Developer. Gate status: bug handoff. Stage 13 endpoint execution should not close until E13-01c, E13-07, E13-08, and E13-14 are fixed or explicitly reclassified by Manager with new design evidence.
