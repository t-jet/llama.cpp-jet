# Test report 20260610-04: Stage 13 E13-14 + E13-16 CUDA rerun

- Date: 2026-06-10 local
- Owner: QA execution
- Scope: E13-14, E13-16 only
- Verdict: PASS
- Evidence root: `._design_docs/.test_reports/test-report-20260610-04-artifacts/`
- Build directory: `build-cuda-stage13`
- CUDA-only rule: every verdict uses CUDA Release binaries from `build-cuda-stage13`. CPU build probes were not run.

## Build and environment

This report reuses the clean build from report 03 plus the 16:04 rebuild cited by the Developer bug-fix loop. Binary timestamps in `cuda-binary-timestamps.txt` (retained from report 04 prior artifacts):

- `build-cuda-stage13/bin/Release/llama-server.exe` 2026-06-10 16:04:23
- `build-cuda-stage13/bin/Release/test-cache-controller.exe` 2026-06-10 16:04:28
- `build-cuda-stage13/bin/Release/ggml-cuda.dll` 2026-06-10 16:03:37

`cmake-cache-cuda-settings.txt` and `cuda-build-release-targets.log` from report 04 prior artifacts still apply. This session did not rebuild, did not edit product code, and did not modify prior reports.

## Process inventory

| Probe | Port | PID | Cleanup |
| --- | --- | --- | --- |
| E13-14 degraded rerun | 18234 | 32440 | graceful taskkill |

`e13-14-qwen-degraded-rerun-timestamps.txt` records start, pid, health-ready iteration, graceful-kill attempt, exit, and force fallback if any. The `/quit` endpoint does not exist on this build (404 from `e13-14-qwen-degraded-rerun-quit-error.txt`); graceful taskkill without `/F` did not exit the detached process, so `Stop-Process -Force` was used as fallback. The log was already complete with 1622 lines before the kill, so no request-phase data was lost.

## Row results

| Row | Verdict | Evidence |
| --- | --- | --- |
| E13-14 | PASS | Both degraded probes returned `status=OK`. Bounded `cache metadata:` lines emitted for both route families. Diagnostic-line leak scan clean. |
| E13-16 | PASS | Fresh report 04 created, CUDA Release binary timestamps cited above, build evidence on disk, no product edits in this session. |

## E13-14 per-row evidence

Native `/completion` degraded probe:

- Request: `E13-14-native-degraded-request.json` reused (boundary-missing string prompt with secret marker).
- Response: `E13-14-native-degraded-rerun-response.json`, `E13-14-native-degraded-rerun-status.txt` shows `status=OK`. Timings show `cache_n=0`, `prompt_n=30`, `predicted_n=2`, no error.
- Diagnostic line: `0.35.094.407 I srv  launch_slot_: cache metadata: source=native-completion method=token-position-fallback degraded=minimal token span metadata tokens=30 boundaries=2`.
- Cited from `e13-14-qwen-degraded-rerun-server.err.log` line 1314 (1-indexed). Saved to `E13-14-diagnostic-lines-rerun.txt`.

OpenAI `/v1/chat/completions` degraded probe:

- Request: `E13-14-chat-degraded-request.json` reused (system + user with secret marker).
- Response: `E13-14-chat-degraded-rerun-response.json`, `E13-14-chat-degraded-rerun-status.txt` shows `status=OK`. `id=chatcmpl-17xu2PP6GVH64FwMcbpLnWwEzHBbFR5q`, `model=Qwen3-0.6B-Q8_0.gguf`, 1 choice, `finish_reason=length`.
- Diagnostic line: `0.43.239.542 I srv  launch_slot_: cache metadata: source=openai-chat method=rendered-text-boundary-inference degraded=rendered text boundary inference tokens=46 boundaries=6`.
- Cited from `e13-14-qwen-degraded-rerun-server.err.log` line 1501 (1-indexed). Saved to `E13-14-diagnostic-lines-rerun.txt`.

`/metrics` end snapshot (`e13-14-qwen-degraded-rerun-metrics-end.txt`, 309 lines) shows hybrid counters moved: `llamacpp_cache_entries{mode="hybrid"} 2`, `llamacpp_cache_tokens{mode="hybrid"} 78`, `llamacpp_cache_misses_total{mode="hybrid"} 2`, `cache_namespace_count{mode="hybrid"} 2`, and `cache_branch_lookups_total` rows under two distinct namespaces (`11473233988182544917`, `3348821213330223339`) by `method=token_span` and `method=checksum_span`.

## Leak scan

Scope follows the QA memory rule: leak scan targets the `SRV_DBG` `cache metadata:` line family only. Pre-existing `log_server_r: request:` and `log_server_r: response:` lines at `--log-verbosity 5` echo the full request and response bodies and are not a Stage 13 regression. Per-line hit counts from `E13-14-leak-scan-rerun.txt`:

| Diagnostic line | secret | tool_arg | path | checksum | rawmedia |
| --- | --- | --- | --- | --- | --- |
| native `cache metadata: source=native-completion` | 0 | 0 | 0 | 0 | 0 |
| chat `cache metadata: source=openai-chat` | 0 | 0 | 0 | 0 | 0 |

The bounded line format kept `source=`, `method=`, `degraded=`, `tokens=`, `boundaries=` only, matching the part-23 E13-14 contract.

## Summary

- PASS: 2
- FAIL: 0
- BLOCKED: 0
- SKIP: 0
- Product bugs: none
- Unresolved blockers: none

E13-14 emitted the required bounded `cache metadata:` diagnostic line for both native and chat degraded fallback probes, and the diagnostic lines contained no prompt text, marker text, tool arguments, paths, checksums, payload bytes, or raw media. E13-16 is satisfied: fresh report, fresh build timestamps, no product edits, all evidence under the session artifact root.

Next owner: Developer review of the rerun evidence, then Manager closure decision for Stage 13 E13-14 + E13-16.
