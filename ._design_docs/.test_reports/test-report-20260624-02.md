# Stage 24 build-path inspection and aborted CUDA execution setup

RunId: `stage24-chat-s02-s03-20260624-02`
RunRoot: `._test_output/stage24-chat-s02-s03-20260624-02`
Date: 2026-06-24
Status: `BLOCKED-build-path-amendment`

## Scope

This record preserves the abandoned setup attempt for suffix `20260624-02`.
No dry-run plan was written, no route-only gate ran, no `llama-server` live leg
started, and no S02/S03 request evidence exists.

## Findings

The attempted clean `build-cov` CUDA build used the Stage 24 plan command that
removes `build-cov` before configure. The configure selected Visual Studio 18
2026 and `GGML_CUDA=ON`, but the fresh tree forced full `ggml-cuda` kernel
compilation. That is the source of the long build time.

This shell is not a Visual Studio prompt: `cl` and `MSBuild` are not on PATH.
CMake still found Visual Studio and invoked it through the generated solution,
so this was not a missing-CUDA-library issue.

The previous CUDA-stage build directory `build-cuda` is configured as Visual
Studio 17 2022 x64 with `GGML_CUDA=ON`, `GGML_NATIVE=OFF`, and
`BUILD_SHARED_LIBS=OFF`. A non-destructive target build on that directory
completed in 17.042 seconds after the source compile blocker below was fixed.

## Compile blocker found and fixed

The first prior-style `build-cuda` build failed in current source:

```text
tools/server/server-context.cpp(4589,1): error C2065: 'Explain': undeclared identifier
```

The offending line was an accidental uncommitted token before an `if` statement:

```text
Explain while     if (!metadata.has_boundaries() && !tokens.empty()) {
```

The token was removed. The corrected `build-cuda` target build passed:

```text
Command: cmake --build build-cuda --config Release -j --target llama-server
Result: exit 0, elapsed 17.042 s
Binary: build-cuda/bin/Release/llama-server.exe
Size: 168648704
LastWriteTimeUtc: 2026-06-24T12:02:15.4493131Z
```

## Evidence paths

```text
._test_output/stage24-chat-s02-s03-20260624-02/preflight-qa-20260624-133403/cmake-configure.log
._test_output/stage24-chat-s02-s03-20260624-02/preflight-qa-20260624-133403/cmake-build-llama-server.log
._test_output/stage24-build-inspection-build-cuda-incremental.log
._test_output/stage24-build-inspection-build-cuda-incremental-02.log
```

## Verdict and handoff

Overall verdict: `BLOCKED-build-path-amendment`.

Suffix `20260624-02` is consumed by this setup record. Do not reuse its run
root. Manager must amend the Stage 24 execution handoff before QA starts a new
fresh suffix.
