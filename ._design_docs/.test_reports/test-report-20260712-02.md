# Stage 39 QA execution report

Date: 2026-07-12
Verdict: BLOCKED
Plan: `cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260712-02/`

## Session

QA recorded the dirty tree before execution in `preflight-git-status.txt`. The
tree already contained Stage 39 product, test, script, and documentation changes,
plus the unrelated modified Grafana dashboard. QA did not edit product code or
the dashboard.

The clean-build rule was followed: QA removed `build`, configured Release, and
built fresh `llama-server` and `test-cache-controller` binaries. Both focused
targets built successfully and their timestamps are in `binary-freshness.json`.
The full required-target command failed because
`test-step6-demotion-protocol.cpp` still calls removed controller APIs including
`process_completions`, `debug_start_io_worker_for_tests`, and
`debug_stop_io_worker_for_tests`. See `build.log`.

Fixtures found in the repository include the 639,446,688-byte Qwen3-0.6B model,
Qwen3.5-4B target/MTP models, Qwen3-8B, and Qwen3.6-27B. QA also searched the
documented external roots and leading-dot variants. Evidence is in
`repo-model-fixtures.txt`, `external-model-fixtures.txt`, and
`model-root-references.txt`.

## Results

| ID | Verdict | Evidence and reason |
| --- | --- | --- |
| TP-39-01 | PASS | Canonical standard live run committed cold data, restored 71 cached tokens, recorded promotion, and kept payload eviction and pruning deltas at zero. |
| TP-39-02 | BLOCKED | Focused tests exercise multi-victim transactions, but no evidence proves equal-rank payload-ID tie-breaking plus the complete live contract. |
| TP-39-03 | BLOCKED | Focused capacity-reason selection passed, but no live run proved both positive layers filled with no eligible victim. |
| TP-39-04 | BLOCKED | Focused size selection passed. Canonical live attempts with 8/16 MiB and 1/1 MiB did not emit `evicted/oversized_both`; the smaller run emitted no Stage 39 decision series. |
| TP-39-05 | PASS | Canonical cold-disabled run emitted the bypass family. Hot-zero run emitted neither Stage 39 family. |
| TP-39-06 | PASS | Focused controller transaction fault tests retained hot bytes and passed rollback boundaries. |
| TP-39-07 | BLOCKED | Prepared-object tests include target/draft data, but evidence does not cover pair-atomic commit, restore, rollback, and eviction as one mapped row. |
| TP-39-08 | BLOCKED | No mapped exact-blob and checkpoint independent-pressure evidence exists. |
| TP-39-09 | BLOCKED | No mapped protected-root/live-descendant Stage 39 pressure evidence exists. |
| TP-39-10 | BLOCKED | No mapped concurrent-slot transaction evidence exists. |
| TP-39-11 | BLOCKED | Canonical Stage 39 script has no legacy scenario; no equivalent live legacy workload was captured. |
| TP-39-12 | PASS | Standard live run drove production `tx_save` pressure, committed cold data, and restored it before any payload eviction. |
| TP-39-13 | PASS | Focused serialized-size boundary test covered exact fit, one-byte-over, overhead, and checked-add overflow. |
| TP-39-14 | PASS | Focused boundary, multi-victim mutation-position, and restart-idempotence tests passed. |
| TP-39-15 | PASS | Focused typed metric/cardinality test passed; live standard and hot-zero runs confirmed bounded/absent families. |

Counts: PASS 7, FAIL 0, BLOCKED 8, SKIP 0.

## Build, coverage, and live evidence

- `test-cache-controller.exe`: PASS; `test-cache-controller.log`.
- `ctest -C Release -R cache`: PASS, 1/1; `ctest-cache.log`.
- Full required-target Release build: FAIL; stale removed APIs in
  `test-step6-demotion-protocol.cpp`; `build.log`.
- Canonical changed-line coverage: BLOCKED. OpenCppCoverage produced no `.cov`
  files, several expected binaries were absent, one focused binary exited 1,
  and the profiled server did not become ready in 120 seconds. See
  `coverage-console.log` and `coverage/`. The required 80% result is unavailable.
- Canonical standard, cold-disabled, and hot-zero live runs: PASS. Oversized-both
  live row: BLOCKED by workload/budget evidence described above.

## Gate decision

Stage 39 cannot close. Coverage is unavailable, eight plan rows lack complete
evidence, and the required focused-target build is not clean. Developer results
review owns the next gate. Fix the stale focused targets and coverage production,
add canonical live workloads for both-filled, oversized-both, legacy, and the
unmapped topology/concurrency contracts, then return to QA.

Before citing this report, QA verified every cited relative artifact path with
`Test-Path`; the result is stored in `artifact-verification.txt`.
