# Stage 23 S03 focused rerun 20260621-03

Status: FAIL-bug-handoff
Date: 2026-06-21
Owner: QA
Scope: focused S03 rerun after Architect S03 fix re-review PASS.

## Preflight

- `build-cov/CMakeCache.txt`: `GGML_CUDA:BOOL=ON`.
- `llama-server --list-devices`: CUDA0 and CUDA1 are NVIDIA GeForce RTX 5060 Ti.
- Wrapper dry-run: PASS for S03.
- Dry-run flags include `--n-gpu-layers all` and `--fit off`.

## Harness correction

The first focused attempt, `stage23-s03-rerun-20260621-02`, exposed a harness
gate gap. The S03 child wrote `Server did not start`, but the wrapper returned
exit 0 and only logged missing metrics.

The wrapper now treats nonzero child exit or missing `metrics-before.txt` /
`metrics-after.txt` as row failure and exits 1. Stress/longrun startup waits now
use 300 seconds before readiness failure.

## Focused rerun

Run root:
`._test_output/stage23-s03-rerun-20260621-03`

Command shape:

- row: S03 only
- model: `._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf`
- cache cold path: `D:\tmp\cache-cold-stage23-s03-rerun-20260621-03`
- cache RAM: 512 MiB
- cold max: 512 MiB
- prompt evidence: redacted
- CUDA flags: `--n-gpu-layers all --fit off`

Result: FAIL.

Evidence:

- wrapper exit: 1
- row gate: `ok=False`
- child launch error: `Server did not start`
- missing files: `metrics-before.txt`, `metrics-after.txt`
- server log reaches CUDA device discovery and model load, then stops before
  health readiness.

## Direct isolation

Run root:
`._test_output/stage23-s03-direct-isolation-20260621-01`

Direct launch with the same S03 server flags exited before `/health`.

Result:

- `ready=False`
- `exited=True`
- `exitCode=-1073740791`

CUDA evidence:

- server log lists CUDA0 and CUDA1 RTX 5060 Ti.
- `nvidia-smi` records driver 595.79 and CUDA 13.2.
- GPU memory remains 0 MiB before exit, so the row never reaches model offload.

## Verdict

FAIL-bug-handoff.

This is not a valid S03 PASS. The matrix remains stopped at S03. Developer must
triage the startup crash before QA continues S03 or the remaining rows.
