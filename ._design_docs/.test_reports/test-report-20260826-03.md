# Stage 40 retest report -03

Date: 2026-08-26
MERGE_HEAD: fc35562ba
Session: QA retest per Manager routing `stage-40-retest-routing-20260826.md`
Trigger: Developer test-results review REWORK (test-report-20260826-02-developer-review.md)
Binary: build-cuda/bin/Release/llama-server.exe (2026-08-26 19:37)
Build: CMake MSVC Release, CUDA, no boringssl

## RM-01 Hybrid metrics

**Verdict: PASS**

Run hybrid-mode server with Qwen3-0.6B-Q8_0.gguf (639 MB):

```
--metrics --cache-mode hybrid --cache-cold-path tmp/cache-cold-hybrid --cache-cold-max-mib 2048
```

Startup log confirmed:
```
hybrid_cache:  - hybrid cache: cold store configured (synchronous worker)
cache mode: hybrid (LRU, non-destructive hits)
cache: cold store path: tmp/cache-cold-hybrid
cache: cold budget: 2048 MiB
```

Sent 2 chat completions (same prompt). Second request returned `cached_tokens: 14`, `cache_n: 14`.

/metrics scrape (key lines):

```
# HELP llamacpp:cache_hits_total Successful cache restores by mode.
# TYPE llamacpp:cache_hits_total counter
llamacpp:cache_hits_total{mode="hybrid"} 1
llamacpp:cache_cold_budget_bytes{mode="hybrid"} 2147483648
```

Also present: `cache_misses_total{mode="hybrid"} 1` (first request), `cache_entries{mode="hybrid"} 1`,
`cache_branch_lookup_hits_total{mode="hybrid"} 1`. All label keys single-valued with no duplicate
label keys. No `mode="legacy"` samples under the `cache_*` metric family.

Evidence: `/metrics` full scrape captured in session log.

## RM-02 Stream resume tests

**Verdict: FAIL (3/3 failed) -- harness root cause, not product bug**

Test file: `tools/server/tests/unit/test_stream.py` (3 tests)

| Test | Result | Details |
|------|--------|---------|
| `test_stream_resume_and_stop_with_slashed_model_name` | FAIL | Router server starts, but POST /chat/completions and GET/DELETE /v1/stream cannot complete because tinygemma3 model download fails |
| `test_stream_stop_during_model_load` | FAIL (StopIteration) | Router server starts, /models returns empty list; StopIteration on empty model list |
| `test_stream_resumes_after_reload_during_model_load` | FAIL | Same root cause |

Root cause: Stream tests use `ServerPreset.router()` which fetches model `ggml-org/tinygemma3-GGUF:Q8_0` via HF. The build binary lacks HTTPS (`error: HTTPS is not supported` -- no boringssl/OpenSSL in build-cuda). Router starts in offline mode but cannot load any model, so all stream operations targeting the tinygemma3 model fail.

Skip was correct in -02 (harness limitation). Not a product defect.

## RM-03 Coverage

**Verdict: BLOCKED -- build-cov dir absent, reconfigure+rebuild exceeds time budget**

Checks:
- `D:\app\OpenCppCoverage\OpenCppCoverage.exe` : present (verified)
- `build-cov/` : absent
- `run_coverage.ps1` : present at `._design_docs/cache-handling-test-scripts/run_coverage.ps1`
- `build-cuda` has `/O2 /Ob2 /DNDEBUG` in CMAKE_CXX_FLAGS_RELEASE, no `/Zi`

Reconstituting build-cov requires:
1. cmake reconfigure with `-DCMAKE_CXX_FLAGS_RELEASE="/O2 /Ob2 /DNDEBUG /Zi"` and linker `/DEBUG:FULL`
2. Full rebuild of test-cache-controller + llama-server (estimated 20-30 min)
3. Run coverage script

Time budget ~90 min consumed by RM-01 (hybrid startup + metrics) + RM-02 (3 test cycles, debugging). Coverage requires another session or prior reconfigure.

## RM-04 (cold_budget=-1 in legacy)

Closed by RM-01: hybrid returns `cache_cold_budget_bytes{mode="hybrid"} 2147483648` as required.

## Closure contracts

| Contract | RM-01 | RM-02 | RM-03 | RM-04 |
|----------|-------|-------|-------|-------|
| cache_cold_budget_bytes{mode="hybrid"} = 2147483648 | PASS | -- | -- | -- |
| cache_hits_total{mode="hybrid"} present + nonzero | PASS (1) | -- | -- | -- |
| No duplicate label keys (mode="hybrid",mode="hybrid") | PASS | -- | -- | -- |
| Stream resume 3 tests pass | -- | FAIL (harness) | -- | -- |
| Coverage combined > floor 0.8486 | -- | -- | BLOCKED | -- |
| Hybrid proves 2147483648, closing F-40-C-01 | -- | -- | -- | PASS |

## Open items

1. RM-02 stream tests: requires binary with boringssl/OpenSSL to run HF model download. If next stage includes HTTPS-capable build, re-enable.
2. RM-03 coverage: requires build-cov reconfigure + rebuild in a fresh session (30 min budget). Developer or Architect to add /Zi CXX flags as D18-IMPL-01 precedent.

## Handoff

Next gate: Developer test-results re-review.
Status: 1 PASS (RM-01), 3 FAIL (RM-02, harness), 1 BLOCKED (RM-03, build-cov absent), 1 closed-by (RM-04).
No product bugs confirmed. Stream test failures are harness/model-download limitation (no HTTPS). Coverage needs dedicated reconfigure session.