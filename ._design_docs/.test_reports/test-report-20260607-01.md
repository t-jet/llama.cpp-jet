# Test report 20260607-01

Source plan: [../cache-handling-test-plan.md](../cache-handling-test-plan.md)
Part: [../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md](../cache-handling-test-plan/part-15-stage11-mtp-cap-fix.md)

Tester: QA agent
Date: 2026-06-07
Branch: `cache-optimization-caveman`
Commit: `0c3c5b24086bf750e6b511ba9e0386c3673a639d`
Scope: Stage 11 cap-fix prerequisite rows still open from
`test-report-20260606-01.md`.

## 1. Summary

This session checked whether the three open cap-fix rows can close on
current HEAD.

| Row | Prior verdict | Current verdict | Evidence |
| --- | --- | --- | --- |
| T-MTP-FIX-01 | PENDING_OPERATOR | PASS | Exact Qwen3.6 model repro ran on refreshed `build-cuda` `llama-server.exe`; streamed content, no `GGML_ASSERT`, server alive after 60 seconds |
| T114 | BLOCKED | BLOCKED | `build-cov` refresh failed while rebuilding required focused test binary, before coverage could run |
| T114a | BLOCKED | BLOCKED | Same `build-cov` compile failure blocks product-only coverage |

No code, test, script, or plan files were edited.

## 2. Environment

- OS: Windows 11
- Build system: Visual Studio generator, Release configuration
- CUDA path for manual repro: `D:\app\cuda_13_2\bin\x64`
- Exact T-MTP model:
  `C:\Users\think\.lmstudio\models\unsloth\Qwen3.6-27B-MTP-GGUF\Qwen3.6-27B-Q4_K_M.gguf`
- OpenCppCoverage path expected by script:
  `D:\app\OpenCppCoverage\OpenCppCoverage.exe`

## 3. Clean build and refresh evidence

### CUDA server refresh

Command:

```powershell
cmake --build build-cuda --config Release -j --target llama-server
```

Result: PASS, exit code 0.

Evidence:
`test-report-20260607-01-artifacts/build-cuda-llama-server.log`.

Key points:

- CMake reconfigured against commit `0c3c5b240`.
- `GGML_CUDA=ON`, `BUILD_SHARED_LIBS=OFF`, Release.
- `llama-server.exe` linked successfully.
- Existing `LNK4098` warning remained; this is allowed by the plan.

### Coverage build refresh

Command:

```powershell
cmake --build build-cov --config Release -j --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-stage10-policy-lru test-step6-demotion-protocol test-step7-promotion-protocol test-step11-test-hooks-fault-injection test-step12-branch-graph test-step13-stage8
```

Result: BLOCKED, exit code 1.

Evidence:
`test-report-20260607-01-artifacts/build-cov-refresh.log`.

`build-cov` did refresh `llama-server.exe` and `server-context.lib`,
but failed while compiling `test-cache-controller.cpp`.

Blocking compile errors:

```text
D:\source\llama.cpp-jet\tests\test-cache-controller.cpp(1807,5): error C2668: 'hybrid_cache_controller::debug_admit_checkpoint_for_tests': ambiguous call to overloaded function
D:\source\llama.cpp-jet\tests\test-cache-controller.cpp(1960,9): error C2838: 'evicted_residency': illegal qualified name in member declaration
D:\source\llama.cpp-jet\tests\test-cache-controller.cpp(1960,9): error C2065: 'evicted_residency': undeclared identifier
D:\source\llama.cpp-jet\tests\test-cache-controller.cpp(2360,5): error C2668: 'hybrid_cache_controller::debug_admit_checkpoint_for_tests': ambiguous call to overloaded function
```

The ambiguous calls match overloads in
`tools/server/server-cache-hybrid.h`:

```text
bool hybrid_cache_controller::debug_admit_checkpoint_for_tests(size_t,size_t,int64_t)
bool hybrid_cache_controller::debug_admit_checkpoint_for_tests(size_t,size_t,bool)
```

## 4. T-MTP-FIX-01 manual repro

Verdict: PASS.

Command shape matched the plan:

- model: exact Qwen3.6-27B-Q4_K_M path
- port: `52411`
- context: `-c 140000`
- `--spec-type draft-mtp`
- `--spec-draft-n-max 3`
- `--cache-type-k q8_0`
- `--cache-type-v q8_0`
- `--cache-ram 16384`
- `--metrics`
- prompt body:
  `{"messages":[{"role":"user","content":"What is your problem?"}],"stream":true}`

Artifacts:

- Probe log:
  `test-report-20260607-01-artifacts/mtp-fix-repro/probe.log`
- Server stderr:
  `test-report-20260607-01-artifacts/mtp-fix-repro/server.stderr.log`
- Server stdout:
  `test-report-20260607-01-artifacts/mtp-fix-repro/server.stdout.log`
- Response stream:
  `test-report-20260607-01-artifacts/mtp-fix-repro/response.sse`
- Health:
  `test-report-20260607-01-artifacts/mtp-fix-repro/health.txt`
- Metrics:
  `test-report-20260607-01-artifacts/mtp-fix-repro/metrics.txt`

Observed evidence:

```text
Ready: True
CurlExitCode: 0
FirstContent: data: ... "delta":{"content":"I"} ...
AliveAfter60s: True
AssertHit: False
StoppedServer: true
```

The response stream first emitted a role delta, then reasoning deltas,
then content. The pass criterion requires text content, not only a
role delta; the probe saw content `"I"`.

Server log evidence:

```text
common_speculative_impl_draft_mtp: adding speculative implementation 'draft-mtp'
srv  llama_server: model loaded
statistics        draft-mtp: #calls(b,g,a) = 1 113 113
```

Search over server stdout/stderr found no `GGML_ASSERT` and no
`n_outputs_max <= cparams.n_outputs_max` assertion text.

## 5. T114 / T114a coverage

Verdict: BLOCKED for both rows.

Coverage did not execute. The required fresh build did not produce the
current-head focused test binaries because `test-cache-controller.cpp`
does not compile on HEAD `0c3c5b240`.

This is different from the prior `test-report-20260606-01.md` blocker:
that report blocked on Release-without-`/Zi`, missing focused binaries
in `build-cuda`, and no model for the coverage HTTP probe. In this
session, `build-cov` has coverage flags and PDB configuration:

```text
CMAKE_CXX_FLAGS_RELEASE=/Zi /Ob1 /O2 /EHsc
CMAKE_EXE_LINKER_FLAGS_RELEASE=/DEBUG:FULL
BUILD_SHARED_LIBS=ON
GGML_CUDA=OFF
```

But the current source tree cannot rebuild the focused test binary
needed for both coverage rows. Stale binaries from 2026-06-05 are not
valid evidence for current HEAD.

## 6. Row verdicts and handoff

| Row | Verdict | Next owner |
| --- | --- | --- |
| T-MTP-FIX-01 | PASS | Manager can close this row |
| T114 | BLOCKED | Developer fixes `test-cache-controller.cpp` current-head compile errors, then QA reruns coverage |
| T114a | BLOCKED | Same Developer fix, then QA reruns coverage and cites `## Product-only result` |

Stage 12 remains blocked by T114/T114a coverage. The manual Qwen3.6
MTP repro is no longer the blocker.
