# Stage 39 focused QA rerun

Date: 2026-07-17
Gate: D39-QA-04
Verdict: FAIL
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260717-04/`

## Scope

Manager Part 159 authorized one fresh canonical TP-39-03 node, followed only
after a full pass by four coverage blocks. The node reached `Assert-Tp3903`
and failed its authenticated terminal topology check. Fail-fast kept every
coverage block unopened. QA changed no product, driver, fixture, seam, plan,
threshold, or contract.

## Environment and build

- Session ran from `2026-07-17T11:41:46Z` through `2026-07-17T11:51:58Z`
  at HEAD `2c501427cdc318b50d50fcfe329e71f90a9a1e89`.
- Pre-run dirty state, tool versions, NVIDIA inventory, CMake flags, command
  lines, and UTC timestamps are preserved under the evidence root.
- Configuration was Release, CPU (`GGML_CUDA=OFF`), tests ON, and
  `LLAMA_STAGE39_LIVE_TEST_SEAM=ON`.
- The MTP fixture was 2,834,975,040 bytes, SHA-256
  `3874209241C9A397E2F62CD3F70F80FD2DFBF0DFCCB6838416BDB48A714E8630`.
  The 10,313-byte template SHA-256 was
  `47A7BDCBAE1F459E0A6F793D22E428A17F355C5A3100B77A14A601737A82F7F5`.

QA safely removed the verified workspace `build` and
`build-stage39-seam-on` roots. Fresh configure exited 0 in 9.746 seconds.
The first build launcher timed out while its MSBuild child continued normally;
no test ran. After the child finished, the identical authoritative build
command exited 0 in 9.507 seconds on the same fresh tree. It covered
`llama-server` and all 11 focused coverage targets. Binary and implementation
DLL timestamps were under two minutes old, and their hashes are in
`binary-inventory.json` and `server-impl-inventory.json`.

## Cheap gates

| Check | Result |
| --- | --- |
| PowerShell 7 parser API | PASS, exit 0, zero errors |
| Windows PowerShell 5 parser API | PASS, exit 0, zero errors |
| PowerShell 7 pure self-test | PASS, exit 0 |
| Windows PowerShell 5 pure self-test | PASS, exit 0 |

Evidence: `cheap-tests.json`, `cheap-test-commands.txt`, and four raw logs.

## TP-39-03

Exactly one canonical node ran on port 8195 with the approved fixture,
8192-token context, and two chats. It exited 1 after 157.360 seconds at driver
line 367:

```text
TP-39-03 authenticated terminal state mismatch
```

The node passed the exact speculative-init, checkpoint-config, checkpoint-
creation, prompt-capacity, discovery, proof-binding, and apply gates. Prompt
counts were 3,631 and 3,632 (7,263 total, 929-token margin). Peak sampled RSS
was 5,511,774,208 bytes; the cold root stayed below the 4-GiB cap. Apply was
consumed and pressure completed.

Production emitted the required `retained_cold/cold_room` and
`evicted/both_filled` decisions and one `commit/none` cold transaction. The
authenticated terminal proof retained two entries and two nodes with zero
entry, node, and pruning deltas. It did not retain the required LRU membership:
`lru_memberships` was 0 instead of 1, and `lru_membership_delta` was
`18446744073709551615` (signed `-1`) instead of 0. The server also warned that
eviction could not satisfy the payload budget: resident 254,351,576 bytes
against budget 187,834,252 bytes. This violates TP-39-03's retained-topology
and bounded-accounting contract.

| ID | Verdict | Reason |
| --- | --- | --- |
| TP-39-03 | FAIL | Authenticated terminal topology lost one LRU membership; accounting remained above budget. |

Evidence: `tp39-03-command.txt`, `tp39-03-result.json`,
`tp39-03-diagnosis.json`, `tp39-03-run.log`, and the complete raw/redacted
`TP-39-03/` bundle.

## Coverage

| Block | Classification |
| --- | --- |
| PowerShell 7 success | NOT RUN, required fail-fast stop |
| PowerShell 7 forced merge failure | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 success | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 forced merge failure | NOT RUN, required fail-fast stop |

Coverage remains BLOCKED: no fresh capture, merged XML, Markdown percentage,
or forced exit-23 evidence exists. `coverage-stop.json` verifies all four roots
are absent.

## Cleanup and handoff

Cleanup found zero `llama-server.exe` processes and zero port 8195 listeners.
Artifact hashes are in `artifact-verification.csv`.

Final classification: TP-39-03 `FAIL`; coverage `BLOCKED`; session `FAIL`.
Developer review should trace the LRU membership loss and over-budget terminal
accounting before another Manager-authorized rerun.
