# Stage 20 heavy tier execution report

Status: PASS-INFRASTRUCTURE
Date: 2026-06-18
Stage: 20 (Stage 17 Test Infrastructure Additions)
RunId: 20260618-130854
Branch: work-branch
Evidence path: `._test_output/stage20-heavy-real3/20260618-130854`

## Scope

This report records the post-closure heavy-tier run for TP-20-HV1 and
TP-20-HV2 on the Qwen3.6-27B-MTP fixture. The run used a chat-feasible
cap: 15 minutes, 8 requests, and a reduced context configuration.

The run validates infrastructure and Stage 16 parity. It does not cover
the full several-hour mixed exact-repeat, near-prefix, and new-turn
workload from the Stage 17 row.

## Configuration

| Parameter | Value |
| --- | --- |
| Wrapper | `._design_docs/cache-handling-test-scripts/kickoff-stage20-heavy.ps1` |
| Fixture | `._test_models/Qwen3.6-27B-MTP-GGUF/Qwen3.6-27B-Q4_K_M.gguf` |
| Port | 8830 |
| Cache mode | hybrid |
| Cache cold path | `D:\tmp\cache-cold-stage20-hv-real3` |
| Cache cold max MiB | 4096 |
| Cache RAM MiB | 2048 |
| Prompt evidence | redacted |
| Context size | 2048 |
| N parallel | 1 |
| Chat template | Path A, built-in template, no `--chat-template-file` |

## HV1 result

| Req | HTTP | cache_n | Duration |
| --- | --- | --- | --- |
| 1 | 200 | 0 | 23787 ms |
| 2 | 200 | 0 | 22229 ms |
| 3 | 200 | 0 | 23341 ms |
| 4 | 200 | 0 | 23225 ms |
| 5 | 200 | 0 | 23335 ms |
| 6 | 200 | 0 | 23007 ms |
| 7 | 200 | 0 | 22337 ms |
| 8 | 200 | 0 | 22751 ms |

Result: 8/8 requests completed. All had `cache_n=0` because the workload
used unique prompts only.

Cache state at end:

| Metric | Value |
| --- | --- |
| Cache entries | 8 |
| Total payload | 1825 MiB |
| Total cache size | 3022 MiB |
| Total tokens | 637 |
| Cache payload limit | 2048 MiB |
| Restore misses | 8, all `reason=exact_entry_absent`, `profile=checkpoint_dependent`, `pair_state=target_only` |

## HV2 comparison

| Metric | Stage 16 baseline | Stage 20 heavy run |
| --- | --- | --- |
| Fixture | Qwen3.6-27B-MTP | Qwen3.6-27B-MTP |
| Duration | About 10h 45m | 200s |
| Requests | 19+ | 8 |
| `cache_n=0` rate | 100% | 100% |
| Restore miss reason | `exact_entry_absent` | `exact_entry_absent` |
| Save behavior | Saves on slot release | Saves on slot release |
| Cold store | Not enabled | Enabled |

Verdict: Stage 20 heavy run matches Stage 16 baseline for the sampled
agentic workload class.

## Findings

F-20-EXEC-01: `cache_n=0` on all requests.
Result: non-blocking. Unique prompts do not produce exact restore hits.
Full workload coverage still needs exact repeats and near-prefix variants.

F-20-EXEC-02: 27B memory fit.
Result: resolved. The fixture starts and serves requests with `-c 2048`,
`-np 1`, and `--cache-ram 2048`. Working set was about 29 GB during
inference.

F-20-EXEC-03: invalid cache RAM flag.
Result: resolved. The wrapper now uses `--cache-ram`, not
`--cache-ram-mib`.

F-20-EXEC-04: PowerShell date formatting.
Result: resolved. The wrapper uses `.ToString('HH:mm:ss')` instead of
`Get-Date -DateTime`.

## Pass/fail summary

| Row | Result | Notes |
| --- | --- | --- |
| TP-20-HV1 | PASS-INFRASTRUCTURE | 8/8 requests OK, 27B fits memory, cache save/miss path works |
| TP-20-HV2 | PASS-COMPARISON | `cache_n=0` rate and restore miss reason match Stage 16 baseline |

## Handoff

Heavy infrastructure is ready. Full coverage still needs a multi-hour
mixed workload with exact repeats, near-prefix variants, and new prompts.
`kickoff-stage20-heavy-v2.ps1` is the current prototype for that follow-up.
