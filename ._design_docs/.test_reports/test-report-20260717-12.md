# Stage 39 D39-QA-12 execution report

Date: 2026-07-17
Verdict: FAIL
Authority: `cache-handling-phase39-implementation/part-195-manager-d39-qa12-rerun-gate-20260717.md`
Test run ID: `test-report-20260717-12`
Evidence root: `._test_output/test-report-20260717-12/`

## Scope and execution status

Authorized gate:

1. Fresh clean Release seam-ON build of the full D39-QA target set.
2. PowerShell 7 and Windows PowerShell 5 parser and pure tests for
   `stage39-two-layer-pressure.ps1`.
3. One bounded canonical TP-39-03 node.
4. Four coverage blocks from Parts 149 and 155 only after full
   `Assert-Tp3903` PASS.

QA stopped at canonical TP-39-03. Full `Assert-Tp3903` did not PASS, so coverage
was not run.

No product, driver, fixture, seam, workload, budget, threshold, commit, push, PR,
or reviewer-response change was made. The worktree was already dirty; QA added
only this report and `._test_output/test-report-20260717-12/` evidence.

## Plan or automation changes made

None. This was execution only.

## Commands and results

| Step | Command | Result | Evidence |
| --- | --- | --- | --- |
| Setup | `git rev-parse HEAD`; `git status --short`; tool versions; model and server hashes | PASS | `setup/`, `TP-39-03/tp3903-hashes.json` |
| Clean build root | Initial cleanup of `build-stage39-seam-on` was rejected before configure. Used fresh never-existing `build-stage39-seam-on-qa12`. | PASS | `setup/build-paths.txt` |
| Configure | `cmake -S . -B build-stage39-seam-on-qa12 -DLLAMA_STAGE39_LIVE_TEST_SEAM=ON -DLLAMA_BUILD_TESTS=ON -DCMAKE_BUILD_TYPE=Release` | PASS | `setup/cmake-configure-seam-on-qa12.log`, `setup/build-exits.txt` |
| Full target build | `cmake --build build-stage39-seam-on-qa12 --config Release --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-step6-demotion-protocol test-step7-promotion-protocol test-step11-test-hooks-fault-injection test-step12-branch-graph test-step13-stage8 -j 4`, then auxiliary targets `test-step1-state-machine test-step2-cold-store test-step3-4-cold-store-write-read test-step9-startup-validation test-stage10-policy-lru` | PASS | `setup/cmake-build-full-target-set-qa12.log`, `setup/cmake-build-auxiliary-targets-qa12.log`, `setup/full-target-binaries.json` |
| PowerShell 7 parser | `pwsh.exe -NoProfile -Command [scriptblock]::Create(...)` | PASS | `parser-pure/pwsh7-parser.*.log` |
| Windows PowerShell 5 parser | `powershell.exe -NoProfile -Command [scriptblock]::Create(...)` | PASS | `parser-pure/powershell5-parser.*.log` |
| PowerShell 7 pure | `pwsh.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -MetricValidationSelfTest` | PASS | `parser-pure/pwsh7-metric-selftest.*.log` |
| Windows PowerShell 5 pure | `powershell.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -MetricValidationSelfTest` | PASS | `parser-pure/powershell5-metric-selftest.*.log` |
| Canonical TP-39-03 | `pwsh.exe -NoProfile -File ._design_docs/cache-handling-test-scripts/stage39-two-layer-pressure.ps1 -ModelPath ._test_models/Qwen3.5-4B-MTP-GGUF/Qwen3.5-4B-Q4_K_M.gguf -LlamaServerPath build-stage39-seam-on-qa12/bin/Release/llama-server.exe -Scenario both-filled -Requests 2 -ContextSize 8192 -HotBudgetMiB 2048 -ColdBudgetMiB 2048 -Port 8295 -RunRoot ._test_output/test-report-20260717-12/TP-39-03-node` | FAIL | `TP-39-03/TP-39-03-node.exit.json`, `TP-39-03-node/` |
| Coverage block 1, PowerShell 7 success | Part 149 success block | NOT RUN | `coverage/coverage-stop.json` |
| Coverage block 2, PowerShell 7 forced merge failure | Part 149 forced block | NOT RUN | `coverage/coverage-stop.json` |
| Coverage block 3, Windows PowerShell 5 success | Part 149 success block | NOT RUN | `coverage/coverage-stop.json` |
| Coverage block 4, Windows PowerShell 5 forced merge failure | Part 149 forced block | NOT RUN | `coverage/coverage-stop.json` |

## Evidence and failures

Canonical TP-39-03 exited `1`, not timed out. The saved exception is:

```text
TP-39-03 exact-cold/checkpoint-evicted state mismatch
```

Failure location: `stage39-two-layer-pressure.ps1:1191`, in `Assert-Tp3903`.

The prior D39-QA-11 descriptor-delta failure did not reproduce. Descriptor and
residency metric deltas now match Architect Part 194:

```text
llamacpp:cache_evicted_payload_descriptors: before=0 after=1 delta=1
llamacpp:cache_payload_evictions_total: before=0 after=1 delta=1
llamacpp:cache_hot_payload_descriptors: before=4 after=2 delta=-2
llamacpp:cache_cold_payload_count: before=0 after=1 delta=1
```

The guarded apply also reached the expected production tuples:

```text
event=cache_two_layer_decision result=retained_cold reason=cold_room payload_id=1
event=cache_cold_transaction result=commit reason=none tx_id=1
event=cache_two_layer_decision result=evicted reason=both_filled payload_id=2
```

The failing conjunct is the final cold inventory assertion. The saved
`control-cold-files-after.csv` contains two rows:

```text
"1.cold","187834316","2026-07-17T17:01:46.7900458Z"
"ownership.claims","184","2026-07-17T17:01:47.7541803Z"
```

`Assert-Tp3903` requires `1.cold`, no checkpoint `.cold`, and total cold
inventory count `1`. The presence of `ownership.claims` makes the total count
`2`, so full `Assert-Tp3903` fails even though terminal proof reports exact
descriptor cold and checkpoint descriptor evicted.

Cleanup: port `8295` is closed and no `llama-server.exe` process from
`build-stage39-seam-on-qa12` remains. See `cleanup.json`.

## Final outcome

- Setup and clean seam-ON Release full target build: PASS.
- PowerShell 7 parser and pure tests: PASS.
- Windows PowerShell 5 parser and pure tests: PASS.
- Canonical TP-39-03: FAIL, full `Assert-Tp3903` did not PASS.
- Coverage: NOT RUN by gate.

Final counts: PASS 5, FAIL 1, NOT RUN 4, BLOCKED 0, SKIP 0.

Status: bug-handoff.

Next owner: Developer for D39-QA-12 test-results review and classification of
the TP-39-03 cold-inventory assertion versus `ownership.claims` artifact
handling. Coverage remains gated until a fresh TP-39-03 run reaches full
`Assert-Tp3903` PASS.
