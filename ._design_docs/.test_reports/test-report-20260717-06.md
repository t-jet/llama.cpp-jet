# Stage 39 focused QA rerun

Date: 2026-07-17
Gate: D39-QA-06
Verdict: FAIL
Plan: `../cache-handling-test-plan/part-43-stage39-two-layer-retention.md`
Evidence: `../../._test_output/test-report-20260717-06/`

## Scope

Manager Part 169 authorized one fresh canonical TP-39-03 node, followed only
after full `Assert-Tp3903` PASS by four coverage blocks. The node reached the
terminal proof check, but the driver rejected the expanded forbidden-effect
map before `Assert-Tp3903`. Fail-fast kept all coverage blocks unopened. QA
changed no product, driver, fixture, seam, plan, threshold, or contract.

## Environment and clean build

- Session ran at HEAD `2c501427cdc318b50d50fcfe329e71f90a9a1e89`.
- Pre-run dirty state, tool versions, NVIDIA inventory, CMake flags, command
  lines, timestamps, binary hashes, and fixture hashes are under the evidence
  root.
- Configuration was Release, CPU (`GGML_CUDA=OFF`), tests ON, and
  `LLAMA_STAGE39_LIVE_TEST_SEAM=ON`.
- The MTP fixture was 2,834,975,040 bytes, SHA-256
  `3874209241C9A397E2F62CD3F70F80FD2DFBF0DFCCB6838416BDB48A714E8630`.
  The 10,313-byte template SHA-256 was
  `47A7BDCBAE1F459E0A6F793D22E428A17F355C5A3100B77A14A601737A82F7F5`.

QA removed the verified workspace `build` and `build-stage39-seam-on` roots.
Fresh configure exited 0 in 10.653 seconds. The clean build exited 0 in
273.826 seconds and produced `llama-server` plus all 11 focused coverage
targets. Binary and implementation DLL timestamps and hashes are preserved in
`binary-inventory.json` and `server-impl-inventory.json`.

## Cheap gates

| Check | Result |
| --- | --- |
| PowerShell 7 parser API | PASS, exit 0, zero errors |
| Windows PowerShell 5 parser API | PASS, exit 0, zero errors |
| PowerShell 7 pure self-test | PASS, exit 0 |
| Windows PowerShell 5 pure self-test | PASS, exit 0 |

Evidence: `cheap-tests.json` and four raw logs.

## TP-39-03

Exactly one canonical node ran on port 8196 with the approved fixture,
8192-token context, and two chats. It exited 1 after 162.444 seconds at driver
line 439:

```text
TP-39-03 terminal forbidden effect mismatch
```

Prompt counts were 3,631 and 3,632, leaving a 929-token margin. Peak sampled
RSS was 5,501,685,760 bytes. The immutable exact cold file was 187,834,316
bytes, below the 4-GiB cap. Apply was consumed and pressure completed.

The authenticated proof contains the expected Stage 39 outcome: one
`retained_cold/cold_room`, one `evicted/both_filled`, one `commit/none`, zero
entry, node, and pruning deltas, signed LRU membership delta -1, exact retained
cold, and checkpoint eviction. `later_work_delta` is 0.

The driver failed its exact property-set guard before value comparison.
`effectExpected` contains 15 fields, while production now returns 18. The three
additional fields are `later_kind_work_delta`, `post_abort_pressure_delta`, and
`post_abort_diagnostic_delta`; each is 0. All 15 expected values also match.
These fields came from the reviewed Part 168 correction, but the canonical
driver contract was not updated to accept and verify them.

| ID | Verdict | Reason |
| --- | --- | --- |
| TP-39-03 | FAIL | Driver exact-property-set contract rejects three reviewed zero-valued component fields before `Assert-Tp3903`. |

Evidence: `tp39-03-command.txt`, `tp39-03-result.json`,
`tp39-03-diagnosis.json`, `tp39-03-run.log`, and the complete `TP-39-03/`
bundle.

## Coverage

| Block | Classification |
| --- | --- |
| PowerShell 7 success | NOT RUN, required fail-fast stop |
| PowerShell 7 forced merge failure | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 success | NOT RUN, required fail-fast stop |
| Windows PowerShell 5 forced merge failure | NOT RUN, required fail-fast stop |

Coverage remains BLOCKED. No fresh capture, merged XML, Markdown percentage,
or forced exit-23 evidence exists. `coverage-stop.json` verifies all four roots
are absent.

## Cleanup and handoff

Cleanup found zero `llama-server.exe` processes and zero port 8196 listeners.
Artifact hashes are in `artifact-verification.csv`.

Final classification: TP-39-03 `FAIL-driver-contract`; coverage `BLOCKED`;
session `FAIL`. Developer review must align the canonical terminal effect map
with the three reviewed component counters, keep exact field and zero-value
checks, and rerun the PowerShell 7/5 pure matrix before another Manager gate.
The saved live proof does not establish a product failure.
