# Test report 20260717-07

Stage: 39
Gate: D39-QA-07
Status: FAIL
Owner: QA

## Scope

D39-QA-07 authorizes one fresh clean Release seam-ON CPU build, PowerShell 7 and
Windows PowerShell 5 parser and pure checks for
`stage39-two-layer-pressure.ps1`, one bounded canonical TP-39-03 node, and only
after full `Assert-Tp3903` PASS, the four coverage blocks from Manager Parts 149
and 155.

No product, driver, fixture, seam, workload, budget, threshold, commit, push, PR,
or reviewer-response change is authorized.

## Evidence roots

- Durable report: `._design_docs/.test_reports/test-report-20260717-07.md`
- Artifact root: `._test_output/test-report-20260717-07/`

## Results

Clean Release seam-ON build failed before parser, pure, TP-39-03, or coverage
execution. Per D39-QA-07, execution stopped on this first verdict-fixing
failure.

## Environment

- Git HEAD: `2c501427cdc318b50d50fcfe329e71f90a9a1e89`
- Dirty state: recorded in `._test_output/test-report-20260717-07/preflight-env.json`
- PowerShell 7: `7.6.3`
- Windows PowerShell 5: `5.1.26100.8875`
- CMake: `4.3.2`
- Qwen3.5 MTP fixture present: `true`
- Qwen3 0.6B coverage fixture present: `true`
- OpenCppCoverage present: `true`
- Build dir used: `build-stage39-seam-on-20260717-07`

`build-stage39-seam-on` already existed and recursive removal was blocked by
tool policy, so this session used a fresh never-existing build directory for the
clean build.

## Commands

Configure:

```powershell
cmake -S . -B build-stage39-seam-on-20260717-07 -DCMAKE_BUILD_TYPE=Release -DLLAMA_STAGE39_LIVE_TEST_SEAM=ON -DLLAMA_BUILD_TESTS=ON -DGGML_CUDA=OFF
```

Result: `PASS`, exit `0`.

Build:

```powershell
cmake --build build-stage39-seam-on-20260717-07 --config Release --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-step6-demotion-protocol test-step7-promotion-protocol test-step11-test-hooks-fault-injection test-step12-branch-graph test-step13-stage8 -j 4
```

Result: `FAIL`, exit `1`.

## Evidence

- `._test_output/test-report-20260717-07/preflight-env.json`
- `._test_output/test-report-20260717-07/configure-command.txt`
- `._test_output/test-report-20260717-07/configure.log`
- `._test_output/test-report-20260717-07/configure-result.json`
- `._test_output/test-report-20260717-07/build-command.txt`
- `._test_output/test-report-20260717-07/build.log`
- `._test_output/test-report-20260717-07/build-result.json`
- `._test_output/test-report-20260717-07/build-error-summary.txt`
- `._test_output/test-report-20260717-07/session-verdict.json`

## Failure

`test-step7-promotion-protocol` does not compile against the current
`hybrid_cache_controller` API. First recorded compiler error:

```text
D:\source\llama.cpp-jet\tests\test-step7-promotion-protocol.cpp(212,10): error C2039: 'debug_start_io_worker_for_tests': is not a member of 'hybrid_cache_controller'
```

The same target then reports additional removed-member errors for
`process_completions`, `debug_stop_io_worker_for_tests`, and
`debug_set_io_worker_for_tests`. `build-error-summary.txt` records 36 matching
error lines.

## Not run

- PowerShell 7 parser check: not run because clean build failed.
- Windows PowerShell 5 parser check: not run because clean build failed.
- PowerShell 7 pure check: not run because clean build failed.
- Windows PowerShell 5 pure check: not run because clean build failed.
- Canonical TP-39-03 node: not run because clean build failed.
- Four coverage blocks: not run because TP-39-03 did not reach full
  `Assert-Tp3903` PASS.

## Outcome

- PASS: 1 setup step (`configure`)
- FAIL: 1 setup step (`clean Release seam-ON build`)
- BLOCKED: parser/pure, TP-39-03, and coverage are blocked by failed clean
  build
- Server cleanup: no server was started.

Handoff owner: Developer. Fix the stale `test-step7-promotion-protocol` calls to
removed `hybrid_cache_controller` async APIs, then request a fresh QA rerun under
a new Manager gate or explicit authorization.
