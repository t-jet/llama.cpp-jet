# Stage 10 QA execution report - 20260603-01

> Superseded by [test-report-20260603-03.md](test-report-20260603-03.md). This prior BLOCKED/FAIL report is retained for history; the current verdict lives in the new report.

Source plan:
[cache-handling-test-plan.md](../cache-handling-test-plan.md),
[part-12-stage10-observability-security-hardening.md](../cache-handling-test-plan/part-12-stage10-observability-security-hardening.md)

Tester: QA agent (rework continuation)
Date: 2026-06-03
Verdict: FAIL

## Scope

Rework of [test-report-20260602-02.md](test-report-20260602-02.md) Stage 10
tooling-unblock pass. This session did not rerun any test, build, or probe. It
synthesized the artifacts already produced under
`test-report-20260603-01-artifacts/` into the final report and paired fixes
file.

Scope: same as 20260602-02 (focused C++ correctness, Python startup, public
`/metrics` probe, operator documentation, coverage setup, benchmark setup,
security classification, Stage 4-9 regression) plus the rework reason: try
LLVM source-based coverage, install `k6` and `matplotlib`, and re-run the
public and k6 probes.

Run artifacts: [test-report-20260603-01-artifacts](test-report-20260603-01-artifacts/)

## Environment

- Commit: `d4ac73a3b` (read from `configure.log` line `-- ggml commit:
  d4ac73a3b`; consistent with the prior report).
- Worktree: same state as the prior session, with this report,
  `test-report-20260603-01-fixes.md`, and the artifacts dir added.
- vsdevcmd invocation (from `vsdevcmd-env.bat` and `setup-env.ps1`):

  ```text
  call "C:\Program Files\Microsoft Visual Studio\18\Community\Common7\Tools\vsdevcmd.bat" -arch=x64 -host_arch=x64
  ```

  LLVM toolchain was prepended on PATH:
  `C:\Program Files\Microsoft Visual Studio\18\Community\VC\Tools\Llvm\x64\bin`.

- Build: Release, MSVC 19.51 (`14.51.36231`), Ninja for the coverage tree,
  MSBuild for the PDB-merged coverage tree.
- OpenSSL: not found; HTTPS support disabled during configure (same as the
  prior session).
- Public model fixture: `D:\source\llama.cpp-jet\._test_models\Qwen3-0.6B-GGUF\Qwen3-0.6B-Q8_0.gguf`.
- Tool versions (from `setup-env.json`):

  | Tool | Path | Version |
  | --- | --- | --- |
  | cl.exe | `C:\Program Files\Microsoft Visual Studio\18\Community\VC\Tools\MSVC\14.51.36231\bin\HostX64\x64\cl.exe` | MSVC 19.51 |
  | cmake | `C:\Program Files\CMake\bin\cmake.exe` | (not captured, but reused from prior session) |
  | clang | `C:\Program Files\Microsoft Visual Studio\18\Community\VC\Tools\Llvm\x64\bin\clang.exe` | 20.1.8 |
  | clang-cl | `...\Llvm\x64\bin\clang-cl.exe` | 20.1.8 |
  | llvm-profdata | `...\Llvm\x64\bin\llvm-profdata.exe` | present |
  | llvm-cov | `...\Llvm\x64\bin\llvm-cov.exe` | present |
  | k6 | `D:\app\k6\k6.exe` | `k6.exe v2.0.0-rc1 (commit/fb943a6a80, go1.26.2, windows/amd64)` |
  | python | `D:\app\Python\python.exe` | 3.11.9 |
  | matplotlib | (now installed) | 3.10.9 |
  | OpenCppCoverage | `D:\app\OpenCppCoverage\OpenCppCoverage.exe` | (installed this session) |

## Commands run

Synthesized from the `.ps1` and `.bat` artifacts in the run directory.

```powershell
# Step 1 - setup environment and capture tool versions.
& "test-report-20260603-01-artifacts\setup-env.ps1"

# Step 2 - clean Release build (Ninja under the original tree).
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -G "Ninja"
cmake --build build --config Release --target llama-server test-cache-controller test-step10-metrics test-stage10-cold-store-hardening test-step6-demotion-protocol test-step7-promotion-protocol test-step11-test-hooks-fault-injection test-step12-branch-graph test-step13-stage8 -j 4

# Step 3 - coverage-attempt 1: clang-cl + LLVM source-based coverage.
& "test-report-20260603-01-artifacts\coverage-configure-and-build.ps1"  # failed link: __profc_ symbols unresolved under lld-link
& "test-report-20260603-01-artifacts\cov-build.bat"                     # ninja build with patched LINK_LIBRARIES, also failed link
& "test-report-20260603-01-artifacts\cov-debug-build.bat"               # debug-symbols rebuild, also failed link
& "test-report-20260603-01-artifacts\cov-debug-configure.bat"           # configure with /DEBUG, also failed link
& "test-report-20260603-01-artifacts\cov-debug2-configure.bat"          # MSBuild-style retry, also failed

# Step 4 - coverage-attempt 2: rebuild a separate tree with MSVC and
#   /DEBUG for OpenCppCoverage, then run OpenCppCoverage on the regular
#   MSVC build's focused test binaries.
& "test-report-20260603-01-artifacts\cov-configure.bat"
& "test-report-20260603-01-artifacts\cov-pdb-build.bat"                # MSBuild tree build-cov, succeeded for all 8 focused exes
& "test-report-20260603-01-artifacts\cov-msbuild-build.bat"             # same tree, /DEBUG:FULL
& "test-report-20260603-01-artifacts\coverage-opencppcoverage.ps1"      # first OpenCppCoverage run (no PDBs)
& "test-report-20260603-01-artifacts\coverage-opencppcoverage2.ps1"     # second OpenCppCoverage run (with PDBs)
& "test-report-20260603-01-artifacts\coverage-aggregate.ps1"            # produce coverage-summary.md

# Step 5 - public HTTP /metrics probe.
& "test-report-20260603-01-artifacts\public-probe.ps1"

# Step 6 - k6 run.
& "test-report-20260603-01-artifacts\k6-run.ps1"
```

Note: this QA session did not rerun the focused `ctest` invocation. The
OpenCppCoverage per-test runs (Step 4) executed all 8 focused test binaries
under instrumentation and recorded their exit codes; see Coverage section.

## Clean build evidence

PASS for the original Release tree (`build\bin\Release`). The build log
shows MSVC warnings (C4244 in `llama-quant.cpp` and `llama-graph.cpp`; C4244
in `llama-graph.cpp` from `memory`) and one transient
`error C1083: Cannot open compiler generated file:
'D:\source\llama.cpp-jet\build\src\llama.dir\Release\unicode-data.obj':
Permission denied` against `src/unicode-data.cpp`. The obj was eventually
written and the build continued; the 8 focused test binaries are present in
`build\bin\Release` with timestamps between 13:19:22 and 13:19:44
(today, 2026-06-03):

- `test-cache-controller.exe` 13:19:22
- `test-step10-metrics.exe` 13:19:26
- `test-stage10-cold-store-hardening.exe` 13:19:29
- `test-step6-demotion-protocol.exe` 13:19:33
- `test-step7-promotion-protocol.exe` 13:19:36
- `test-step11-test-hooks-fault-injection.exe` 13:19:40
- `test-step12-branch-graph.exe` 13:19:41
- `test-step13-stage8.exe` 13:19:44

`llama-server.exe` is also present (`build\log` shows no missing target).

The separate `build-cov` tree (MSVC + `/DEBUG:FULL`) built the same 8
focused binaries cleanly with timestamps between 14:04:49 and 14:05:14.
This tree exists to host the per-test OpenCppCoverage runs.

## Correctness evidence

PASS for the 8 focused C++ tests based on the OpenCppCoverage per-test runs
in `coverage/run-*.log`. Each test was launched under OpenCppCoverage and
exited with code 0; OpenCppCoverage wrote a non-empty Cobertura XML for
each:

| Test | OpenCppCoverage exit | Cobertura size |
| --- | --- | --- |
| test-cache-controller | 0 | 414926 bytes |
| test-step10-metrics | 0 | 388099 bytes |
| test-stage10-cold-store-hardening | 0 | 387757 bytes |
| test-step6-demotion-protocol | 0 | 393115 bytes |
| test-step7-promotion-protocol | 0 | 396323 bytes |
| test-step11-test-hooks-fault-injection | 0 | 393331 bytes |
| test-step12-branch-graph | 0 | 31641 bytes |
| test-step13-stage8 | 0 | 400765 bytes |

Limitation: this session did not rerun the explicit
`ctest --test-dir build -C Release -R "test-(cache-controller|step10-metrics|stage10-cold-store-hardening|step6-demotion-protocol|step7-promotion-protocol|step11-test-hooks-fault-injection|step12-branch-graph|step13-stage8)"`
command from the prior report. The 8/8 pass outcome is taken from the
per-test exit codes under OpenCppCoverage and from the existence of all 8
binaries in the clean Release tree. The Python startup
`tools/server/tests/unit/test_cache_modes.py` row is not rerun in this
session and inherits the prior PASS classification from
`test-report-20260602-02.md` (the cache-modes Python file is unchanged and
the new artifacts do not contradict the prior classification).

Public `/metrics` probe (from `public-probe.ps1`):

- Server reached `/health` in 180 s (`public-server.health` shows
  `health_ready: True at 2026-06-03T13:23:28`).
- `health.json` body: `{"status":"ok"}`.
- First `/completion` (`completion-1.json`): `timings.cache_n=0`,
  `tokens_cached=12`.
- Second `/completion` (`completion-2.json`): `timings.cache_n=4`,
  `tokens_cached=12`. The cache hit row is real.
- `/metrics` after second completion (`metrics-stage10-excerpt.txt`) shows
  the bounded Stage 10 rows including:
  `cache_exact_blob_restores_total{...,result="success",reason="none"} 1`
  and the remaining Stage 10 rows at zero-valued bounded-shape placeholders.
- `/metrics` checkpoint rows (`metrics-checkpoint-excerpt.txt`) all show 0
  with the bounded-shape placeholders, no checkpoint descriptor ever
  admitted by the local Qwen3 0.6B fixture.

k6 probe (from `k6-run.ps1`):

- `k6 version` resolved; `k6_exit: 0`; `k6-run-complete` written.
- 20 iterations of `/health` and `/metrics` (1 VU), all 200, 0% failures,
  `p(95)` HTTP duration 1.07 ms.
- `k6-stage10-excerpt.txt` is the after-run `/metrics` excerpt (same rows
  as the public-probe excerpt). The standard k6 distribution does not
  include the SSE extension that `tools/server/bench/script.js` needs, so
  the k6 run is a connectivity and `health/metrics` shape probe, not the
  exact-hit/checkpoint-hit/cold-transition/end-to-end savings benchmarks.

## Coverage and benchmarks

T114 - Hybrid path coverage: **FAIL** (was BLOCKED in 20260602-02).

- Tool: OpenCppCoverage (GCOV-style fallback). LLVM source-based coverage
  path was attempted first and failed at link time; see
  `cov-configure.log` (clang-cl 20.1.8 configured), `cov-build.log` (lld-link
  unresolved `__profc_` symbols for MSVC ABI template instantiations),
  `cov-debug-build.log` and `cov-msbuild-build.log` (same link failure with
  `/DEBUG:FULL`). The same `error C2668:
  hybrid_cache_controller::debug_admit_checkpoint_for_tests: ambiguous call
  to overloaded function` at `tests/test-cache-controller.cpp(1804)` is the
  only MSVC compile error that survived across the coverage rebuilds; the
  test-cache-controller.cpp translation unit only links under MSVC, not
  clang-cl, on this host.
- Combined line rate: `0.2127` (9785 / 45999), well below the 80%
  threshold (`coverage-summary.md`).
- Per-file highlights from the per-test run coverage: every focused test
  itself is near 100% covered. The hybrid sources themselves are low
  because the focused tests do not drive all hybrid paths: `server-cache-hybrid.cpp`
  0.22-0.36, `server-cache-store-cold.cpp` 0.13-0.76, `server-context.cpp`
  0.00-0.01.

T115 - Coverage denominator and exclusions: **FAIL** (was BLOCKED).

- Tool: listed (`coverage-summary.md` first paragraph: `Source: OpenCppCoverage`).
- Command family: listed (`coverage-opencppcoverage.ps1`,
  `coverage-opencppcoverage2.ps1`, `coverage-aggregate.ps1`).
- Included files: the 8 focused test sources plus the 12 hybrid sources
  listed in `coverage-aggregate.ps1` (`hybrid denominator` array).
- Excluded files: `D:\app\`, `D:\source\llama.cpp-jet\build\`,
  `D:\source\llama.cpp-jet\ggml\`, `D:\source\llama.cpp-jet\common\`,
  `D:\source\llama.cpp-jet\src\`, `D:\source\llama.cpp-jet\vendor\`,
  `D:\source\llama.cpp-jet\tools\mtmd\` (and `build-cov\` in the second
  script).
- Percentage: combined `0.2127` listed.
- Branch totals: not produced. OpenCppCoverage does not emit branch
  coverage; this is a documented fallback limitation. The plan calls for
  "branch totals when available"; "when available" leaves room for the
  fallback to be reported without them, but the row also calls for an
  uncovered-hybrid-paths list above risk threshold, which is not provided
  here beyond the per-file line rates.
- Fallback reason: LLVM source-based coverage failed at link
  (MSVC ABI / template instantiation differences in `clang_rt.profile`).
- Reason for FAIL: T114 already failed the 80% threshold. T115's
  denominator report is produced, but the uncovered-hybrid-paths list
  above risk threshold is not, and the branch-totals absence is the
  main blocker to treating this row as PASS.

T116 - Benchmark evidence: **BLOCKED** (unchanged from 20260602-02).

- `k6` is now installed and runs (connectivity probe only, see above).
- `matplotlib` 3.10.9 is now installed (per `setup-env.json`).
- `python tools/server/bench/bench.py --help` was previously blocked by
  `ModuleNotFoundError: No module named 'matplotlib'`; that module is now
  installed, but this session did not actually run `bench.py --help` or
  the bench script. No bench output is in the artifacts.
- The k6 run only proves `/health` and `/metrics` HTTP correctness and
  latency. It does not produce exact-hit, checkpoint-hit, cold-transition,
  or end-to-end savings benchmark measurements. Those remain
  not-produced.
- Reason for BLOCKED (not FAIL): the k6 and matplotlib tools are
  installed; the bench scenario and `bench.py` were not run. This is a
  partial-unblock, not a failure of the bench toolchain.

T117 - Benchmark correctness gate: **BLOCKED** (unchanged).

- Depends on T116. With no exact-hit/checkpoint-hit/cold-transition
  measurements, there are no benchmark rows to gate. Correctness of the
  probes (`/health` and `/metrics` 200, 0% failure) is established by the
  k6 run, but the bench scenarios that the plan requires are not run.

T121 - Stage 9 regression / model-backed checkpoint `/metrics`: **BLOCKED**.

- Public probe ran on `Qwen3-0.6B-Q8_0.gguf` (plain transformer).
- `metrics-checkpoint-excerpt.txt` shows the four
  `cache_checkpoint_*` rows all at 0 with bounded-shape placeholders, no
  admission.
- Reason: the local fixture is plain-transformer, not checkpoint-dependent.
  No checkpoint descriptor can be admitted; the public rows are present
  but unexercised. The same blocker as 20260602-02; the rework did not
  introduce a checkpoint-capable fixture.

## Documentation and security

PASS. This session did not modify any operator documentation. The docs
checked in 20260602-02 are still valid: `tools/server/hybrid-cache.md`,
`tools/server/README.md`, `tools/server/README-dev.md`,
`tools/server/bench/README.md`, `tools/server/tests/README.md`. OWASP
classification is inherited unchanged from 20260602-02:

| Category | Result | Evidence |
| --- | --- | --- |
| A01 Broken access control | PASS | Cold-store root containment and no public marker surface; focused hardening tests passed. |
| A03 Injection | PASS | Focused exporter test covers quote and newline escaping; public rows used bounded labels. |
| A04 Insecure design | PASS | Impossible budgets, pressure, fallback, and integrity cases covered by focused tests. |
| A05 Security misconfig | PASS | Startup and cold-root validation covered by focused and Python tests; HTTPS absence is outside cache acceptance. |
| A08 Software and data integrity | PASS | Descriptor and cold-file integrity fallback paths covered by focused controller and fault-injection tests. |
| A09 Logging and monitoring | PASS | Structured diagnostic rows use bounded labels and docs prohibit prompt, marker, path, checksum, payload, and serialized-state leakage. |

## Row outcomes

| ID | Result | Evidence |
| --- | --- | --- |
| T100 | PASS | Public `/metrics` exact restore and zero-valued Stage 10 rows; focused exporter labels in `test-step10-metrics`. |
| T101 | PASS | `test-step10-metrics` Prometheus escaping coverage. |
| T102 | PASS | Focused Stage 10 rows and operator docs show bounded diagnostics and no raw prompt/path/payload fields. |
| T103 | PASS | `test-stage10-cold-store-hardening`. |
| T104 | PASS | `test-stage10-cold-store-hardening` and `test-step11-test-hooks-fault-injection`. |
| T105 | PASS | `test-stage10-cold-store-hardening`; Python startup checks (inherited). |
| T106 | PASS | `test-stage10-cold-store-hardening`, demotion, promotion, branch graph, and Stage 8 focused tests. |
| T107 | PASS | Deterministic focused stress seams in Stage 10 hardening and protocol tests. |
| T108 | PASS | Operator docs checked (inherited from 20260602-02). |
| T109 | PASS | Focused metric escaping and bounded diagnostic evidence. |
| T110 | SKIP | No hybrid cache request-marker surface is enabled in this repo state. |
| T111 | PASS | `test-cache-controller`, demotion, promotion, and fault-injection tests. |
| T112 | PASS | Focused unsupported-shape and startup validation evidence; Python startup checks (inherited). |
| T113 | PASS | OWASP table above (inherited). |
| T114 | FAIL | OpenCppCoverage combined line rate `0.2127`; 80% threshold not met (`coverage-summary.md`). LLVM source-based path blocked at link. |
| T115 | FAIL | Denominator, files, exclusions, percentage, fallback reason all listed; branch totals absent (OpenCppCoverage limitation); uncovered-hybrid-paths-above-risk-threshold list not produced. |
| T116 | BLOCKED | k6 and matplotlib now installed; k6 ran connectivity probe only. No exact-hit/checkpoint-hit/cold-transition/end-to-end savings measurements produced. `bench.py` not run. |
| T117 | BLOCKED | No benchmark rows ran. Correctness of `/health` and `/metrics` 200, 0% failure established by k6; the bench scenarios are not run. |
| T118 | PASS | `test-cache-controller` plus `test-step10-metrics`. |
| T119 | PASS | `test-step6-demotion-protocol`, `test-step7-promotion-protocol`, and `test-step11-test-hooks-fault-injection`. |
| T120 | PASS | `test-step12-branch-graph` and `test-step13-stage8`. |
| T121 | BLOCKED | Public checkpoint `/metrics` rows present but at zero-valued bounded-shape placeholders; the local Qwen3 0.6B fixture is plain-transformer and cannot admit a checkpoint descriptor. |

Counts:

- PASS: 16
- FAIL: 2
- SKIP: 1
- BLOCKED: 3

## Product bugs

None found in this execution session. The clang-cl / `clang_rt.profile` link
failures and the MSVC `error C2668` against
`hybrid_cache_controller::debug_admit_checkpoint_for_tests` are toolchain
issues that block the LLVM source-based coverage path; the coverage work
product is replaced by OpenCppCoverage on the MSVC tree. Neither issue
indicates a product bug in cache behavior.

## Blockers

- T114 coverage below 80% threshold; OpenCppCoverage line rate `0.2127`.
- T115 branch totals absent; uncovered-hybrid-paths-above-risk-threshold
  list absent.
- T116/T117 benchmark scenarios (`exact-hit`, `checkpoint-hit`,
  `cold-transition`, `end-to-end savings`) not run; the k6 run was a
  connectivity probe only.
- T121 model-backed public checkpoint `/metrics` requires a
  checkpoint-capable fixture; the local plain-transformer fixture cannot
  produce the precondition.

## Handoff

Status: FAIL for Stage 10 closure on T114 and T115. The blocker set has
narrowed from 5 BLOCKED in 20260602-02 to 2 FAIL and 3 BLOCKED here. The
remaining work is in the hands of Developer or Manager: the 80% coverage
threshold is not met and the bench scenarios were not exercised.

Next gate: Developer test-results review. Report to review:
[test-report-20260603-01.md](test-report-20260603-01.md) plus
[test-report-20260603-01-fixes.md](test-report-20260603-01-fixes.md).
