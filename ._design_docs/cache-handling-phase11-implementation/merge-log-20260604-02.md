# Stage 11 merge log 20260604-02 (canonical real merge)

Stage: 11 (upstream merge integration).
Design baseline:
[cache-handling-phase11-design.md](../cache-handling-phase11-design.md),
Parts 01 through 06, plus the Architect design review in
[part-07-design-review-gate-01.md](../cache-handling-phase11-design/part-07-design-review-gate-01.md)
(verdict PASS, 7 of 7 author decisions accepted).
Date opened: 2026-06-04.
Date closed: 2026-06-05 (canonical; re-closure pending Manager
gate on commit `6e3aa045c`).
Owner: Stage 11 Developer.
Reviewer: Stage 11 Architect.
Approver: Stage 11 Manager.

This is the canonical merge log for the real two-parent merge
`e0f3f868b` against `upstream_master`. The prior merge log
[merge-log-20260604-01.md](merge-log-20260604-01.md) is
INVALIDATED 2026-06-05 by the rework loop (D7); the merge
commit `72cfbcd44` it recorded was a fabricated single-parent
commit, not a real `git merge`. The full correction record is
in
[part-15-real-merge-correction.md](part-15-real-merge-correction.md).

## Cover and metadata

- Source upstream reference: local `upstream_master` tracking
  branch (Manager plan-change decision 2026-06-04).
- Target integration branch: `cache-optimization-stage11-merge`
  at SHA `3b9ed9712` (the post-Stage-10 tip with 5 Stage 11
  commits on top of the fabricated `72cfbcd44`).
- Merge base SHA: `40d5358d3c730b81729ba81cd5c44ed596d02510`.
- `upstream_master` tip SHA:
  `6ddc9430b145f61a0c1733b9d79c99c0ebdedf50`.
- In-range commit count: 225 (range
  `cache-optimization-stage11-merge..upstream_master`).
- Merge commit SHA: `e0f3f868b03a6384912695e718b3f9ef8393be44`.
- Merge parents: `3b9ed9712` (cache-optimization tip, first
  parent) and `6ddc9430b` (upstream_master tip, second
  parent). Verified by
  `git rev-list --parents -n 1 e0f3f868b`.
- Merge strategy: `--no-ff` per design Part 3.
- Path chosen: A (merge on top of the current tip). The 5
  Stage 11 commits (`8682e209b`, `031309040`, `023fe967d`,
  `57faa3e91`, `3b9ed9712`) sat on top of the fabricated
  `72cfbcd44`. Path A kept those commits and produced a true
  two-parent merge. Path B (reset and rebase) was not needed
  because the post-Stage-10 chain had no textual conflicts
  with the upstream content.
- D1 (fork point SHA): approved by Manager 2026-06-04.
- D6 (5-commit staleness gap): resolved by Manager 2026-06-04;
  proceed with the 5-commit gap and re-sync `upstream_master`
  after the merge closes.
- D7 (fabricated merge `72cfbcd44`): raised 2026-06-04. The
  Manager rejected the fabricated single-parent commit and
  authorized the real two-parent merge at `e0f3f868b`.
- D8 (build-defect duplicates): raised 2026-06-04. The real
  merge surfaced two semantic duplicates in
  `tools/server/server-context.cpp` that the 3-way merge did
  not flag. The Manager authorized the minimum-diff removal
  of both. Fix commit `602f3e3f0`. Record in
  [part-16-build-defect-semantic-duplicates.md](part-16-build-defect-semantic-duplicates.md).
- D9 (T114a soft-closure rejection): raised 2026-06-05. The
  rework loop lifted T114a to 0.7035 on the 70% floor via
  commit `6e3aa045c`. The 2026-06-04 soft-closure as a
  tooling limitation is rejected. Record in
  [part-17-t114a-product-only-coverage.md](part-17-t114a-product-only-coverage.md).
- Architect pre-merge review verdict: PASS, recorded in
  [part-07-architect-pre-merge-review-gate-01.md](part-07-architect-pre-merge-review-gate-01.md).
- `run_coverage.ps1` T114a prerequisite review verdict: PASS,
  recorded in
  [part-06-architect-prereq-script-review-gate-01.md](part-06-architect-prereq-script-review-gate-01.md).
- Real-merge correction record:
  [part-15-real-merge-correction.md](part-15-real-merge-correction.md).
- Authoritative test report:
  [../../.test_reports/test-report-20260604-06.md](../../.test_reports/test-report-20260604-06.md).

## Conflicts encountered

The 3-way merge on the real `e0f3f868b` surfaced 3 textual
conflicts and 1 semantic conflict in
`tools/server/server-context.cpp` (a 4th conflict that the
fabricated `72cfbcd44` log recorded as `src/llama-context.cpp`
was a phantom: the renamed API symbols `pre_norm` -> `nextn`
in `src/llama-context.cpp` resolved cleanly via auto-merge on
the real merge). The full per-conflict table:

| # | File | Conflict type | Resolution | Local adjustment | Prior-stage contract preserved |
| --- | --- | --- | --- | --- | --- |
| 1 | `common/fit.h` | Both parents add new function declarations. Upstream adds `common_get_device_memory_data`; Stage 11 adds `common_get_mtp_ctx_memory_overhead`. | Take upstream content. Re-add Stage 11 `common_get_mtp_ctx_memory_overhead` declaration after `common_fit_params`. Drop the duplicate `#include <vector>` that Stage 11 had introduced (single upstream include is enough). | 1 region (5 lines). | Stage 6 cold store memory accounting, Stage 9 MTP path. Both function declarations are present at HEAD. |
| 2 | `common/speculative.cpp` | API rename `pre_norm` -> `nextn` (upstream commit `166fe294`). Local has the old `pre_norm` API and the Stage 9 fix that flips `llama_set_embeddings_nextn(ctx_tgt, true, true)` `masked` flag from `false` to `true` for both `ctx_tgt` and `ctx_dft`. | Take the upstream API name (`nextn`). Keep the local Stage 9 fix's `masked=true` value for both contexts. Update the local comment to use the new API name. | 1 region (3 lines). The local comment was updated from "pre-norm rows" to "nextn rows". | Architecture Part 6 (speculative mode namespace isolation), Stage 5 (draft runtime mode identity). The MTP path remains a distinct mode identity. |
| 3 | `ggml/src/ggml-backend-meta.cpp` | Upstream removal of local-fork pre-merge code that referenced non-existent struct fields. The pre-merge local code in `ggml_backend_meta_buffer_reset` referenced `buf_ctx->simple_tensors` and `buf_ctx->buf_configs` fields that do not exist on `ggml_backend_meta_buffer_context` in the local struct. | Take the upstream reset path verbatim. The local's `split_state_cache.clear()` defensive intent is documented in a code comment as a follow-up item for a future Stage 11 cycle. | 1 region (10 lines added, 8 lines removed). Comment-only adjustment. | Stage 6 cold store async I/O reset semantics. The local intent is recorded as a follow-up item in a code comment. |
| 4 | `tools/server/server-context.cpp` (semantic) | Both parents add a `const bool near_prompt_end` declaration in the same loop body. Upstream commit `e2ef8fe42` ("server: fix checkpoints creation", PR #22929) and the cache-optimization chain both add the same line. The 3-way merge did not flag this; both declarations ended up at adjacent lines 3651 and 3653. A second pre-existing duplicate `server_n_outputs_max` (upstream commits `de6f727aa` PR #23861 and `5dcb71166` PR #23988) was masked by the first error. | Both duplicates removed. Fix commit `602f3e3f0`. The first duplicate was the `near_prompt_end` redefinition at line 3653. The second duplicate was the `server_n_outputs_max` byte-identical body at lines 219-232. | 17 lines removed (1 `near_prompt_end` declaration, 1 orphan blank, 13 `server_n_outputs_max` body, 1 blank, 1 trailing blank). 0 inserted. | Stage 9 checkpoint integration, Stage 10 metrics. The local copy of `server_n_outputs_max` was kept (line 204, brought in from the cache-optimization side via the merge commit). The byte-identical `near_prompt_end` declaration was kept at line 3636 (cache-optimization side). |

Mechanical rename of `pre_norm` -> `nextn` applied to the
non-conflict regions of the following files to keep the
rename consistent across the codebase:
`common/speculative.h` (1 reference), `src/llama-cparams.h`
(2 references), `src/llama-ext.h` (3 references),
`src/llama-graph.cpp` (2 references), `src/llama-graph.h`
(2 references), `src/models/qwen35.cpp` (2 references),
`src/models/qwen35moe.cpp` (2 references). The auto-merge
handled the rest of the rename scope. The fix-report docs
(`fix-qwen-eval-2`, `fix-qwen-summary`) reference the old
API name in 6+ places; these are deferred to a follow-up
per pre-merge report R3.

The full per-duplicate table for the build-defect fix is in
[part-16-build-defect-semantic-duplicates.md](part-16-build-defect-semantic-duplicates.md).

## Deferred upstream commits

The local `upstream_master` is 5 commits behind the actual
`ggml-org/llama.cpp` `master` tip
`4586479852e40f0ce8d4a38b8ec3b98c042d5a04` as of 2026-06-04.
The 5 missing commits:

| # | SHA (short) | PR | Subject | Out-of-scope reason |
| --- | --- | --- | --- | --- |
| 1 | `e3ba22d6` | #24091 | fix(mtmd): handle Gemma 4 audio projector embedding size | `tools/mtmd/clip.cpp` outside file-glob groups in design Part 2. |
| 2 | `7ac5a422` | #24053 | cmake: skip cvector-generator and export-lora when CPU backend is disabled | `tools/CMakeLists.txt` outside file-glob groups. |
| 3 | `00664040` | #24089 | server: add header to `tools/server/server-http.h` | Adds `#include <unordered_map>` (already transitively available). Null functional impact. |
| 4 | `4d742877` | #23974 | build: use umbrella Headers directory for XCFramework module map | `build-xcframework.sh` outside file-glob groups. |
| 5 | `45864798` | #24065 | webui: fix tool selector toggle/counter | Touches `tools/ui/...` and one `#include` line in `server-http.h`. Null functional impact. |

Follow-up owner: Stage 11 Developer (the next Stage 11
cycle that reopens the upstream merge activity).
Target cycle: the next Stage 11 cycle that opens after
the current merge closes.
Lift condition: `upstream_master` tip is at least 25
commits behind actual upstream `master`, or the Manager
opens a new Stage 11 cycle for any other reason.
Action: fetch upstream `master` into `upstream_master`
with a fast-forward, re-run the pre-merge analysis on the
refreshed range, and surface a new D6 decision if the new
range exposes a contract gap the current range did not.

## Reworks opened

0 rework candidates. The pre-merge triage recorded 0
REWORK-REQUIRED commits. The merge did not surface any new
contract-breaking change that the pre-merge triage missed.
The local's `split_state_cache.clear()` defensive intent
in the `ggml_backend_meta_buffer_reset` function is
documented in a code comment as a follow-up item for a
future Stage 11 cycle.

## Regression evidence

The minimum regression scope per the Architect pre-merge
review: build, ctest, focused ctest, HTTP probe T121, and
coverage T114/T114a. The full re-verification on
`6e3aa045c` is in the authoritative test report
[../../.test_reports/test-report-20260604-06.md](../../.test_reports/test-report-20260604-06.md).
The prior BLOCKED report on the real merge `e0f3f868b`
is at
[../../.test_reports/test-report-20260604-05.md](../../.test_reports/test-report-20260604-05.md).

| Row | Verdict on `6e3aa045c` | Citation |
| --- | --- | --- |
| Build (incremental) | PASS | `test-report-20260604-06.md` "Build (incremental, verification only)" section |
| ctest (9 focused tests) | PASS (9/9 in 10.7 s) | `ctest-20260605-02.log` |
| T114 (Combined) | PASS at 0.8553 (5436/6356) | `coverage-run-20260605-03/coverage-report.md` |
| T114a (Product-only) | PASS at 0.7035 (2090/2971) | same report |
| T115 (per-file aggregation) | PASS, 19 rows | same report |
| T121 (public checkpoint admission) | PASS, metric value 1 non-zero | `t121-probe-metrics-after-20260605-06.txt` |
| pytest | PASS by reference | `pytest-20260604-06.log` |
| HTTP probe (T121) | PASS | `t121-probe-metrics-after-20260605-06.txt` |
| OWASP | PASS by reference | `test-report-20260604-06.md` |
| k6 | PASS by reference | `k6-20260605-01.log` |
| Stage 4-9 regression | PASS (ctest covers it) | `ctest-20260605-02.log` |

PASS: 10 of 10 closure rows, BLOCKED: 0, FAIL: 0, SKIP: 0.

## Closure status (canonical, pending Manager re-closure)

**Stage 11 closure status: rework loop complete on commit
`6e3aa045c`. All closure contracts PASS.** The Manager
re-closes Stage 11 in a separate gate. This log records the
canonical real merge and the rework loop. The prior
2026-06-04 closure (based on the fabricated single-parent
merge `72cfbcd44` and the soft T114a closure as a tooling
limitation) is INVALIDATED. The next gate is the Manager
re-closure decision on commit `6e3aa045c`. The next stage
(per the architecture doc) is Stage 12 (Stress Testing and
Benchmarking) or a future Stage 11 cycle that re-opens the
upstream merge activity.
